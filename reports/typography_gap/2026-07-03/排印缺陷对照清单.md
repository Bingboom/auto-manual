# 排印质量缺陷对照清单（生成 PDF vs 归档竖版成品）

日期：2026-07-03
对照基准：
- **生成侧**：`build.py pdf --config configs/config.us-en.yaml --model JE-1000F --region US --data-root tests/fixtures/phase2`
  → `docs/_build/JE-1000F/US/en/pdf/manual_je1000f_us_en.pdf`（20 页，XeLaTeX 3 轮，TeX Live 2026）
- **归档侧**：`~/Documents/最新竖版归档/美规/Jackery Explorer 1000 V2/Jackery Explorer 1000 v2.pdf`（InDesign 成品，130×185mm，52 页）

> ⚠️ 本轮生成用的是 CI 冻结样本快照（`tests/fixtures/phase2`），并非真实 `data/phase2`。
> 标注【待复测】的条目可能是样本数据缺陷而非引擎缺陷，**修复动手前先刷新真实快照重跑一次，把数据缺陷从清单剔除**。

已确认达标项（不在清单内）：开本 130.1×185.1mm 与归档一致；黑色章节条 / WARNING 警示框 / 双栏安全条款等设计元素齐备；CMYK 印刷链；页脚页码体系。

---

## P0 — 内容遮挡 / 丢失（发布阻断）

| # | 缺陷 | 证据 | 归档参照 | 疑似根因 | 归属 |
|---|---|---|---|---|---|
| 1 | ✅**已修复(2026-07-04)** 页码叠印在表格文字上：页码 "08" 直接压在 LCD 图标表倒数第 2 行的文字上 | 生成 p8（`evidence/gen_p08.png` 右下） | 归档任何页页码均在版心外 | **实际根因**：`HBLcdIconTable` 是 tcolorbox 框内 tabular——盒子不可跨页,15+ 行大表整体溢出版心 345pt(`Overfull \vbox`)压进页脚区 | 引擎 |
| 2 | ✅**已修复(2026-07-04)** 表格末行被页边裁切。**复盘发现实际比记录严重：tcolorbox 的 `clip upper` 把溢出的第 16–27 行共 12 行内容静默裁掉**,不只是"末行切断" | 生成 p8 底部 | 归档表格从不贴底 | 同上——`clip upper` 裁切一切超出盒框的内容 | 引擎 |

**#1/#2 修复记录(2026-07-04)**：
- `docs/renderers/latex/components_base.tex`：`HBLcdIconTable` 由「tcolorbox 框 + tabular」改为 **longtable**(可在行边界跨页;`\endhead` 在每个续页重画顶边线;`LTpre/LTpost=0` 保持原间距)。代价:该表失去圆角外框(`HBcomp_table_outer_arc`)。
- 顺手加固(HTML/Word 分支同样受益)：`renderers_lcd_icons.py`、`renderers_symbols.py`(3 处)的 list-table 加 `:class: longtable`;10 个语言的 `10_troubleshooting.rst` 模板同样处理。
- 验证:重建后 345pt `Overfull \vbox` 消失;p8 页码回到页脚、不再叠印;第 16–27 行内容找回,表格在 p8→p9 行边界断页、边线闭合;`build.py check` 通过;`tests.test_csv_page_renderers` + `test_csv_page_builder` 65 项单测全绿。
- ~~遗留同型隐患(归入 #5)：symbols 页 LaTeX 分支的 `\HBSymbolSignalRow` 系列、`HBLcdModeTable`(即 line 766 的 45.8pt 溢出)、spec 页 `tabularx` 仍是"盒/tabular 不可跨页"结构~~ → **已于 2026-07-04 全部处理,见下方追加记录**。

**同型隐患修复记录(2026-07-04,第二批)**：
- `HBSymbolTable`(components_base.tex)：同配方 longtable 化;表头行放进 `\endhead`,跨页自动重复表头。
- `spectable`(components_spec.tex + renderers_spec.py)：由「tcolorbox + tabularx」改为 longtable;原先由盒子 overlay 绘制的左列灰底改用 `\columncolor{HBSharedTableLeftBg}` 等效实现;渲染器不再发射 tabularx 行,列宽定义收敛到组件层。
- `HBLcdModeTable`(components_base.tex)：**根因不同**——不是跨页,而是 `\multirow{3}` 的状态标签比它跨的 3 行更高,文字溢出表格压到下方正文,且 `clip upper` 静默裁切。重构为无 multirow 的三层嵌套结构(图片|组 → 状态|动作块 → 动作|说明),每行行高随最高单元格自然生长;`clip upper` 从框中移除(未来溢出必须可见,不许静默裁切)。宏签名不变。
- 模板统一:7 个语言模板(us-en/us-es/us-fr/us-pt-br/eu-es/eu-fr/jp)的 `05_operation_guide_placeholder.rst` 内联 raw-latex 模式表(历史分叉副本,绕过组件宏)全部替换为 `HBLcdModeTable` 宏调用,与 eu-en/eu-uk/eu-kr/eu-de/eu-it 对齐——12 个语言模板现在走同一组件。
- 验证:`Overfull \vbox` **全部清零**(含 line 766 的 45.8pt);p11 模式表标签完整收进单元格、不再压正文;p16 spec 左列灰底保留;symbols 表正常流排(19 页);`build.py check` 通过;65 项渲染器单测全绿。证据:`evidence/final_mode.png`、`final_spec.png`、`sym_p2.png`。
| 3 | ✅**已修复(2026-07-04,复测定案为引擎缺陷)** 表格图标列整列空白 | 生成 p4、p8（`evidence/gen_p04.png`） | 归档同表每行有图标 | **实际根因**:raw-latex 渲染器用裸文件名 `\IfFileExists` 找图,但 `_attachments` 附件从未被拷进 LaTeX 构建目录(Sphinx 只拷 `.. image::` 引用的图),`\HBImageOrPlaceholder` 静默画空占位。真实快照复测同样空白 → 与数据无关 | 引擎 |
| 4 | ✅**复测定案(2026-07-04):样本数据缺陷** 整页空白只剩页码:p7 除页码外无内容 | 生成 p7 | — | 真实 `data/phase2` 快照下无此页;fixture 样本缺资产所致,引擎无缺陷 | 数据(已排除) |

**#3 修复记录(2026-07-04)**:`tools/build_docs_export.py` 新增 `_copy_attachment_images_for_latex`——LaTeX 构建时把 bundle 内 `_repo_assets/**/_attachments/**` 的图片平铺拷进 latex 目录(本次 38 张)。复测(真实快照):LCD 表 27 行图标全部渲染、symbols 图标行全部渲染。证据:`evidence/icons_fixed.png`、`sym_icons.png`。

## P1 — 排印质感（与归档成品的可见差距）

| # | 缺陷 | 证据 | 归档参照 | 疑似根因 | 归属 |
|---|---|---|---|---|---|
| 5 | **表格跨页无控制**：断点任意、无行保护（keep-together）、续页无 "continued" 标识 | 生成 p3→p4 | 归档由人工控制断点，单元格从不跨页断开 | Sphinx 默认 longtable 输出，`components_base.tex` 未做跨页策略 | 引擎 |
| 6 | **同一张表行高忽大忽小**：符号表续页大片留白（单行文字配三行行高），规格表却过挤、文字贴线 | 生成 p4 vs p17（`evidence/gen_p17.png`） | 归档行高统一、留白均匀 | 行高完全随内容浮动，无 `\extrarowheight`/行高参数；`layout_params.csv` 无表格行距项 | 引擎 |
| 7 | 🟡**部分修复(2026-07-04)** 单元格内孤行："Surge Peak" 断成孤行 | 生成 p17 | 归档单元格内换行有人工把关 | spec 值列 `\raggedright`→ragged2e `\RaggedRight`(允许断字、断行更均衡)+ `\finalhyphendemerits`;p5 端口 overview 已均衡为两行,p16 规格页因内容物理超一行仍余孤行——**遗留为数据措辞项**(可在 Base 里用不换行空格绑定 "3000 W Surge Peak") | 引擎(已尽力)+数据 |
| 8 | ✅**复测关闭(2026-07-04)**:真实数据高DPI下 `LiFePO₄` 下标渲染正确,前判为低分辨率截图误读;`FRAGILE_UNICODE_REPLACEMENTS` 补丁工作正常("补丁式"架构注记保留至 #11) | 生成 p16(`evidence/spec_zoom.png`) | — | — | 误报(关闭) |
| 9 | ✅**已修复(2026-07-04)** 断字激进 | 生成 p1、p2（`evidence/生成_p1.png`） | 归档同样存在断字,但密度更低 | 新增全局断行参数(`layout_params.csv` LINE BREAKING 块 + `layout_core.tex`):`hyphenpenalty 50→800`、`tolerance 200→1500`、`doublehyphendemerits`、`finalhyphendemerits`、`emergencystretch 1em`。修后全文行尾断字 11 处(19页,≈0.6/页) | 引擎 |
| 10 | ✅**已修复(2026-07-04)** 两端对齐词距不匀 | 生成 p18（`evidence/gen_p18.png`） | 归档词距均匀 | 同 #9:`tolerance`+`emergencystretch` 给对齐引擎泄压空间 | 引擎 |
| 11 | ✅**已治理(2026-07-04)** 组件间距靠负值补丁治理:三处 "cancel Sphinx glue" 负补丁 | `data/layout_params.csv` | — | **逐一查明真身并改为"正声明"**:① `comp_subbar_tighten` 是死参数(组件已无引用)→ 删除;② `comp_twocol_tighten` 实际抵消的是 multicol 包的 `\multicolsep`(12pt)而非 Sphinx → 环境内 `\multicolsep=0` + 新键 `comp_twocol_before/after` 直接声明(FR/ES 语言覆盖同步换算);③ `section_after_fix` 与 H1 pill after skip 双点叠加 → titlesec afterskip 归零,单点收敛到 `comp_h1_pill_after`(净值折算,含语言覆盖)。**验收:763 行文本坐标逐行对比零漂移(0.0pt)**;overfull 保持 0;check + 65 单测绿 | 引擎(已治理) |
| 12 | **表格视觉风格不同代**：生成侧为全框线网格+灰底首列；归档为开放式行（仅横向细分隔线、无竖线、行距宽松） | 生成 p17 vs 归档 p3（`evidence/归档_p3_important.png`） | — | longtable 样式未按品牌规范重写 | 引擎（需设计定稿） |

## 追加发现(2026-07-04,P1 批次中新捕获并已修复)

| # | 缺陷 | 根因与修复 |
|---|---|---|
| 15 | ✅ **WHAT'S IN THE BOX 三格图片全空 + 标签窄栏折行且底边裁切**(38.7pt hbox 溢出 ×3) | 双重缺陷:(a) `common_assets` 模板图片未进 LaTeX 目录(静态清单只列 2 张)→ 资产拷贝函数扩展为同时平铺 `common_assets` 树(70 张);(b) `\HBInBoxCell` minipage 宽 `0.29\linewidth`——在 m 列内 `\linewidth` 已是列宽,0.29 一乘把标签压成全宽 10% 的窄条,'Explorer' 都放不下,且 `clip upper` 静默裁底 → 改 `\linewidth` 全列宽 + 移除 clip。证据:`evidence/inbox_final.png` |

**里程碑(2026-07-04)**:真实数据构建 `Overfull \vbox = 0` 且 `Overfull \hbox = 0`,溢出/裁切类缺陷全部清零。

## P2 — 字形 / 资产

| # | 缺陷 | 证据 | 疑似根因 | 归属 |
|---|---|---|---|---|
| 13 | **品牌字体未生效**：Gilroy 回退到默认字体,字形与归档不一致 | 全文 | 本机缺 `patch_latex_fonts.py` 要求的 4 个指定命名字体文件（`gilroy-regular-3.otf` 等）,未设 `AUTO_MANUAL_LOCAL_GILROY_DIR` | 环境 |
| 14 | ✅**复测定案(2026-07-04):样本数据缺陷** App 下载二维码为占位灰块 | 生成 p18 | 真实快照下 QR 正常渲染(`evidence/app_qr.png`),引擎无缺陷 | 数据(已排除) |

---

## 修复路线建议（按序）

1. **先复测**：刷新真实 `data/phase2` 快照 → 重跑同一构建 → #3/#4/#14 若消失则划掉，剩余即纯引擎工作量。
2. **P0 表格层重写**（`docs/renderers/latex/components_base.tex`）：
   - longtable 底部保护（页脚区隔离，解 #1/#2）
   - 行高参数化进 `layout_params.csv`（表格行距、cell padding，解 #6）
   - 行级 keep-together + 续页表头策略（解 #5）
3. **段落排印策略**：`\hyphenpenalty`/`\tolerance`/`\emergencystretch` 全局调参（解 #9/#10）；评估从源头拦 Sphinx glue 替代逐点负间距补丁（解 #11）。
4. **字体系统**：集中解决 Unicode 回退（#8）与 Gilroy 资产分发（#13）。
5. **表格样式对标**（#12）：需要设计侧先定稿"竖版表格规范"（是否去竖线/灰底），再实现。
6. **回归机制**：把 `~/Documents/最新竖版归档` 的对应机型 PDF 作为逐页视觉回归基线,每次渲染层改动后对照（归档索引见该目录下 `资料清单梳理.xlsx`）。

## 证据文件

`evidence/` 目录：
- `gen_p04.png` 符号表续页（图标列空白、行高失控）
- `gen_p08.png` LCD 表（页码叠印、末行裁切、图标空白）
- `gen_p17.png` 规格表（行高过挤、Surge Peak 孤行、LiFePO 下标）
- `gen_p18.png` warranty（词距不匀、QR 占位）
- `生成_p1.png` / `生成_p3.png` 前言与符号表首页
- `归档_p1.png` / `归档_p3_important.png` 归档成品参照（封面 / 规格页）
