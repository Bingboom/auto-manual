interface:
  display_name: "Manual Revision Back-port"
  short_description: "Reverse-sync a reviewer's tracked-changes manual .docx into repo templates + Feishu source"
  default_prompt: "Use $manual-revision-backport to extract a reviewed Word manual's changes, map them to current RST or Feishu sources, apply only confirmed deltas, and verify zero residuals."
