interface:
  display_name: "New Region / Compliance Manual Line"
  short_description: "Stand up a brand-new (Model, Region, Language) manual line end-to-end"
  default_prompt: "Use $new-region-line to onboard a new market or output language end to end: collect operator inputs, add config/manifests/templates and code registration, gate Feishu source-table intake, sync, and validate before a PR."
