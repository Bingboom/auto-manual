interface:
  display_name: "Markdown RST Template Intake"
  short_description: "Map Markdown manuals into repo RST templates"
  default_prompt: "Use $markdown-rst-template-intake to map this Markdown manual into the repo's RST template family, preserve placeholders, update the correct recipes, and validate the build."
