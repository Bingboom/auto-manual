# JE-300D EU English operator-designated source

The Git-frozen PDF-compatible Illustrator file is the content authority for
this Web engineering target. The DingTalk demand record is `QviOQihBNj` in
Base `YndMj49yWjP03jNjCDojvAQdJ3pmz5aA`, table `97v7518`. That record has no
published-PDF link, so this source is intentionally recorded as a candidate
rather than a formally published revision. The operator explicitly waived
version comparison and instructed the build to use the existing AI original.

The attachment filename contains `HTE150300A`, while the source specification
table and cover identify the target as `JE-300D` / `Jackery Explorer 300D`.
The model identity is therefore taken from the manual content and specified
record, not inferred from the attachment filename.

`phase2/` is the immutable target-scoped data root used by the normal build.
`scope-snapshot.json` is the read-only batch reconciliation snapshot; its two
composite rows are excluded from executable batch work and are not pending
manual targets.
