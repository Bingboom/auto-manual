# 说明书信息架构盘点报告（Phase A · 2026-08）

Status: Phase A deliverable — 待操作者裁决 · Owner: 夏冰 · 执行 2026-08-20

本报告是 [`product_skeleton_library_requirements.md`](product_skeleton_library_requirements.md)
Phase A 的交付物：对现有说明书语料做只读的信息架构盘点，为
[`Product_Skeleton_Library_Design.md`](Product_Skeleton_Library_Design.md) 的骨架库设计提供证据。

语料来源：操作者提供的 `信息架构分析/` 目录（56 份 PDF）+ 2026-08-20 同日补充的 4 份
（HTE152 日规真本、HTE153 澳/韩/巴西规），共 **59 份 PDF**（按字节去重的磁盘文件口径：
原目录 56 含墨规重复路径 1 份，56−1+4=59），两个品类。
方法：机械化抽取（PyMuPDF）→ 17 个分解 agent 归一化章节树（11 个文本批 + 6 份转曲件视觉读取）
→ 5 个分析透镜（复用矩阵 / 骨架归纳 / 品类差集 / 区域语言归因 / 仓库差集）。

## 0. 结论摘要

1. **多骨架成立，且数量应该很少。** 本报告推荐 4 条正式锚点 + 1 条待收敛 + 3 本 outlier，
   不是"每产品一个骨架"。重构测试：纯删除 **47/58 = 81.0%**（首轮 67.3% → 口径统一 78.2% →
   终账 81.0%，可由 corpus_audit_2026-08/stats.py 复算）；
   允许每本 ≤1 条 overlay 后 **55/58 = 94.8%**。
   > **Phase B 已把锚点结构升级为二维稀疏键 `(骨架家族 × 体例族)` 的 5 个格子**，A5 撤锚为
   > `house_style_version`。见 [`Product_Skeleton_Library_Design.md`](Product_Skeleton_Library_Design.md) §4。
   > 本报告保留 A1–A5 编号作为证据链的可追溯坐标。
2. **瓶颈被量化了：当前产线只能生成 15/58 本（25.9%），SKU 覆盖率 5/22 = 22.7%，
   36 本（62.1%）是产线外盲区，加电包品类 0% 可生成。** 日规是最大群体（18 本 = 31%）却只有 1 本可生成。
3. **最大的结构差异来源不是品类，是区域体例（43%）；最该自动化的不是章节，是语言编排。**
   页数 10→120 页的 12 倍跨度里，**语言数解释 82% 的方差**，章节数是弱预测器。
4. **"可复用模块"的上界是 19 个 topic 覆盖 92.0%（898/976，归一口径；终账，可由
   corpus_audit_2026-08/stats.py 复算）的槽位决策**——
   但这是骨架槽位复用率，不是文本复用率，后者必须另做一次测量。
   > **注意**：初版举的反证「中规是独立撰写而非翻译」经 Phase B 逐条比对**只对一半**——
   > 中规 15 个 topic 里 12 个与 `MAIN@INTL` 同源（安全条目 1–7/10 逐条同序对应、存储章逐句同构、
   > 故障码表四区规完全相同、LCD 图标集逐项对应），仅前言/保修/合格证三处自撰。
   > 上界声明本身仍然成立（另有日规加电包保修本地重写等反证），但这条例证要改写。
5. **语料本身带 3 处建档缺陷 + 12 条归一化警告**，落库前必须先修，否则骨架库会收进假骨架。

## 1. A1 语料登记

| 维度 | 分布 |
| --- | --- |
| 磁盘文件 | **59 份 PDF**（按字节去重：原目录 56 − 墨规重复路径 1 + 补充 4） |
| 去重后独立内容 | **58 份**（两组折叠：HTE153 美加规≡墨西哥规同一文件 MD5 `131f37a1…`；HTE152 错档件≡HTE154 日规正名件，文本逐字节相同、仅表格灰底有无） |
| 已分解 | **58/58 = 100%**（首轮账面 55/55 经 Phase B QC 证实是双错抵消、实为 54/55；本轮补齐并新增 4 本） |
| 品类 | 便携主机 **52** · 便携加电包 7（文件口径；独立内容口径为主机 51 · 加电包 7） |
| 容量段 | E100 / E240 / E300 / E500 / E600 / E1000 / E1500 / E2000 / E3000 / E3600 / E5000 |
| SKU | 22 个（HTE 18 个 + HTP 4 个） |
| 区规 | 独立内容口径：日规 18 · 美加规 13 · 欧英规 12 · 中规 7 · **澳规 3** · **韩规 2** · **巴西规 2** · 菲律宾 1 · 墨西哥 0（合计 58；墨规=美加规同文件别名）。登记行口径为日规 19 · 墨西哥 1（合计 60） |
| 页数 | 10 – 120 页 |

目录层级 `品类/容量段/料号/区规` 本身即可作为登记表主键，无需另建。

**文本可读性（已机械核实）**：**51 份**有文字层，**8 份为印刷用文字转曲件（真实文本 0 字符）**——
补充的 3 本 HTE153 全部有文字层，韩规与澳规由此各得首本可读书，两区规不再是「唯转曲/唯滞留」盲区。原 8 份为——
澳规 HTE133、韩规 HTE118、巴西规 HTE140、日规 HTE150、日规 HTE140（2 份）、
**美加规 HTE158（55 页）**、日规 HTE110（22 字符）。

> 我的预处理曾按 800 字符阈值筛查转曲件，漏判了 HTE158 美加规——它的 55 个页边界标记
> 产生 871 字符开销，正好越过阈值。**该本的分解仍然有效**：负责它的 agent 自行发现
> `get_text()` 全空、改用逐页渲染视觉读取，并主动标注"registry 里 chars 应为 0"。
> 教训写进建议：转曲件检测必须扣除标记开销、按每页真实字符数判定。

**工程风险（需单列）**：所有基于文本的 diff / QC / 术语扫描 / 回写工具对这 8 份**静默失效**
（不报错、返回空）。骨架库入库时应把"有无文字层"作为一等字段。

## 2. A2 结构分解

归一化到 39 个 topic id（Phase B 归一后收敛为 **30 个**），共 **983 个** topic 槽位实例
（原始分解口径；归一后 **976 个**），平均 16.9 章/本（原始）/ 16.8 章/本（归一）。

补充的四本**未产生任何新 topic id**——30 词表对第七个区规封闭（巴西规 ANATEL 映射既有 `regulatory_compliance`）。

分解口径：多语合订本只记第一语言块的完整序列，语言重复模式另记；
只记章节骨架不抄正文；忠实记录缺章/多章/顺序差异。

## 3. A3 复用度分析（三档）

（归一口径，合计 976 槽；终账，可由 corpus_audit_2026-08/stats.py 复算）

| 档 | id 数 | 槽位数 | 占比 |
| --- | --- | --- | --- |
| universal（跨品类通用） | 15 | 762 | **78.1%** |
| category_common（品类内通用） | 4 | 136 | 13.9% |
| specific（限定 SKU/区规） | 11 | 78 | 8.0% |

7 个 topic 覆盖率 100%（58/58）：`cover` `box_contents` `product_overview` `lcd_display`
`operation` `charging` `specifications`。其后 `safety_info` 98.3%（57/58）·
`troubleshooting` 91.4%（53/58）· `warranty` 87.9%（51/58）· `toc` 79.3%（46/58）·
`back_cover` 74.1%（43/58，归一后中规封底归零并入合格证）·
`storage` 65.5%（38/58，原名 `storage_maintenance`）· `symbol_meaning` 63.8%（37/58）·
`preface_important` 53.4%（31/58）。

**核心数字：19 个 id（universal + category_common）承担 898/976 = 92.0% 的槽位决策**
（归一口径；终账，可由 corpus_audit_2026-08/stats.py 复算）。
归一后 11 个 specific id 只解释 8.0%，其中纯单例 1 个（`smart_home_backup_ess`）。

模块库规模估算：**19 个共享模块 + 4 条区规骨架 + 约 8 个能力插拔件 + 4 个合规 fragment**
即可覆盖约 90% 的槽位决策。

> **必须写明的上界声明**：以上是 **topic 存在性**的复用率，**不是文本复用率**。
> 语料内已有反证——中规 7 本是独立撰写而非翻译（品牌/App/售后体系全换）；
> 日规加电包的保修是渠道条款本地重写；HTE154 中规的 LCD 章功能集与 EU/US 本不同构。
> 78–90% 应读作"骨架槽位可复用比例的上界"。

## 4. A4 骨架归纳与重构测试

### 4.1 候选锚点

| 锚点 | 成员 | 覆盖 | 纯删除通过 | +≤1 overlay |
| --- | --- | --- | --- | --- |
| **A1-INTL-MAIN** 国际多语主机 | ~~25~~ → **27**（独立内容口径，墨规别名不计） | 15 SKU × 7 区规 × E100–E3600 | ~~16 (64.0%)~~ → **22 (81.5%)** | 27/27 |
| **A2-JP-MAIN** 日规三段式 | 14 | 日规单语主机 14（终账构成置换：−错档件 −2 加电包，+HTE119 +No-225 +HTE152 真本） | 13 (92.9%) | 14/14 |
| **A3-CN-MAIN** 中规国内线 | 7 | 中规单语（电小二） | ~~4 (57.1%)~~ → **7 (100%)** | 7/7（0 条 overlay） |
| **A4-INTL-BP** 国际多语加电包 | 4 | HTP011/015/017 欧英+美加 | 3 (75.0%) | 4/4 |
| **BP@JP**（A5 已撤销改档） | 3 | 日规加电包（对 JP@v1 序列；格规范序列仍 pending） | 2 (66.7%) | 3/3 |

> A5 撤销去向：HTE119 日规 → MAIN@JP（纯删除通过）、HTP007 → BP@JP（T7）、
> HTE110 日规 → outlier（Phase A 曾将其双计致 59>58）。

**全库汇总：纯删除 ~~37/55 = 67.3%~~ → **47/58 = 81.0%**（终账，可由 corpus_audit_2026-08/stats.py 复算）；允许 ≤1 条 overlay **55/58 = 94.8%**。**
需 overlay 的本数 15 → 8（T1×3、T2×1、T3×1、T4×1、T6×1、T7×1）；无法重构 3 本（均不变）。

> **Phase B 重测更正（2026-08-20 同日）**：口径统一到「印刷页序、同页不计先后」后，
> A1 有 3 本（HTE147 欧英/HTE156 欧英/HTE156 美加）实测为**假分叉**（目录序与版面序一致，
> 是纯文本抽取序骗了分解员），A3 有 3 本共印刷页因而成为同槽平局。
> **T2（storage↔troubleshooting 互换）成员 7 本 → 1 本**（仅 HTE157 美加规为真分叉，
> 且两章在不同印刷页），应重新定性为 per-book 槽位对参数而非骨架 overlay。
> 补语料后（HTE153 澳/韩/巴西三本全部纯删除通过、HTE152 日规真本 0 overlay），
> A1 成员 25（含墨规别名）→ 27（独立内容）、纯删除 19→22。若要给 Phase A 定阈值（§9.8），
> 可用数字是 **81.0% / 94.8%**（终账，可由 corpus_audit_2026-08/stats.py 复算）。
> 另一处 T2 类记录经几何核实为**幻影**：美加/欧英「symbol-first 互换」不存在，两本都是 UMI 在前，
> 只因 UMI 从不进目录而看起来像互换——七本 HTE153 之间连一条槽位对互换都没有。

### 4.2 锚点数量为什么是 4 条

- **不能更多**：按区规拆会得到 8+ 个锚点，但 A1 的 25 本里 16 本是同一条序列的**纯子序列**
  （历史证据；终账口径为 27 本里 22 本），跨 7 个区规、1–6 种语言、6 个容量档。区规差异实测全部落在四类参数上（合规载体与重复粒度、
  语言集与块长、单位制/法人主体、可选章开合）——把参数当锚点会得到一组彼此只差删除的假骨架。
- **按 SKU 拆更糟**：同 SKU 跨区规的分叉（平均对称差 5.42）**大于**同区规跨 SKU（3.20）。
  SKU 不是骨架的一级维度，能力位才是。
- **不能更少**：A2 ← A1 需 2 插入 + 1 重排（`usage_precautions`/`disclaimer` 在 A1 全 25 本零命中）；
  A4 ← A1 需 2 重排（troubleshooting 前置、specifications 前置）。均超出验收线。

### 4.3 无法重构的 3 本

1. **HTE118 澳规**（代际滞留）：模板版本 JAK-UM-V1.0，同 SKU 美加规姊妹本已 V2.0 且是 A1 纯通过成员。
2. **HTE110 欧英规**（老范式）：章序轴与语言编排轴同时不兼容，任何 overlay 数量都救不回来。
3. **HTE110 日规**：需 2 次重排 + 末尾挂一本自带独立编号空间的 `app_user_manual` 附录。

HTE110 两本**跨区规复现 specifications 首章**，是全库唯一一条跨区规复现的世代签名，
可考虑合并为"HTE110 世代锚点"。

### 4.4 测试未覆盖的两层（不能宣称通过）

- **页级重构**：本测试只验章序。语料几乎每本都有多章共页（HTE147 欧英规 13 印刷页装 11 章），
  锚点若只存 (章, 起始页) 就丢掉共页约束（重测 10 本里 7 本有两章共一印刷页）。

  > **本条曾有一处错误断言，已更正**：初版写「共页两章的文本抽取顺序**系统性**与目录相反」。
  > Phase B 对全库 16 本共页书机械核实：**只有 2 本**标题顺序翻转（HTE147 欧英、HTE132 欧英），
  > 是低频而非系统性。而目录序与版面序实测 **6/6 完全一致**——真正不可信的是**第三种口径**
  > （PyMuPDF 纯文本抽取序），且它有两种失效形态：(A) 标题整体翻转；(B) 标题顺序正确但正文
  > 引导句被抽到前一章标题**之上**，任何「按首次出现判序」的启发式都会在此翻转（初版完全未记录）。
  > 结论：章序不得由 `get_text()` 字符偏移推导，只能由 block bbox 的 (y, x) 几何序或视觉核实得出。
- **呈现层级**：同一 topic 有五态——独立章进目录 / 有标题不进目录 / 无标题块 / 降级为子节 / 缺席。
  `fcc`（13 本：美加 11 + 巴西 2；HTE153 美加寄生块尚欠台账登记，补登后 14）与
  `user_maintenance_instructions`（27 本）**全库零次进入目录**。
  只按目录抽骨架每本漏 3–4 块。

## 5. 差异归因（variance taxonomy）

两把尺子结论不同，都要说：

**尺子一（页数方差）**：语言编排 **82%** / 块长 18%（品类直接解释力仅 3%）。
页数公式在 25 本合订本上零例外：

```
pages = F(L) + L × B + K       F(L) = 1 + ⌈L/3⌉ + ⌈L/3⌉    K = 1
```
（L = 语言数，B = 每语言块页数，实测 B ∈ 8..31）

**尺子二（章节集合差异，250 cell 基线）**：

| 类别 | 占比 | 判定 |
| --- | --- | --- |
| **区域体例**（非合规的区域惯例） | **43%** | 先裁决再固化为区域骨架，**不要数据驱动**——同一类别里规则与分叉混在一起，拟合会把分叉学成规则 |
| 型号能力 | 19% | 机制已建好，缺数据覆盖 |
| 品类 | 12% | 做成独立骨架族 |
| 区域合规 | 8% | **绝不能数据驱动**，必须是法务签字的显式裁决表 |
| 不可解释残差 | 17% | 其中至少 8 cell 是归一化缺口而非真差异 |

**结论：最该数据驱动的是语言编排**——它占页数方差 82%、判断量为零、是
`语言集 × 块模板` 的确定性乘法。而产线现状恰好相反：`manual_eu.yaml` 是把 17 页的语言块
**手抄 6 遍的 424 行**，`data/model_languages.csv` 只有 6 行，而语料里欧英规实测有
**4 套不同语言集**。方差占比最大的一类，机制覆盖率最低。

## 6. A5 仓库差集与产线覆盖率

### 6.1 覆盖率四档（这是"多产品扩展瓶颈"的量化）

> 口径声明：档 1 ⊂ 档 2 ⊂ 档 3 为累计口径，档 4 为其余；19（档 3 旧值）+36≠58 的不闭合
> 由此修复：22+36=58。产线四档不在 corpus_audit_2026-08 数据集内，本表按仓库状态另行核算。

| 档 | 本数 | 占比 | 说明 |
| --- | --- | --- | --- |
| 1. config 已写进 targets，直接可构建 | **7** | 12.1% | HTE153 US/EU/JP/**AU**/**KR**、HTE152 CN、HTE140 巴西规 |
| 2. 有规格数据 + 区规 config（加一行即可） | **15** | **25.9%** | **当前产线真正能生成的全部**（含 HTE153 BR：规格齐 + `config.pt-br.yaml` 在，仅缺 targets 一行） |
| 3. 有能力建档但规格零行（需走入库） | **22** | 37.9% | 累计口径，含档 1/2；HTE153 AU/KR/BR 属已建档 SKU，含于本档 |
| 4. 产线外盲区（仓库无任何记录） | **36** | **62.1%** | 19 本属 11 个从未建档的 SKU；17 本是已知 SKU 的未接线区规 |

- **SKU 覆盖率 5/22 = 22.7%**（不变——三本新书都属已覆盖的 HTE153）
- **加电包品类 0 本可生成（0%）**
- **日规 18 本只有 1 本可生成（5.6%）**，而 `manual_jp.yaml` 连 safety 页都没接线
- **档 1 从 5 涨到 7 不是因为 config 变了**，而是因为语料此前没有 HTE153 澳/韩本；
  `JE-1000F_AU`/`JE-1000F_KR` 一直就在 `config.au-en.yaml` / `config.kr.yaml` 的 targets 里

### 6.2 反向差集：~~12~~ → **9** 个仓库目标在语料里没有参考骨架

`JBP-2000B_CN`、`JE-1000H_AU/KR`、`JE-2000E_KR`、`JE-3000C_PH`、`JE-1800B_JP`、
`JE-2000D_KR/US`、`JE-900B_JP`。其中 3 个 SKU 语料完全零命中。

**已结案 3 个**：`JE-1000F_AU/KR/pt-BR` 由 HTE153 补语料补齐——三者现在既有产线又有实体出货书，
是全库第一次可做「产线产物 vs 真实出货书」逐页对齐验证的目标。
**HTE153 因此被指定为骨架库的回归基准 SKU**：唯一七区规语料齐全 + 唯一七目标全建档，语料↔产线可双向对账。
`JE-3000C_PH` 有规格数据与能力行，但没有 `config.ph.yaml` 与 PH manifest——**菲律宾规产线实际不可构建**。

### 6.3 词汇差集（A5）

- **capability 词表**（9 列 × 31 行）：**9 列里 6 列零 gate**（列建了但没有章节挂上去）。
  缺 `App/联网` 列（`app_setup` 在 17 份 manifest 里全部无条件出页，而语料 16 本无 app 章）、
  缺 `AC输入/AC输出` 存在位、缺 `category` 维度、缺 6 项硬件形态位。
- **Row_key / Slot_key**：**先收敛再扩**——功率档同时出现在 Row_key 与 Slot_key 两条轨上
  （`usb_c_100w` vs Slot_key `100w`），16 个 Slot_key 值混着三类语义。扩到 22 SKU 前必须拆成
  `Slot_key`（版面位）+ `Variant_key`（功率/区规档）。
- **必需占位行**：`03_product_overview` 的 `default` 硬写了 `SIDE_AC_INPUT_*`/`FRONT_AC_OUTPUT_*`，
  任何无 AC 输入的机型都过不了闸门，**唯一逃生口是整份 fork**——JE-300E 已付过这笔账
  （1 contract + 5 份 RST），语料里等着再付的还有 HTE150 日规、HTE162、7 本加电包。
- **page_registry 页种**（4 行）：有 `sku_scope` 却**没有区规与品类维度**，而语料的第一切分维度恰恰是区规。
  `lcd_icons_blocks.csv` 只有 Model 列，而 LCD 图标集实证按区规分叉（HTE154 中规比 EU/US 多 5 项）。
- **全仓零命中的页种**：`regulatory_compliance` / RED DoC / UKCA（欧英规 12 本中 11 本实测
  挂 DoC，缺 HTE162 欧英规=悬案；另巴西规 1 本 ANATEL 同 id）、中规合格证（7 本，7/7）、
  日规 `usage_precautions`（16 本）与 `disclaimer`（15 本）（均以 18 本日规为分母）。

### 6.4 三处产线骨架与实测相反

1. **日规**：`manual_jp.yaml` 硬接了一个独立 `symbol_meaning` 页，而语料 18 本日规一律把符号
   降为安全章子节；同时缺 `safety_info`（`safety_ja.rst` 存在但零引用）、缺 `usage_precautions`、
   缺 `disclaimer`。**同时缺章又多章。**
2. **中规**：`manual_zh.yaml` 无 safety 页、无封底、无合格证，而语料 7 本中规 100% 把前言+保修
   压进安全页并以合格证兼封底。
3. **FCC**：产线 JE-1000F US 把 FCC 做成独立页（`p22_01_fcc`），而语料**所有**美加规本都是
   寄生在装箱单/概览页的无标题块。

## 7. 数据质量缺陷（落库前必修）

### 7.1 已机械核实的建档缺陷

1. **HTE153 美加规 = 墨西哥规是同一份文件**（MD5 `131f37a1…`，4,862,894 字节，两处路径）。
   墨西哥规无任何本地化差异：客服号仍是美国号、封底仍是 Fremont CA、无墨西哥进口商信息。
   → 需操作者裁决是"有意复用"还是建档误绑。若有意复用，锚点成员表应记一条文件 + 两条
   (SKU, 区规) 映射并显式标记别名，否则任何按区规统计的报表都会重复计数。
2. **HTE152 日规疑为错档**。文件名 `HTE1542000A-JP` 自证属 HTE154；封面产品为 JE-2000F。
   实测：与 HTE154 日规是**两份不同的 PDF 文件（MD5 不同），但抽取文本完全相同**
   （text MD5 `51d26e771b74`，均 20,333 字符）——即同一本手册的两次导出。
   > 更正上游分析的一处措辞：agent 称"字节完全相同"，实为**抽取文本字符数相同**；
   > 文件字节并不相同。错档结论仍成立（依据是文件名与封面），但依据不是字节相同。
3. **HTE140 日规目录下有 3 份文件**，其中两份 28 页转曲件**不是重复件**（见 §7.3）。

### 7.2 归一化警告（10 条，摘录最高优先级 4 条）

- **W1 重复文件在污染分母**：有效独立文档 54–55 份而非 55 本，所有覆盖率有 ±1 本噪声。
- **W2 区规标签字符级分叉**：HTE110 一本写「日**規**」（繁体），其余 17 本写「日**规**」。
  任何 group-by region 都会把日规族切成 17+1。**入库前必须做枚举归一。**
- **W3 同一内容被三个 id 分记**：中规合格证有 `certificate_of_conformity` / `qc_certificate` /
  `quality_certificate` 三名，另 4 本记进 `back_cover`——**Phase B 已核实：真实覆盖率是 7/7 = 100%**，
  归一为 `cn_conformity_certificate` 后成为中规线的强制尾槽（兼封底），
  而中规 `back_cover` 覆盖率归零（0/7）。归一后词表 39 → 30 个 id、单例 10 → 1，
  核心 19 个 id 承担的槽位决策为归一终账 **92.0%（898/976）**（勘误：原文「从 89.9% 升到」
  的 89.9% 系旧算式值，旧口径 842/926 实为 90.9%；勘误史见 §11）。
- **W5 "缺章"多数是承载位置变化，不是内容缺失**：`preface_important` 日规 0/18 是因为印在封面；
  `storage_maintenance` 日规 0/18 是拆进了规格表与充电章。**把覆盖率读成"内容存在率"会高估区域差异。**
  建议 topic 带 `presentation_level` 属性，`absorbed` 与 `absent` 必须分开。

- **W11（Phase B 新查出，高）抽取文本含 NUL 字节导致 grep 静默假阴性。** HTE153 中规 5 个、
  HTE156 美加规 2 个——源于 PDF 字体把空格映射到码位 0（如 `USB-C\0to\0USB-C\05A`、`130\0°C`）。
  后果有三：grep 判为二进制文件后**静默返回空**（本轮就因此第一轮只查出 6/7 中规合格证，加 `-a` 才 7/7）；
  按空白分词的术语扫描会把 `USB-C to USB-C 5A` 读成一个 token；**NUL 是非法 XML 字符，会让
  RST/XML 构建硬失败**。注意这类文件**有**文字层，所以 §1 建议的"有无文字层"一等字段拦不住它——
  这是第三类静默失效，需要独立的字节合法性检查。
- **W12 覆盖率分母口径**：按"独立内容"计是 58；按 (SKU, 区规) 组合计是 **57**
  （墨西哥规与美加规共用一份文件）。报表须声明用哪个分母。
- **W13（Phase B 补语料时新查出）白色叠印不可见 token**：HTE153 巴西规目录页的文字层里藏着
  `color=ffffff` 的「01-22」，与可见的「01-18」bbox 重叠——渲染不可见，但**会进文本层**，
  污染任何基于文本层的 diff / QC / 术语扫描。继 W11（NUL 字节）之后的**第三类静默陷阱**，
  同样打在"有文字层"的文件上。文本抽取需加不可见/叠印文字检查。
- **W14 已印刷的目录页码 off-by-one（两本）**：`LCD DISPLAY` 目录标折页 05、正文实印 04，
  欧英规 V2.0 母本与继承它的澳规本同病。注意设计文档的 `toc_order ⊑ layout_order` 闸门
  **查序不查页码，抓不住这类缺陷**——该门需补一列「目录页码 vs 实印折页号」比对。

完整 12 条在工作流产物中（见 §9 复现命令）。

### 7.3 我自己的两处执行偏差（如实记录）

1. **8 份转曲件，我最初判为 7 份**：HTE158 美加规（55 页、真实文本 0）被 800 字符阈值漏判，
   原因是页边界标记开销 871 字符。下游 agent 自行改用视觉读取并主动纠正，该本分解有效。
2. **1 份文件我按"字符数相同"判为重复件而跳过，判断错误**：
   `…(沙金色) V1.0-2026-07-10__No-225.pdf`。补分解后语料覆盖率为 **55/55 = 100%**。
   见 §7.4——它同时推翻了我的第一版结论。

### 7.4 HTE140 日规：同一本手册的两份独立登记文件（结论已更正）

补分解结果推翻了本报告第一版的判断，如实记录更正过程。

**第一版结论（错误）**：这是配色变体，实质差异只有封面一个型号 token 加一级标题，
因此建议把 `colorway` 列为骨架参数。

**更正后结论**：这是**已发生结构漂移的独立修订版，不是配色参数差异**。
机械比对（内容流字节数 + 位移容差像素差 + 全库色彩扫描 + QR 解码）：28 页中
**13 页逐像素一致、15 页有差异，共 14 组实质差异**。其中触及骨架层的四类：

| 类型 | 实例 |
| --- | --- |
| 标题层级变化 | 太阳能章新增 2 个 `■` 小节标题 + 1 处提级；App 提示由面板内注释升为面板外独立行 |
| 章序/分页漂移 | App 章「3.」小节跨页迁移（印刷 p23 底部 → p24 顶部），两页内容流字节数近乎互为反数 |
| 表格结构变化 | 规格页 USB-C 由合并单元格拆为两行，并交换 30W/100W 顺序 |
| 内容增删 | 新增一条安全条目、新增规格值 `2ポート最大8A`×2 与 `最大1500W`、删除 `(100V/15A)`、保修条款整段改写并把配件口径从"AC 充电线"扩大为"附属品" |

**身份层证据（决定性）**：封底 QR 解码出的**文档控制号本身不同**——
`16-0102-000275`（WH）vs `160102000113`（SG），且格式不同（带/不带连字符）。
两件在文档管理上就是两份独立登记文件。

**顺带查出两处已印刷的 QC 缺陷**（都在 SG 件上，WH 件已修）：
错误码 `F6` 在 SG 件被渲染成 `F☒` 豆腐块（且已转曲，无法再靠文本工具发现）；
安全章有 1 处**红色残留编辑标记**（全库色彩扫描：WH 件 0 个彩色像素，SG 件仅此 1 处）。
WH 件另修正了 `http→https` 官网链接与术语（`電力グリッド`→`電源供給`）。

**给骨架库的三条结论**：
1. **不可按配色折叠**。本件应作为独立骨架条目入库。真正的教训与第一版相反：
   看似"只换配色"的变体，实际会累积安全条目、规格值、表格结构、保修条款的漂移——
   这正是"复制式复用"的账单，也是骨架库要消灭的东西。
2. **PDF `CreationDate` 不能当版本先后依据**。WH 件的创建时间比 SG 件**早 6 分钟**，
   但内容状态明显更新（修了缺陷）——同批次导出的时间戳。登记应以内容状态与文档控制号为准。
3. **转曲件的 QC 盲区是复合的**：`F☒` 这类缺字符缺陷一旦转曲，文本工具与视觉抽检都容易漏过，
   只有逐页机械比对才发现。这加强 §1 的建议：转曲件必须走渲染 + 像素比对路径。

> HTE140 日规目录下的第三份文件（`Jackeryポータブル電源1500 New 取扱説明書.pdf`，
> 27 页、有文字层）已在文本批 B4 中分解，不是遗漏项。三份文件构成同一 SKU 同区规的
> **三个变体**（27 页有字层 / 28 页 SG / 28 页 WH），是全库单点最密集的分叉。

## 8. 对设计文档的修正建议

1. **A3-CN 是否独立成锚，由操作者拍板**——它技术上可表达为「A1 + 中规 overlay」（1 重排 + 1 插入），
   够不上"不可裁剪"；保留它的理由是**语义与血统**（中规独立撰写而非翻译），不是序列距离。
   这是唯一一处建议由人裁决而非数据裁决的边界。
2. **加电包立锚点，但形态是「host skeleton + pack overlay」**，不建议现在升为一级骨架库——
   样本量只有 4 个 SKU、缺中规/澳规/韩规加电包。已给出三条升级判定规则（见工作流产物）。
3. **容量段不应做骨架一级键**，只该作为能力位默认值来源。结构阈值只有两处：
   `ups_mode` 在 E300/E500 之间出现，E3600 Pro Max 形态扩张。
4. **命名线要分两类看**：档位词（Explorer/HomePower/Pro/Plus/Ultra/New）**不带结构差异**；
   形态词（FridgeGuard/SlimPower/Battery Pack/Pro Max）**带**。跨区改名已实证
   （同一料号 HTE151 在日规叫 SlimPower H1、美加规叫 FridgeGuard），
   **产品名不可做 join 键，(SKU, region) 才行**。
5. **设计文档 §6 的改造清单需补三项**：语言块参数化（收益最大）、
   `presentation_level` 四态属性、转曲件"有无文字层"一等字段。

## 9. 待操作者裁决

1. **A3-CN 独立锚点还是 A1 区域 overlay？**（§8.1）
2. **HTE153 墨西哥规是有意复用美加规文件，还是建档误绑？**（§7.1.1，需查实时 Base）
3. **HTE152 日规错档确认与修正**（§7.1.2）
4. **合规缺口：三处 → 一处，另新增一处生产缺口。**
   - ~~HTE153 美加规/墨西哥规无 FCC~~ **撤销——是分解漏检，不是缺口**。FCC 无标题寄生块
     就在盘点登记的原文件里（EN p6 / FR p24 / ES p42，装箱单页上方 y=35–125，逐语言块重复），
     漏检原因恰是本报告 §4.4 自己警告过的「fcc 全库零次进目录」呈现层级陷阱。
   - ~~巴西规带 FCC~~ **结案——固化正确**。HTE153 BR 印刷页 03 实证，且与 HTE140 巴西规
     构成两个独立 SKU 的一致证据。
   - **仍悬：HTE162 欧英规无 DoC。**
   - **新增（记为管线债务，非印刷阻断——操作者已更正首版措辞）：ANATEL 声明无模板载体。**
     巴西规封底有带题胶囊块 `REGULAMENTAÇÃO ANATEL`（模块 ESP8684-MINI-1-H4X、
     认证码 05561-26-18577、Resolução nº 680），`docs/templates` 与 `data/` 全域零命中，
     `manual_pt-br.yaml` 无封底页条目。**出货书 = 管线产物 + InDesign 手工层**：
     该块今天由操作者在 InDesign 排版时手放，能印出来；代价是每次印刷手放一遍、
     不进版本控制、不随姊妹线传播、仓库侧任何检查都看不见它。归 B8 首批 fragment 化。
   - 澳规「零合规章」现有 3/3 一致证据（跨 2 个 SKU、3 个世代，含首本现代 V2 有字层书），
     但按 §5 纪律仍需法务签字才能固化；落库时记为**显式空行 + 待签字标记**，
     区分「确认不挂载」与「尚未调查」。
5. **产线 FCC 独立页 vs 语料无标题块**，哪个是目标态？（§6.4.3）
6. **9 个无参考骨架的仓库目标**（原 12，`JE-1000F_AU/KR/pt-BR` 已结案）如何处置——
   补语料，还是显式标记为"新建线"？（§6.2）
7. **HTE140 日规三个变体（27 页有字层 / 28 页 SG / 28 页 WH）如何收敛？** 证据表明它们是
   三份独立登记文件而非配色参数（§7.4）。附带两条待处理：SG 件已印刷的 `F☒` 缺字符与
   红色残留编辑标记是否需要召回或补版？
8. **Phase A 的重构覆盖率阈值 X%**：实测纯删除 **81.0%**、≤1 overlay **94.8%**
   （终账，可由 corpus_audit_2026-08/stats.py 复算），是否达标？

## 10. 复现

数据集与统计脚本已入仓：`code-as-doc/architecture/corpus_audit_2026-08/`
（manuals / topics / topic_ledger / reconstruction 四表 + `stats.py` + README），
一条命令复算全部数字：

```bash
python3 code-as-doc/architecture/corpus_audit_2026-08/stats.py
```

exit 0 = 全部一致性断言通过。

溯源注（工作流 journal，含 55 本逐本章节序列、10 条归一化警告全文、5 个透镜完整输出）：
`~/.claude/projects/-Users-hello-tech-team-Documents-GitHub-auto-manual/a8174933-fac0-457b-a22c-ff34a22e6e25/subagents/workflows/wf_1d8d06c3-a3a/journal.jsonl`

执行规模：22 agents · 2,708,768 subagent tokens · 460 次工具调用 · 27.5 分钟。

> **方法门偏差如实记录**：需求文档 §Phase A 写的是"先拿 ~10 本试跑分解方法，方法过关再上全量"。
> 本轮直接跑了全量——语料文本质量好于预期（48/56 有文字层），且操作者一次性提供了全部语料。
> 方法风险因此转移到结果复核：§7.3 记录了 2 处执行偏差，均已定位并更正。

## 11. 修订记录

- 2026-08-20：Phase A 首轮盘点，56 份 PDF / 54 份已分解。
- 2026-08-20（同日第三轮·补语料）：操作者补齐 HTE152 日规真本与 HTE153 澳/韩/巴西三本。
  语料 56→59 份文件 / 58 份独立内容；A1 成员 25→28、纯删除 19→22；全库 79.3% / 94.8%；
  产线覆盖 12/55→15/58；反向差集 12→9。**三处悬案全部结案**（澳规零合规章 3/3 成立、
  巴西规 FCC 固化正确、韩规归 MAIN@INTL 不开新格），合规待裁决 3→1 并新增 ANATEL 生产缺口。
  新增 W13/W14 两条警告；§0.4 的「中规独立撰写」例证按逐条比对结果改写。
- 2026-08-20（同日更正）：补分解漏项，覆盖率 55/55 = 100%。**§7.4 更正了一处结论**——
  HTE140 日规两份 28 页文件原判为"配色变体、差异只有一个型号 token"，机械比对证明是
  14 组实质差异的独立修订版（含标题层级、分页、表格结构、安全条目与保修条款漂移），
  文档控制号亦不同。§8 的 `colorway` 参数化建议随之撤回。
- 2026-08-21（终账）：操作者评审第 6 条落地——审计数据集与统计脚本入仓
  `code-as-doc/architecture/corpus_audit_2026-08/`（manuals/topics/topic_ledger/reconstruction
  四表 + stats.py + README），52 行数字勘误逐行应用。终账值：原始槽位 983 / 归一 976；
  三档 15/762/78.1%、4/136/13.9%、11/78/8.0%；核心 19 id = 898/976 = 92.0%；
  五格成员 27/14/7/4/3 + outlier 3 = 58；纯删除 47/58 = 81.0%、≤1 overlay 55/58 = 94.8%；
  §6.1 四档表按累计口径闭合（档 3 = 22，22+36=58）。旧值 981/926、842/895、五格 28/12 等
  全部废止，以 stats.py 复算为准（exit 0 = 全部一致性断言通过）。
