# ③ 翻译裁决落库 — (b) 59 行 Approved + (c) 81 条盖章回灌 — 执行日志

- 执行窗口（UTC）：2026-09-19T08:30:46Z – 08:42Z
- 授权：操作者夏冰「都批」（2026-09-19，四项清单之 ③）。写面 = (b) 的 59 行（52 建议批准 + 7 改后批准，一行不多）+ (c) 的 81 条 ledger 盖章及其回灌链。
- 身份：lark-cli 1.0.69，profile `cli_aaa0db0d4b39dcca`，`--as bot`。Python = repo `.venv`。
- 目标：TM-B base `Ji1hb5ub1aUbewsTljGccvx5nhc`，Translation_Memory `tblqtvNbgjDwR4ya`，Terms `tblzerRpOEuDIkKA`。
- 主 checkout 未动分支（全程只写 untracked 的 `reports/revision_ledger/` 与活表）；无任何 PR。

## (b) 活表写：59 行 Draft→Approved（7 行先写改值）

### 写前基线（08:31:18–30Z，全量分页拉取，`prewrite/`）

| 表 | 总行 | Approved | Draft | Deprecated |
| --- | --- | --- | --- | --- |
| Translation_Memory | 1358 | 949 | **388** | 21 |
| Terms | 232 | 115 | **117** | 0 |

与任务给的基线（TM Draft 388 / Terms Draft 117）逐字相符。

### 写前逐行核验（`verify_59_prewrite.py` → `writes/plan_59.json`）

59 行（TM 18 建议批准 + TM 7 改后批准 + Terms 34 建议批准）全部：live Status=Draft，`zh`/`zh-TW` 与 `rev11_rule_batch.csv` 的源句/现译**逐字节一致**。7 行改值从 CSV `改值建议` 解析（`zh=… | zh-TW=…`），剥离构建器附注 `(s2twp机转参考,须人工润校)`（`build_rule_batch.py:199` 证实该后缀是生成注记，非值本体）。

### 写形与回读（`exec_b_writes.py`；payload/result 在 `writes/`，回读在 `readback/`）

| 步 | UTC | 写形 | 行数 | 回读 |
| --- | --- | --- | --- | --- |
| W1 | 08:32:50Z | TM `+record-batch-update` patch `{"Status":"Approved"}` | 18 | 18/18 GET=Approved |
| W2 | 08:33:15–23Z | TM `+record-upsert --record-id` 逐行 `{zh, zh-TW}`（7 个独立 payload） | 7 | 7/7 GET zh+zh-TW 逐字节=改值 |
| W3 | 08:33:35Z | TM batch-update Status=Approved（W2 的 7 行） | 7 | 7/7 GET=Approved |
| W4 | 08:33:48Z | Terms batch-update Status=Approved | 20 | 20/20 |
| W5 | 08:34:13Z | Terms batch-update Status=Approved | 14 | 14/14 |

TM 18 行（W1）：recvqDsREXMqSv 等，见 `writes/W1_tm_approve18_payload.json`（record_id 全清单）。
TM 7 改值行（W2/W3），改值依据坐标在 `writes/plan_59.json`：

| record_id | zh-TW 新值（节选） | 依据 |
| --- | --- | --- |
| recvqDsREXT6Um | ※ USB Type-C 和 USB-C 是 … 的註冊商標。（去 ®） | Spec_Notes.csv[usb_type_c_trademark] |
| recvqDsRZBAaDF | 短按總電源開關鍵 | page_zh/05_operation…:L148 |
| recvqDsRZB10aN | 將產品連線負載，放電直至故障消失。 | troubleshooting_blocks.csv[No.=5] |
| recvqDsRZBlcvH | 移除連線到產品 DC/USB 埠的負載，等待故障消失。 | troubleshooting_blocks.csv[No.=10] |
| recvqDuMybzioC | 點亮：節能模式開啟。 | lcd_icons_blocks.csv[No.=22] |
| recvqDuMybLg5O | 熄滅：節能模式關閉。 | lcd_icons_blocks.csv[No.=22] |
| recvqDuMyb8HMd | 車充充電只支援 12V 車型…（适用→使用） | page_zh/08_charging_methods.rst:L54 |

Terms 34 行（W4/W5）：见 `writes/W4_*_payload.json` / `W5_*_payload.json`。

### 收尾全表复核（08:34:53Z，`postwrite/`）

| 表 | 总行 | Approved | Draft | Deprecated | 变化 |
| --- | --- | --- | --- | --- | --- |
| Translation_Memory | 1358 | **974** (+25) | **363** (−25) | 21 | 25 = 18+7 ✓ |
| Terms | 232 | **149** (+34) | **83** (−34) | 0 | 34 ✓ |

零漂移 diff（pre vs post 全表逐行逐字段）：**恰好 59 行变化**，且仅 Status（+7 行的 zh/zh-TW、系统 updated_at）；372 留人工 + 116 需复核相关行零触碰（unexpected diffs = 0）。

## (c) ledger 盖章 + 回灌链

### 备份与前置

- 备份（08:35:20Z）：`ledger_backup/ledger_auto-manual_20260919T083520Z.jsonl`（129 行）、`ledger_hello-docs_20260919T083520Z.jsonl`（93 行）。两文件均 git untracked（`?? reports/revision_ledger/`，两仓核实）。
- 盖章前两账本 222 行全 pending（`load_ledger` 复核）。

### 盖章（`stamp_ledgers.py`，用 `tools/revision_ledger.py` 自己的 load/write 与字段契约）

- 盖章值：`final_status="accepted_as_proposed"`；`final_text=reviewer_text`（=工具 `_final_text_for` 对 accepted 的语义）；merge 元数据按行所属轮次：
  - 20260819 → `Hello-Docs#60` / `b7e53f14d0f1…` / 2026-08-20T01:47:39Z
  - 20260826 → `Hello-Docs#65` / `0e2adfdac152…` / 2026-08-28T03:09:53Z
  - 20260905 → `Hello-Docs#67+#68` / commit 留空（两 PR 共同落地，单 SHA 不诚实）/ 2026-09-05T07:21:08Z
  （四个 PR 的 merge SHA/时间由 `gh pr view` 现场核实。）
- 结果：auto-manual 盖 **62** 行、Hello-Docs 盖 **30** 行 = 92 物理行，覆盖 81 唯一 delta_hash（11 个重叠 hash 两账本各一行）。与 prefill `在两份台账` 列推算的期望值逐仓相符。
- 3000C 轮「改后批准形态」提示（exec 摘要 ⚠2）：逐行核对 prefill 理由后，81 条 accepted 的理由均为「新值已出现/文本检出/删除落地」形态 = as-proposed 落地，无一行需要 edited_further/superseded 语义；故全部按 accepted_as_proposed 盖章。
- 回读：重载两文件 diff 备份 —— 变化行数 62/30，变化字段 ⊆ {final_status, final_text, merged_pr, merged_commit, merged_at}，非 81 集合内行零变化；116 需复核（130 物理行）全部仍 pending。终局计数：auto-manual 129 = 62 accepted + 67 pending；Hello-Docs 93 = 30 accepted + 63 pending。

### tm-candidates（08:39Z，`candidates/`）

- auto-manual 账本 → **29** 条；Hello-Docs 账本 → **19** 条；按 delta_hash 合并去重 → **43** 条唯一候选（全 ko）。
- 与预期一致性：review-route accepted 唯一 hash 共 52，其中 9 被工具口径自然排除（删除类 reviewer_text 空 2、空白级 normalize 后 old==new 3、insert 类 old_text 空等）→ 43。
- 合并候选同时落到机制层规范位置 `auto-manual/reports/revision_ledger/tm_candidates.jsonl`（#538 flow_dashboard 读的就是这里）；副本在 `candidates/tm_candidates_merged.jsonl`。
- 盖章后 stats 存档：`candidates/stats_*.json`（acceptance_rate 1.0 —— 本轮只盖 accepted，rejected 判定归语言责任人后续轮）。

### tm-apply 审批链（`apply/`）

- 审批文件：`apply/approve_hashes_42.txt` —— 43 条候选批 **42**，**扣留 1 条** `b0c1a6056fc6…`：
  - 该候选是 JE-2000E 专属改法「AC 전원 버튼→**AC1** 전원 버튼」，但 exact-or-abstain 解析命中的 TM 行 `recvoioP8x6aCn` **Model 为空（未按型号隔离）**，且同批 3000C 轮 accepted 行 `1ef29f0b8796`（源表建议）方向相反（AC1→AC，3000C 单 AC 位）。写入即向共享语料注入型号事实分叉（rev11 调查 §2#10 对同类行的裁决就是「限 JE-2000E KR scope」）。
  - fail-closed 处理：ledger 盖章保留（评审页确实如此落地），TM 写扣留待语言责任人拍板（可选项：克隆一行 Model=JE-2000E 的定向行，或裁决共享行归属）。**补写一条命令即可**：
    `.venv/bin/python tools/revision_ledger.py tm-apply --candidates reports/revision_ledger/tm_candidates.jsonl --approve b0c1a6056fc62aa5f0f1d9e648bdf2b92c501bed8de3cd368e10812aa5498c4a --tm-binding Ji1hb5ub1aUbewsTljGccvx5nhc:tblqtvNbgjDwR4ya --lark-cli "lark-cli --profile cli_aaa0db0d4b39dcca" --write`
- 离线解析 dry-run（活表快照上跑 `plan_translation_writes`，`apply/dryrun_resolved_plan.json`）：apply 4 / already 1 / unresolved 37 / ambiguous 1。CLI dry-run 存档 `apply/cli_dryrun.json`（42 apply-deferred / 1 skip）。
- **--write 实跑（08:39:52–08:40:52Z，`apply/cli_write_result.json`）**：

| 结果 | delta_hash | record_id | 字段 | 内容 |
| --- | --- | --- | --- | --- |
| written | 542711959ed3… | recvpC5RGSQ1ci | ko | 무정전 전원 공급 장치(UPS)→무정전 전원장치(UPS)… |
| written | 76e932d7045c… | recvgEwErzUeTm | ko | 장치가 APP에→장치가 App에… |
| already | f0f6f7eddfa3… | recvgEwErzoNQ2 | ko | 已等于新值（前轮已落） |
| already | 37d51fe409bc… | recvgEwErzUeTm | ko | 与 76e932d7045c 同一行同值，幂等 |
| skip×39 | — | — | — | 1 扣留未批 + 37 unresolved + 1 ambiguous（exact-or-abstain 设计语义：old 译文不在 TM ko 列，弃权不写） |

- 汇总：**新增行 0（该链路只更新不建行）/ 更新 2 / already 2 / verify_failed 0 / error 0**。
- 独立回读（不依赖工具自验，08:41Z）：3 个 record `+record-get`，ko 与 new_text **逐字节一致**（`readback/apply_rec*.json`）；再全量拉表（`postwrite/tm_final_*`）与 (b) 后快照 diff——**恰好 2 行变化，且仅 ko + updated_at**；状态计数不变（974/363/21，总 1358）。

## 终局状态一览

- TM 活表：1358 行 = Approved 974 / Draft 363 / Deprecated 21（(b) +25 Approved；(c) 另更新 2 行 ko 内容，不动状态）。
- Terms 活表：232 行 = Approved 149 / Draft 83。
- Ledger：197 唯一 delta 中 81 accepted（92 物理行已盖章带 PR 元数据）/ 116 仍 pending 待复核。
- 候选机制层：`reports/revision_ledger/tm_candidates.jsonl` = 43 条（含 1 条扣留未写、37+1 条 abstain 未消化——它们留在候选文件里属机制常态，flow_dashboard 可见）。

## 留给操作者的 2 件事

1. **扣留写**：`b0c1a6056fc6…` 的 AC1 定向写（命令在上文），或裁决 TM 行 `recvoioP8x6aCn` 的型号归属。
2. 37 条 unresolved 候选是「评审句对不在 TM ko 列」的自然弃权 —— 若想收编成新 TM 行（建行而非改行），是链路外的新写面，需另行授权（本轮未做）。

## 证据文件索引（本目录）

`started_utc.txt`、`pull_table.py`、`verify_59_prewrite.py`、`exec_b_writes.py`、`stamp_ledgers.py`
`prewrite/`（写前全表快照）| `writes/`（plan_59 + 5 批 payload/result + exec_b_log.txt + ledger_stamp_report.json）| `readback/`（每批 GET 回读 + apply 3 行回读）| `postwrite/`（(b) 后与终局全表快照）| `ledger_backup/`（两账本原件）| `candidates/`（两账本候选 + 合并 43 + stats）| `apply/`（dry-run 两份 + 审批文件 + write 结果）
