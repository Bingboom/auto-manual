# Merge Authorizations (gate-on-green registry)

The single source of truth for **who may merge what without waiting for the
operator's per-PR review**. Default remains AGENTS.md §8.6: agents do not
self-merge. A self-merge is allowed only when a **live entry in the table
below covers the PR** and the gate-on-green protocol passes. Chat authority
alone is not enough: when the operator grants an authorization in chat, the
agent's FIRST action is to record it here (this file is the durable form —
session memory is not), then act on it.

Decided by the operator on 2026-07-30 (four picks: all-checks-green
definition; branch-pattern + PR-list scoping; parity line stays live; the P0
skill batch #738–#742 stays operator-reviewed).

## Gate-on-green protocol

An authorized PR may be merged only when ALL of the following hold:

1. **Every check is green** — `gh pr checks <N> --watch` until settled, then
   verify each listed check is `pass`. This includes **non-required** checks,
   and `pending`/`queued` is NOT green (a PR was once merged on a pending
   check; a non-required secret-scan once caught a real finding — both are
   why "required checks only" was rejected).
2. **No changes-requested review and no unresolved review threads.**
3. **The PR is covered by a live table entry** (branch pattern or explicit PR
   number). Doubt about coverage = not covered; fall back to waiting.

Then: `gh pr merge <N> --squash --delete-branch`. If the line's workflow
requires it (e.g. an in-review target consuming the change), trigger the
matching rebuild/republish after merge.

Red path: pull the failing job's log, diagnose, fix or report — never merge
partially green, never re-run checks blindly to "wash" a real failure.

## Grant / revoke flow

- **Grant**: operator states the scope in chat → agent adds a row (next
  `MA-nnn`, scope, grant quote + date, expiry condition) → the entry is live
  once pushed. Time-boxed grants name their milestone/date; standing grants
  say "until revoked".
- **Revoke / expire**: operator says so, or the expiry condition is met →
  flip 状态 to 已失效 in the next touch of this file. Expired entries stay as
  history; never delete rows.

## Registry

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-176 | 按顺序：(a) 合入 PR #1258（`feat/web-je2000e-eu-app-control-panel`，JE-2000E/EU 英/法/西/德/意 App 添加设备图换成各语块「两屏截图加灰底控制面板」裁图）：先与最新 main 同步并解决冲突（登记表、资产登记计数、测试等），不改其功能内容；同步时把 MA-156 标为已失效（被本行取代）；须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 #1258 与 #1284 均合入且镜像同步落地后，从新 main 按冻结源 `manual_sources/JE-2000E/EU/en/2.0/phase2`（默认来源模式）Git-only 构建 JE-2000E/EU 英/法/西/德/意 Web **2.2**（英语由 2.0-20260913、法/西/德/意由 2.1 统一为 2.2；乌克兰语不重建），与 MA-175 (b) 的 JE-1000F/EU 2.4 进同一个 Hello-Docs 发布 PR；JE-2000E 候选差异只允许：五个语种的添加设备图换成 #1258 的新裁图、法/西/德/意四行按键名文字移入替代文字、西/德三行储存时长标签（#1284）、新图入池（及不再被引用的旧池文件随之移除）、发布证据与元数据、英语路由的证据格式追平（去掉旧格式文件，同 JE-3600A 英语那次）；超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对这五个规范网址。**不含**：Hello-Docs `review/*` 分支（HD#135、HD#125 仍由操作者合）、飞书写入、乌克兰语 App 面板、JE-300E、其它型号、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「授权你合入。然后继续」「#1258 JE-2000E App 面板」「合成一次 2.2（推荐）」2026-09-26（答复合入范围与 JE-2000E 发布两问；HD#135、HD#125 未选） | (a)(b) 均完成即失效，或操作者撤销 | 生效（推送后生效） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-175 | 按顺序：(a) PR（`fix/data-storage-labels-es-de`，**含本登记行**）：JE-1000F/EU 与 JE-2000E/EU 储存页西/德时长标签对调，按印刷改回：冻结源 `phase2/Spec_Master.csv` 储存温度三行 `Param_es` 改为「1 mes/3 meses/12 meses」、`Param_de` 改为「1 Monat/3 Monate/12 Monate」（JE-1000F 印刷德语此处小写，按操作者裁定大写，与 JE-2000E 印刷及其它型号一致），JE-1000F 评审副本 p42/p57 同步，两个 `source_manifest.json` 重锁；新增回归测试，JE-1000F 冻结源 README 同步。同一次改动把 MA-174 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按各自冻结源 Git-only 构建 JE-1000F/EU 英/法/西/德/意 Web **2.4**（由 2.3 升级；英/法 `--source review-asis`、西/德/意 `--source review`；英/法/意内容不变，只升版本与证据）；JE-2000E/EU 的西/德储存修正不单独发布，随 #1258 合入后的 JE-2000E/EU Web 2.2 一起发（操作者 2026-09-26「合成一次 2.2」，见 MA-176），两者进同一个 Hello-Docs 发布 PR；JE-1000F/EU 候选只允许改动这五条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池，页面变化须与合入前试构建一致（西/德只改三行储存时长标签，英/法/意内容不变），超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对五个规范网址。**不含**：飞书写入、Hello-Docs `review/*` 分支、JE-300E（操作者「先别做」）、其它型号、#1258、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「以 PDF 为准」「写成 1 Monat（推荐）」「西/德先发 2.1-20260926（推荐）」「全绿后合入并发布（推荐）」2026-09-26（先以「以 PDF 为准」裁定待定项，再答复德语大小写、JE-2000E 版本、合入与发布三问）；随后「合成一次 2.2（推荐）」2026-09-26 把 JE-2000E 并入 #1258 的 2.2 | (a)(b) 均完成即失效，或操作者撤销 | 生效（推送后生效） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-174 | 按顺序：(a) PR（`fix/charging-emergency-gate-ac-line`，**含本登记行**）：共享模板 `docs/templates/page_shared/{fr,es,de,it,uk}/charging.rst` 给「应急快充模式」段加上与英语模板相同的能力标记（开始标记放在原有两个空行之间、结束标记紧跟正文末行：能力为 TRUE 的目标输出逐字节不变，为 FALSE 的去掉该段，JE-3000C/EU 法–乌与 JE-3600A/EU 法/西不再印这段）；JE-3600A/EU 英/法/西插图清单给墙座充电图加 `covered_annotations`，把图中已印的连接说明句收进 alt、页面不再重复，冻结源 `source_manifest.json` 重锁英语清单；新增回归测试，两份入库评审附记与冻结源 README 同步。同一次改动把 MA-173 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按各自冻结源（默认来源模式）Git-only 构建 JE-3000C/EU 英/法/西/德/意/乌 Web **2.6**（由 2.5 升级；英语内容不变，只升版本与证据）与 JE-3600A/EU 英/西/法 Web **2026-05-25.6**（由 2026-05-25.5 升级），进同一个 Hello-Docs 发布 PR；候选只允许改动这九条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池；页面变化须与合入前试构建一致（JE-3000C 法–乌只去掉应急快充段、英语不变；JE-3600A 英只把墙座说明句收进图 alt，法/西另去掉应急快充段），超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对九个规范网址。**不含**：飞书写入、Hello-Docs `review/*` 分支、其它型号、#1258、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「共享模板补标记（推荐）」「收进图片 alt（推荐）」「全绿后合入并发布（推荐）」2026-09-26（答复修法、重复文字、合入与发布三问，同时裁定 JE-3000C/EU 英语保持英标；此前「继续」启动本项） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1283 squash `13bbd003`、Hello-Docs #136 squash `30ac7753` 均已合入；2026-09-26 RTD 九个规范网址核对通过：JE-3000C 2.6 无应急快充段、JE-3600A 2026-05-25.6 墙座句只在图 alt 且法/西无应急快充段） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-173 | 按顺序：(a) PR（`fix/assets-je1000f-eu-overrides`，**含本登记行**）：JE-1000F/EU 开箱主机图、LCD 显示模式图与 UPS 连接图改为按印刷语块的目标覆盖：`data/asset_recipes/manual_je1000f_eu_uk_20260618_block_art.json` 从 V2.0 EU-UK 2026-06-18 印刷英语块（PDF 7/14/15 页，英标插座）与法语块（24/31/32 页，欧标插座）裁出 6 张（各 PDF+PNG，LCD 图保持 `op_lcd_mode.png` 的 536×404 版式），`data/asset_registry.csv` 新增 6 行按语言覆盖（英语→英语块，法/西/德/意/乌→法语块），替换原先落回的共享图（美标插座主机、他机型带轮机身）；打包改写器支持 `\HBInBoxThree` 图片参数与 `\includegraphics` 的 `asset:` 引用（`asset_rewrites.py` 同步还原）；冻结评审副本法–乌页面、德/西/意/乌生成稿与英语开箱 LaTeX 宏改用 `asset:` URI，`source_manifest.json` 重锁 20 个文件并记录规范化；新增回归测试，构建指南、操作指南、网页发布说明与冻结源 README 同步。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（英/法 `--source review-asis`，西/德/意 `--source review`）Git-only 构建 JE-1000F/EU 英/法/西/德/意 Web **2.3**（由 2.2 升级），进同一个 Hello-Docs 发布 PR；候选只允许改动这五条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池；每条路由的页面变化须与合入前试构建一致（只换开箱主机、LCD 显示模式、UPS 三张图，alt 与文字不变），超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对五个规范网址。**不含**：飞书写入（`04_资产定义` 六行逐条另行确认）、Hello-Docs `review/*` 分支（`review/JE-1000F-EU` 镜像 PR 由操作者处理）、其它型号、#1258、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「在源头修（推荐）」「按计划做（推荐）」「全绿后合入（推荐）」「五语一起升 2.3（推荐）」2026-09-25（答复修法、计划、合入与发布四问；此前「继续下一个」启动本项） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1282 squash `b95cdb42`、Hello-Docs #134 squash `388cff04` 均已合入；2026-09-26 RTD 英/法/西/德/意五个规范网址核对通过：2.3、本语块开箱主机/LCD 显示模式/UPS 图在线、无共享图） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-172 | PR #1276（`docs/plan-multi-agent-dispatch`，**含本登记行**）：REV-45 多 Agent 调度与任务续接的设计定稿 `code-as-doc/dev/multi_agent_dispatch_design.md`（第一版不做自动调度：台账“执行”格认领、交接记录写在任务 PR 描述里、续接只读四样、只读取号工具、认领 7 天过期、用 REV-39 契约表冻结演示）；台账 REV-45 记起草与定稿；系统建设页“当前在做”加 REV-45，“Agent 执行”卡的“多 Agent 调度与任务续接”附设计。同一次改动把 MA-170 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：REV-45 的实施（取号工具、任务状态数据域、`AGENTS.md` 规则，另开 PR）、REV-39 契约表、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。（MA-171 已由开着的 #1277 预留，故用 MA-172。） | 「合入」2026-09-25（答复“合入 #1276：设计定稿、台账 REV-45 的执行记录、系统建设页当前在做里的 REV-45”） | 本 PR 合入即失效，或操作者撤销 | 生效（推送后生效） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-171 | 按顺序：(a) PR（`fix/web-je3000c-eu-illustrations`，**含本登记行**）：JE-3000C/EU 网页插图：`data/asset_recipes/manual_je3000c_eu_web.json` 新增法/西/德/意/乌各 15 张从 V2.0-2026-07-31 印刷各语块（PDF 22–37/38–53/54–69/70–85/86–101 页）裁出的本机插图（该印刷按区域分版：英语块画英标插座、其余画欧标），五份单语插图清单绑到与英语相同的文件名，替换原先落回的 JE-1000F 通用图；图里已印的文字按英语做法收进 alt（`consume_before_presentation`）；按操作者裁定，德语正视图与法语侧视图照印刷原样并保留页面标注表，意/乌节能面板印成英文的说明行抹掉、页面保留本地化句子；`web_manual.css` 给五种语言的直流面板图加与英语相同的边框；冻结源 `source_manifest.json` 重锁配方，新增回归测试，README 与入库评审附记同步。同一次改动把 MA-168 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-3000C/EU 英/法/西/德/意/乌 Web **2.5**（由 2.4 升级；英语内容不变，只升版本与证据），进同一个 Hello-Docs 发布 PR；候选只允许改动这六条路由的页面、证据、元数据、`publish_manifest.json` 对应条目、内容池，以及共享样式表 `docs/publish/web/_static/web_manual.css`（只允许新增这五条直流面板选择器）；法–乌页面变化须与合入前试构建逐行一致、英语页面内容不变，超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对六个规范网址。**不含**：其它型号（JE-2000E、JE-1000F 同类缺口另行处理）、#1258、Hello-Docs `review/*` 分支、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「照印刷原样，这两张保留页面标注表（推荐）」「全绿后合入（推荐）」「六种语言一起升 2.5（推荐）」2026-09-25（答复概览缺陷、合入与发布三问；此前「继续下一个」启动本项） | (a)(b) 均完成即失效，或操作者撤销 | 生效（推送后生效） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-170 | PR #1275（`docs/plan-rev44-accepted`，**含本登记行**）：记录 REV-44 验收——台账 REV-44 由 verifying 改为 done 并写明证据（#1273、#1274 与 RTD 线上核验）和验收原话；系统建设页“Agent 执行”卡的“来源登记（SSOT）”由 available 改为 validated，证据改为 `rev:REV-44=done` 并加操作者确认，REV-44 移出“当前在做”；设计文档状态行记验收。同一次改动把 MA-169 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：REV-39 契约表、REV-45 的设计与实施、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「合入」2026-09-25（答复“要合的话说‘合入’，CI 通过后我就合”；此前「验收」） | 本 PR 合入即失效，或操作者撤销 | 已失效（#1275 squash `313fa642` 已合入；2026-09-25 RTD `377b8d54` 核验：“来源登记（SSOT）”显示已验证，REV-44 已移出“当前在做”） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-169 | 按顺序：(a) PR #1273（`docs/plan-ssot-agent-dispatch`，**含本登记行**）：台账新增 REV-44 SSOT 来源登记与 REV-45 多 Agent 调度与任务续接（依赖 REV-44），REV-39 标为开始只读摸底，并记范围变更；系统建设页“当前在做”加 REV-39、REV-44，“Agent 执行”卡加两项；设计文档 `code-as-doc/dev/ssot_source_registry_design.md` 定稿。同一次改动把 MA-167 标为已失效。(b) 在 (a) 合入后，PR #1274（`feat/portal-source-registry`）改以 main 为基准、只保留它自己的提交：新增来源登记 `tools/rtd_portal_assets/source_registry.yaml` 与 `tools/rtd_source_registry.py`；系统建设页与交付物页的快照名、过期天数和降级文案改为从登记读取，状态配置与交付物模块删去原副本；`check` 校验登记；系统建设页底部公开“数据来源”表；REV-44 改为 verifying。两项均须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；(b) 合入后经既有镜像同步与 RTD 链路核对线上系统建设页与交付物页。**不含**：REV-39 契约表（另开 PR）、REV-45 的设计与实施、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「合入」2026-09-25（答复“要合的话说‘合入’：我先合 #1273，再把 #1274 换到 main 上，等 CI 全部通过后合入”） | (a)(b) 均合入且线上核验完成即失效，或操作者撤销 | 已失效（#1273 squash `4005e4a3`、#1274 squash `eb114b8f` 均已合入；2026-09-25 RTD `ae3889c2` 核验：系统建设页底部“数据来源”表列出 8 个数据域，“当前在做”与“Agent 执行”卡已更新，交付物页不变；操作者同日「验收」REV-44） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-168 | 按顺序：(a) PR（`fix/web-je3600a-eu-illustrations`，**含本登记行**）：JE-3600A/EU 网页插图：`data/asset_recipes/manual_je3600a_eu_web.json` 新增法/西各 15 张从印刷法语块（PDF 24–34 页）与西语块（41–51 页）裁出的本机插图（装箱、正/侧视图、LCD 图、四块操作面板、LCD 模式、UPS、三种充电），`je3600a_eu_fr_illustrations.json` 与 `je3600a_eu_es_illustrations.json` 绑到与英语相同的文件名，替换原先落回的 JE-1000F 通用图；英/法/西 LCD 裁图放宽为 `[34, 55, 339, 195]`，补回被裁掉的 ⑧⑨⑳㉑；UPS 说明（法/西）与西语车充两行按英语做法收进图片 alt；法语块车充图印的是英文，按操作者裁定去掉图内文字（`redact_text`）、页面保留法语两行；冻结源 `source_manifest.json` 重锁配方与英语插图清单，新增回归测试，README 与入库评审附记同步。同一次改动把 MA-166 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-3600A/EU 英/法/西 Web **2026-05-25.5**（由 2026-05-25.4 升级），进同一个 Hello-Docs 发布 PR；候选只允许改动这三条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池，英语页面只允许 LCD 图一处变化，法/西页面变化须与合入前试构建逐行一致，超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对三个规范网址。**不含**：其它型号（JE-3000C、JE-2000E 同类缺口另行处理）、#1258、Hello-Docs `review/*` 分支、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「先做 JE-3600A 西法整套（推荐）」「一起修，英语也重发（推荐）」「全绿后合入（推荐）」「改动的语言一起升 2026-05-25.5（推荐）」「抹掉图里英文，页面留法语（推荐）」2026-09-25（答复范围、英语 LCD 裁图、合入、发布与法语车充图五问） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1272 squash `804cede4`、Hello-Docs #132 squash `ae6ff95a` 均已合入；2026-09-25 RTD 英/西/法三个规范网址核对通过：2026-05-25.5、本语块插图在线、无 JE-1000F 通用图） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-167 | PR #1271（`feat/portal-deliverables-page`，**含本登记行**）：新增知识库「交付物」页 `/workspace/deliverables/`，按型号分组、每个区域一行，汇总网页手册（构建时读 `docs/publish/publish_manifest.json`）、印刷交付包（IDML + PDF）与 Word 云文档的链接；后两列是飞书知识库链接，来自提交的快照 `tools/rtd_portal_assets/deliverables_snapshot.json`（由 `tools/rtd_deliverables.py export` 只读导出，超过 45 天标待复核）；知识库首页与系统建设页侧栏加入口，测试与文档同步。同一次改动把 MA-165 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；合入后经既有镜像同步与 RTD 链路核对线上页面。**不含**：飞书写入、快照定时刷新（需改 workflow）、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「等 CI 跑完，你合」2026-09-25（此前「直接放飞书链接」「只按型号」「型号 ➕ 区域」「网页文档也要链接」） | 合入且线上核验完成即失效，或操作者撤销 | 已失效（#1271 squash `b1d90f26` 已合入；2026-09-25 RTD 核验 `/workspace/deliverables/` 已上线：24 个型号、35 行区域、网页手册链接 54 个、飞书链接 28 个，知识库首页与系统建设页侧栏入口均在） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-166 | 按顺序：(a) PR（`fix/data-je3000c-eu-spec-translations`，**含本登记行**）：JE-3000C/EU 冻结源 `manual_sources/JE-3000C/EU/en/2.0/phase2` 中 `Spec_Master.csv` 的规格页 19 行、储存 3 行、概览规格槽 15 行、待机/自动关机时长与乌语单位行，以及 `Spec_Footnotes.csv` 注脚 ② 的法/西/德/意/乌列，按已锁定的 V2.0-2026-07-31 印刷稿各语块补齐（324 格；措辞以印刷为准，格式统一为已审定冻结源写法，印刷缺陷用跨型号已审定译法；西语 PV 值取同一印刷稿 2026-09-15 修订版的更正），并按操作者裁定在 JE-3000C 与 JE-1000H 的乌语注脚 ② 补上「струму」（JE-1000H `Spec_Footnotes.csv` 1 格）；两个冻结源的 `source_manifest.json` 重锁，新增回归测试，README 与入库评审附记同步。同一次改动把 MA-164 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-3000C/EU 英/法/西/德/意/乌 Web **2.4**（英语由 2.2、法–乌由 2.3 升级；英语内容不变，只升版本与证据）与 JE-1000H/EU 英/法/西/德/意/乌 Web **2.0-20260925.1**（由 2.0-20260925 升级；只有乌语页面注脚一行变化，其余只升版本与证据），进同一个 Hello-Docs 发布 PR；候选只允许改动这十二条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池，JE-3000C 法–乌页面变化须与合入前试构建逐行一致、英语页面内容不变，JE-1000H 只允许乌语页面注脚一行变化，超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对十二个规范网址。**不含**：其它型号、#1258、Hello-Docs `review/*` 分支、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「页面上的英文一次修完（推荐）」「补上“струму”」「全绿后合入（推荐）」「六种语言一起升 2.4（推荐）」「六种语言一起发 2.0-20260925.1（推荐）」2026-09-25（答复 JE-3000C 范围、乌语注脚、合入、发布与 JE-1000H 发布五问） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1270 squash `5c065fb7`、Hello-Docs #131 squash `07bf6374` 均已合入；2026-09-25 RTD 十二个规范网址核对通过：JE-3000C 2.4 与 JE-1000H 2.0-20260925.1 的新单元格在线、英文回落已消失） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-165 | 按顺序：(a) PR（`feat/skills-ai-manual-pagenum-fix`，**含本登记行**）：把主检出里从未提交过的 Claude 技能 `ai-manual-pagenum-fix`（`SKILL.md` 与两份 `references/`，原样不改）提交入库，并在 `.claude/skills/README.md` 登记（用与技能同时起草的那一行）；同一次改动把 MA-163 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) (a) 合入后处理主检出（仓库根目录的检出，现停在已合入的 `feat/sweep-terminology-crosscheck`）：先确认没有进程在其中构建；其余未提交内容（`tools/asset_pipeline/extract.py` 的旧改动、`docs/_build` 生成文件、`reports/` 与 `docs/_review` 下的未跟踪目录、`data/.phase2.snapshot.lock` 等）用 `git stash push -u` 保存，并另建引用 `refs/backup/primary-checkout-20260925` 防丢，不删除、不还原；然后切到最新 `origin/main`（`main` 分支已被 `web-cap-check` worktree 占用，故用 detached HEAD），核验 Claude 钩子的修复和新技能都在工作树中。**不含**：丢弃或还原 stash 内容、删除分支 `feat/sweep-terminology-crosscheck`、改动其他 worktree（含 `web-cap-check`）、`.claude/settings.json` 的内容、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「主检出按你的来」2026-09-25（答复第 4 步提议：技能单开 PR 提交，合入后把主检出其余内容 stash 起来（不删）再切到 main） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1269 squash `895be877` 已合入；2026-09-25 主检出其余未提交内容已 `git stash push -u` 存为 `82be92f0`，另建引用 `refs/backup/primary-checkout-20260925`，主检出以 detached HEAD 切到 `origin/main`，Claude 钩子修复与新技能已在其工作树中） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-164 | 按顺序：(a) PR（`fix/data-je3600a-eu-translations`，**含本登记行**）：JE-3600A/EU 冻结源 `manual_sources/JE-3600A/EU/en/2026-05-25/phase2` 中 `lcd_icons_blocks.csv`、`troubleshooting_blocks.csv`、`Spec_Master.csv`（规格页 21 行、储存 3 行、默认待机时长）、`Spec_Notes.csv`、`Spec_Footnotes.csv` 的法/西列按已发布 PDF 各语块表格线切格补齐（216 格；措辞以印刷为准，格式统一为已审定冻结源写法，LCD 结构跟英文行但不改写印刷句，印刷缺陷用跨型号已审定译法并逐条列在入库评审附记），`source_manifest.json` 重锁，新增回归测试，README 与入库评审附记同步；德/意列不填。同一次改动把 MA-162 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-3600A/EU 英/西/法 Web **2026-05-25.4**（由 2026-05-25.3 升级；英语内容不变，只升版本与证据）；候选只允许改动这三条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池，西/法页面变化须与合入前试构建逐行一致，英语页面内容不变，超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对三个规范网址。**不含**：其它型号、概览槽、#1258、Hello-Docs `review/*` 分支、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「结构跟英文，措辞只取印刷（推荐）」「全绿后合入（推荐）」「英/西/法一起升 2026-05-25.4（推荐）」「候选只动这些就发（推荐）」2026-09-25（答复 LCD 结构、合入、发布与候选范围四问；取值规则沿用同日「印刷为准，缺陷格用跨型号译法（推荐）」） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1268 squash `e8c31183`、Hello-Docs #130 squash `a6012360` 均已合入；2026-09-25 RTD 英/西/法三个规范网址核对通过） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-163 | 按顺序：(a) PR #1266（`fix/hooks-worktree-and-data-plane`，**含本登记行**）：Claude Code 钩子 `.claude/hooks/derived_surface_guard.py` 改为检查构建实际所在的检出（跟随 `cd` 步骤与 `build.py` 路径），只提示还原本次构建改动的文件，新增 `tests/test_derived_surface_guard.py`；`scripts/git_branch_guard.py` 对 `review/*`、`backport/*` 分支和推往 `origin` 以外远端的推送不再做“包含最新 main”检查，补模块说明与两项测试；推送提示与 `scripts/start_branch.sh` 用法行的分支名示例由 `codex/<topic>` 改为 `<type>/<area>-<topic>`，`start_branch.sh` 补可执行位；钩子相关文档同步。(b) PR #1267（`feat/portal-skills-hooks`）：系统建设页新增“技能与钩子”一块（`tools/rtd_system_tooling.py` 构建时读取仓库，契约新增 `tooling.skill_lanes`），`AGENTS.md` §7 补登记 `hardcore-task-execution`，路径辅助函数、测试与文档同步；在 (a) 合入后先合入最新 main 再合。两项均须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(c) 两项合入后，在本机 auto-manual 仓库的共享配置里设 `core.hooksPath=.githooks`，启用推送前检查（不含最新 `origin/main` 的分支推往 `origin` 会被拦下），并实测一次放行、一次拦截。**不含**：主检出（`feat/sweep-terminology-crosscheck`）的切换及其未提交内容（含 `ai-manual-pagenum-fix` 技能）、Hello-Docs 克隆的配置、`.claude/settings.json`、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「合入。 然后开勾子」2026-09-25（答复合入 #1266、#1267 并启用 git 钩子的提议） | (a)(b)(c) 均完成即失效，或操作者撤销 | 已失效（#1266 squash `b5913802`、#1267 squash `f0b9a3bf` 均已合入；2026-09-25 本机 auto-manual 共享配置已设 `core.hooksPath=.githooks`，dry-run 实测：含最新 main 的分支推 origin 放行、落后 main 的分支推 origin 被拦、落后分支推 hello-docs 放行，五个稀疏数据面 worktree 解析出的钩子文件不存在） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-162 | 按顺序：(a) PR #1265（`fix/web-je1000h-eu-spec-translations`，**含本登记行**）：JE-1000H/EU 冻结源 `manual_sources/JE-1000H/EU/en/2.0/phase2/Spec_Master.csv` 的法/西/德/意/乌规格页 22 行与储存 3 行，按已发布 PDF 各语块的表格线切格重建（167 格；措辞以印刷为准，格式统一为已审定冻结源写法，印刷缺陷用跨型号已审定译法并逐条列在入库评审附记），`source_manifest.json` 重锁，新增回归测试，README 与入库评审附记同步。同一次改动把 MA-161 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-1000H/EU 英/法/西/德/意/乌 Web **2.0-20260925**（由 2.0-20260920 升级；英语内容不变，只升版本与证据）；候选只允许改动这六条路由的页面、证据、元数据、`publish_manifest.json` 对应条目与内容池，法–乌页面变化须与试构建一致（共 127 行），英语页面内容不变，超出即停下报告；校验通过后合入 Hello-Docs 发布 PR（publish→main，不带 `--delete-branch`），并在 RTD 核对六个规范网址。**不含**：概览槽（成品图 alt 文本）修复、其它型号、#1258、Hello-Docs `review/*` 分支、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「先修 JE-1000H 规格表（推荐）」「印刷为准，缺陷格用跨型号译法（推荐）」「全绿后合入（推荐）」「六种语言一起升版」「候选只动这些就发（推荐）」2026-09-25（答复顺序、取值规则、合入、发布与候选范围五问） | (a)(b) 均完成即失效，或操作者撤销 | 已失效（#1265 squash `738a0599`、Hello-Docs #129 squash `5bd8cd74` 均已合入；2026-09-25 RTD 六个规范网址核对通过，重建后的单元格在线、旧损坏片段已消失） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-161 | PR（`fix/assets-app-gate-reclassify`，**含本登记行**）：配方关卡漏网的 21 个 App 面板原地改为隔离并补 App/二维码风险标签（JE-1000H/EU 六语的下载（含二维码）、添加设备、连接结果面板共 18 个，JE-3000C/EU 英语 App 设置面板 3 个），键名、导出物与哈希不变、页面零变化；两个冻结源重新绑定 `asset_recipe`；新增守卫测试 `tests/test_app_figure_gate.py`（清单中替换 App 设置图或带 `app-` reference_id 的条目必须对应隔离资产，JE-2000E/EU 英语两张列为已知例外，待 #1258 合入后收）；文档与入库评审附记同步。同一次改动把 MA-159 标为已失效并在其状态中更正范围（(b) 实为 14 个路由）。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：任何 Web 发布（页面不变）、JE-2000E 相关文件、#1258 的合入、Hello-Docs PR、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「改成隔离并加守卫（推荐）」「全绿后合入（推荐）」2026-09-25（答复配方关卡命名漏洞的方案与合入两问） | 本 PR 合入即失效，或操作者撤销 | 已失效（#1264 squash `0ac544ce` 已合入；页面零变化，无需发布） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-160 | 登记表清账 PR 的常设授权：操作者要求清账（如「清一次登记表」）时开出的 PR，只把失效条件已满足、或操作者已在对话中裁定失效的授权行的状态格改为「已失效（…）」，每行写明依据（所列 PR 的 squash SHA、线上核验结果或裁定原话）；不增删行、不改任何行的其他格、不改登记表以外的文件。每个此类 PR 须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后，由 Claude 按本行 squash merge。本行同时覆盖 PR #1263（`docs/registry-expire-157-158`，**含本登记行**：把 MA-158 改为已失效并新增本行；MA-157 已由其窗口在 #1262 改为已失效）。**不含**：新增授权或改动授权范围（新的授权仍须操作者在对话中给出并单独登记）、为关闭单独一行而主动开 PR（仍按 MA-029 在下次触碰时顺带改）、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「加常设授权，等 CI 跑完你合入」2026-09-25（答复 Claude 为只改状态格、不增删行的清账 PR 设常设授权的提议；此前「清一次登记表」） | 操作者撤销 | 生效（推送后生效） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-159 | 按顺序合入两项 PR：(a) PR #1262（`feat/web-eu-app-add-device-panels`，**含本登记行**）：JE-2000F/EU（六个路由）、JE-3000C/EU（法/西/德/意/乌）、JE-3600A/EU（英/西/法）的 App 添加设备图改为各语块自己的「两屏截图加本机型控制面板」12x 裁图，共 14 张（三个 App 配方新增隔离条目），取代通用手机图加 JE-1000F/US 控制面板图的拼合图以及 JE-2000F、JE-3600A 英语只有控制面板框的裁图；页面按键名行改为图片替代文字；三个冻结源重新绑定 App 配方（JE-2000F、JE-3600A 同时重新绑定英语清单），登记表新增 14 行，测试、文档与入库评审附记同步；JE-3000C 英语不变。同一次改动把 MA-157 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-2000F/EU 英/法/西/德/意/乌 Web **2.2**（由 2.1 升级）、JE-3000C/EU 法/西/德/意/乌 Web **2.3**（由 2.2 升级）、JE-3600A/EU 英/西/法 Web **2026-05-25.3**（英语由 .1、西/法由 .2 升级），JE-3000C 英语不重建，合为一个 Hello-Docs `publish` → `main` PR；仅当候选差异只有：这 12 个路由的添加设备图换成新裁图（拼合图与其周围空行、或旧成品图那一行）、新图入池（及不再被引用的旧池文件随之移除）、以及发布证据与元数据时，全部检查成功后合入（不带 `--delete-branch`），并逐个核对 RTD 网址。**不含**：#1258 的合入（由操作者合）、JE-3600A 法/西整段英文漏翻、Hello-Docs `review/*` 评审分支 PR、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。（原拟 MA-158 已被 #1259 占用，MA-156 由 #1258 分支预留，故用 MA-159。） | 「14 张都换（推荐）」「全绿后合入（推荐）」「各升一版（推荐）」「候选只动这些就发（推荐）」2026-09-24（答复借用控制面板图这一项的方案、合入、版本与发布四问） | (a) 与 (b) 均合入即失效，或操作者撤销 | 已失效（#1262 squash `77fe0bd5`、Hello-Docs #128 squash `d134287b` 均已合入，RTD 十四个规范网址核对完成。更正：(b) 所写「12 个路由」实为 14 个——12 个拼合图路由加 JE-2000F、JE-3600A 两条英语，即 (a) 的 14 张裁图；操作者 2026-09-25 以「按 14 条合入」确认后才合入 #128） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-158 | 按顺序合入两项 PR：(a) PR #1259（`docs/registry-operator-ruling-0925`，**含本登记行**）：按操作者 2026-09-25 裁定把 MA-066、MA-016、MA-104 改为已失效，并把 #1257 已用完的 MA-155 改为已失效；只改这四行的状态格。(b) PR #1261（`fix/portal-voc-off`）：`product_voc_endpoint` 由已失效的临时隧道地址改为空（按 `product_voc.md`，空即关闭：手册页与手册中心首页不再渲染产品建议入口），`product_voc.md` 记下试运行结束与重新开启条件，用户手册补一句；整站 A/B 中 55 个页面只去掉建议入口，搜索索引只少建议表单自身的文字。两项均须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；(b) 合入后经既有镜像与 RTD 链路核验线上手册页与手册中心首页不再出现建议入口。**不含**：VOC 接收端代码、VOC 表数据、新的接收地址、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「等 CI 跑完，你合入」2026-09-25（此前「MA-066、MA-016、MA-104 算失效」「先关掉提交，开 PR」） | (a)(b) 均合入且线上核验完成即失效，或操作者撤销 | 已失效（#1259 squash `675748af`、#1261 squash `b8432782` 均已合入；2026-09-25 RTD 核验手册中心首页与手册页不再出现产品建议入口与临时隧道地址，搜索索引不含建议表单文字） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-157 | 按顺序合入两项 PR：(a) PR（`fix/web-eu-control-button-labels`，**含本登记行**）：JE-3000C/EU 与 JE-3600A/EU 冻结源 `Spec_Master.csv` 中 `CONTROLS` 段三个按键名（主电源、DC/USB、AC）补上各语块印刷原文（JE-3000C 法/西/德/意/乌 15 格，JE-3600A 法/西/德/意 12 格），两份 `source_manifest.json` 重新锁定，冻结源 README 与入库评审文档补附记，新增测试；不写飞书（两冻结源无线上依赖，线上占位参数表也无这两个目标的行）。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源（默认来源模式）Git-only 构建 JE-3000C/EU 法/西/德/意/乌 Web **2.2**（由 2.1 升级）与 JE-3600A/EU 西/法 Web **2026-05-25.2**（由 2026-05-25.1 升级），两个英语路由不重建，合为一个 Hello-Docs `publish` → `main` PR；仅当候选差异只有这 7 个页面上按键名所在行的变化以及发布证据与元数据时，全部检查成功后合入（不带 `--delete-branch`），并逐个核对 RTD 网址。**不含**：JE-3600A 法/西页面整段英文漏翻的处理、#1258 的合入（由操作者合）、Hello-Docs `review/*` 评审分支 PR、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。（MA-156 已由 #1258 分支预留，故本行用 MA-157。） | 「按印刷原文补两个型号（推荐）」「全绿后合入（推荐）」「3000C 2.2，3600A 05-25.2（推荐）」「候选只动这些就发（推荐）」2026-09-24（答复 JE-3000C/JE-3600A 按键名的方案、合入、版本与发布四问） | (a) 与 (b) 均合入即失效，或操作者撤销 | 已失效（#1260 squash `18f85dfb`、Hello-Docs #127 squash `91a860c9` 均已合入，RTD 七个规范网址与按键名核对完成） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-155 | PR #1257（`docs/registry-expire-spent-0925`，**含本登记行**）：登记表清账——把失效条件已满足的 MA-152、MA-154 两行状态改为已失效（MA-152 附 #1255 `09f3ba1f`、Hello-Docs #126 `54463827` 两个 squash SHA，以及 2026-09-25 RTD 英/法/西/德/意五个规范网址均返回 200、版本为 2.2 的核验；MA-154 附 #1256 `ae3c6940` 与线上 `/workspace/`、`/workspace/system/` 的核验）。只改这两行的状态格，不增删行；MA-002、MA-016、MA-023、MA-066、MA-104 保持生效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：其它行的内容改动、重复编号（MA-067/077/081）的改号、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「等 CI 跑完，你合入」2026-09-25（此前「清一次登记表」） | #1257 合入即失效，或操作者撤销 | 已失效（#1257 squash `38d07259` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-154 | PR #1256（`fix/portal-workspace-review-followups`，**含本登记行**）：工作空间 `/workspace/` 与系统建设页不再依赖 AI 分享包——`collect_workspace_pages` 始终生成两页，分享包缺失时只隐藏分享导航、分享卡片、搜索框与最近更新中的分享条目（无分享时改用只绑定手机菜单的脚本），系统建设页按 `has_share` 隐藏分享链接；有分享时 `workspace/index.html` 与 main 逐字节一致。语言资产块写明是语料库句对覆盖（各语言有译文的句对占记忆库全部句对的比例）：每条柱加提示，格子改为「语料覆盖语言」，图下注明不是说明书翻译完成率。Hello-Docs `4f36d5eb` 整站 A/B：有分享时 1195 个文件中 4 个按预期变化（系统建设页只改语料文案），232 个 HTML 中 231 个逐字节一致；无分享时新增工作空间与系统建设页两页，原有 209 个 HTML 全部不变。同一次改动把 MA-153 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge，合入后经既有镜像与 RTD 链路核验线上 `/workspace/` 与 `/workspace/system/`。**不含**：SSOT 来源登记（待设计文档）、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「等 CI 跑完，你合入」2026-09-25（此前「前两项合成一个 PR，开 PR」） | #1256 合入且线上 `/workspace/` 与 `/workspace/system/` 核验完成即失效，或操作者撤销 | 已失效（#1256 squash `ae3c6940` 已合入；2026-09-25 RTD 实页核验 `/workspace/` 与 `/workspace/system/` 通过） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-153 | PR #1254（`feat/portal-system-focus-lanes`，**含本登记行**）：系统建设页 `/workspace/system/` 按操作者 2026-09-25 定的四条线重排——页首「当前重点」（现在：说明书网页化、语料库建设与管理；下一步：IR 共享、骨架拓展），每条线的数字在构建时由发布清单、语料快照和 `docs/manifests/skeletons/*/blueprint.yaml` 算出，进度读执行台账；语言资产上移，`corpus-export` 按月带历史（`history`），页面显示与上期对比；能力卡与阶段门加线标签，评审回写、Agent 执行两张卡和 IDML、G4 两道门 `fold: true` 收起；去掉首屏阶段语与六格快照；`tools/utils/path_utils.py` 增 `skeletons_of`；执行台账新增 REV-35–43（均 planned），先在范围变更表登记（语料线可与 G1–G3 并行，IR、骨架不进 G1–G4 串行门、不阻塞 Web 门）。Hello-Docs `9745093a` 整站 A/B：1195 个文件中 4 个按预期变化（页面、样式表、部署回执对应条目、`.buildinfo` 路径哈希），232 个 HTML 中 231 个逐字节一致。本 PR 含执行台账条目（状态类文档，惯例由操作者合入）；Claude 说明后操作者仍明确指示由 Claude 合入。同一次改动把 MA-149 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge，合入后经既有镜像与 RTD 链路核验线上 `/workspace/system/`。**不含**：飞书写入（内部页四条线对照表已按操作者批准的方案另行完成）、定时联网校验或定时语料导出的 workflow、Hello-Docs 评审分支上的任何合入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「等 CI 跑完. 你合入」2026-09-25（此前「1,2」批准页面调整与台账补条目，本地预览后「①对 ②不改 ③可以，开 PR」） | #1254 合入且线上 `/workspace/system/` 核验完成即失效，或操作者撤销 | 已失效（#1254 squash `0f11b193` 已合入；2026-09-25 RTD 实页核验通过） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-152 | 按顺序合入两项 PR：(a) PR（`fix/je1000f-eu-review-app-asset-uris`，**含本登记行**）：JE-1000F/EU 冻结评审副本 `docs/_review/JE-1000F/EU` 中 FR/ES/DE/IT/UK 五个 App 设置页（p31/p46/p61/p76/p91）与 DE/ES/IT/UK 四个生成稿的 `add_device`、`connect_result` 图，由共享日规截图的原始路径改为 `asset:app/add_device`、`asset:app/connect_result`（与 Hello-Docs #125 同样改法），共 18 处；`manual_sources/JE-1000F/EU/en-fr/2.0/source_manifest.json` 只重新锁定这 9 个文件并追加一条规范化记录，冻结源 README 补附记，`web_publish_pipeline.md` §1.1 补一句，新增守卫测试；同一次改动把 MA-151 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。(b) 在 (a) 合入且镜像同步落地后，从新 main 按冻结源发布配方（英/法 `review-asis`、西/德/意 `review`）Git-only 构建 JE-1000F/EU 英/法/西/德/意 Web **2.2**，开 Hello-Docs `publish` → `main` PR；仅当候选差异只有：五个页面各一行 App 连接结果图（隐藏语义图由日规截图换成欧规导出图）、欧规图入池（及不再被引用的旧池文件随之移除）、共享 `web_manual.css` 中 #1253 那一条规则、以及发布证据与元数据时，全部检查成功后合入（不带 `--delete-branch`），并逐个核对 RTD 网址。**不含**：Hello-Docs `review/*` 评审分支 PR（含 #125）的合入、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「按这样两步做（推荐）」「全绿后合入（推荐）」「2.2（推荐）」「候选只动这些就发（推荐）」2026-09-24（答复 JE-1000F/EU 隐藏日规截图的方案、合入、版本与发布四问） | (a) 与 (b) 均合入即失效，或操作者撤销 | 已失效（#1255 squash `09f3ba1f`、Hello-Docs #126 squash `54463827` 均已合入；2026-09-25 RTD 英/法/西/德/意五个规范网址均返回 200、版本为 2.2） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-151 | PR #1253（`fix/web-app-control-panel-aspect`，**含本登记行**）：修复手机上 App 添加设备控制面板的本地化按键标签重叠——`docs/renderers/contracts/web_app_components.css` 的控制面板图规则加 `aspect-ratio: auto 1203 / 265`（懒加载图片到达前先占住高度，加载后以图片自身比例为准）；`tests/test_web_presentation.py` 新增守卫测试（要求 `auto W / H` 写法，且数值等于合同中声明的每一张 `control_artwork` 的像素比例）；`code-as-doc/build_doc_guide.md` 补一句说明。同一次改动把 MA-150 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：任何 Web 发布（操作者选择随下次发布上线）、Hello-Docs PR、页面 Markdown 或资产改动、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「Merge when green (Recommended)」「Ride the next publish (Recommended)」2026-09-24（答复 #1253 合入与上线方式两问） | #1253 合入即失效，或操作者撤销 | 已失效（#1253 squash `1dfd0912` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-150 | PR #1252（`feat/assets-je1000f-eu-app-promotion`，**含本登记行**）：把打印版 App 截图转正模块 `tools/app_ui_promotion.py` 改成多份显式合同（美规 `je1000f-us-app-ui-v1` 名称、取值与行为不变），新增欧规合同 `je1000f-eu-app-ui-v1`（审核人唐夏冰，决定时间 2026-09-24T06:18:00-07:00＝操作者当天「三张都确认」；范围 JE-1000F/EU en/fr/es/de/it/uk；源与冻结参照均为欧规印刷版 V2.0-2026-06-18）；两张欧规印刷裁图按配方保持隔离、转正导出物与裁图逐字节一致；登记表新增 `app/je1000f_eu/add_device`、`app/je1000f_eu/connect_result` 两行覆盖；raw-LaTeX 别名只在两份合同各自的目标内生效；语言字面量基线只换一行。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：Hello-Docs `review/JE-1000F-EU` 评审分支 PR 的合入（按规则由操作者合）、auto-manual 自带 `docs/_review/JE-1000F/EU` 副本改动、JE-1000F/EU 网页重新发布、飞书写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「按计划两步走（推荐）」「五语 + 乌克兰语（推荐）」「全绿后合入（推荐）」2026-09-24（答复第 2 项方案、语言范围与 #1252 合入三问） | #1252 合入即失效，或操作者撤销 | 已失效（#1252 squash `367bd444` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-149 | PR #1251（`docs/tests-node-ui-note`，**含本登记行**）：`tests/AGENTS.md` 与 `tests/CLAUDE.md` 各补三处（开头说明、地图一行、验证命令一行）：浏览器端门户脚本另有 Node UI 测试 `*.test.mjs`，由 `Manual Validation` 的 `node-ui` 任务全部运行，本地命令 `node --test tests/*.test.mjs`；改前已按 Claude 与 Codex 两份 config-review 流程审查。同一次改动把 MA-147 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：根 `AGENTS.md` / `CLAUDE.md`、其它目录的导航文件、`.claude/**` 与 `.agents/**`、workflow、依赖、公开 CLI 或 Base schema 变更。 | 「合入 #1251」2026-09-24（此前「tests/AGENTS.md 和 CLAUDE.md 也补上」） | #1251 合入即失效，或操作者撤销 | 已失效（#1251 squash `7144fd59` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-148 | 按顺序合入两项 PR：(a) PR（`feat/web-je3600a-eu-app-connect`，**含本登记行**）：JE-3600A/EU 英/法/西三条在线路由的网页 App 连接结果改用欧规印刷版（`source/manual_je3600a_eu_uk_20260525`）p22 英语语块 12x 裁出的共用面板（`a1dfd4b7…`，三屏加 2.3/2.4/2.5 编号，不含「截图仅供参考」句），配方条目按 App/QR 配方关卡保持隔离；英语插图清单追加一条、新建法/西两个单条清单并在 `config.eu-{fr,es}.yaml` 绑定，冻结源 `source_manifest.json` 绑定 `app_asset_recipe` 并重绑英语清单，登记表新行 `web/je3600a/eu/shared/app_connect_result`。(b) 其后那一项 Hello-Docs `publish` → `main` 发布 PR：JE-3600A/EU 英/法/西三条单语路由按冻结源 `manual_sources/JE-3600A/EU/en/2026-05-25/phase2` 由 (a) 合入后的 main 以 Git-only 方式重建为 **2026-05-25.1**；前提是三语 Markdown 相对线上只变这张 App 图（可附带与 Hello-Docs #120/#121 相同的文件层格式跟进；另外英语路由为 09-12 旧格式，允许顺带补齐发布证据：新增语言投影证据文件、清单条目 `language_scope` 由 `legacy_unspecified` 改为 `single` 并登记证据哈希、两个 IR 源副本改名（字节不变）、删除单独的 `web_manual.css`——操作者看过预演后于 2026-09-24 以「按这样发（推荐）」批准），且改动全部在 `docs/publish/**`、只涉及 JE-3600A/EU、资产池与发布清单；出现其它差异即停下报告、不开该 PR。两项均须最终 head 包含最新 main（(b) 为 Hello-Docs `main`）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge（(b) 不带 `--delete-branch`）；(b) 合入后核对 RTD 上三个规范网址与图片。**不含**：德/意路由（未发布）、Hello-Docs 评审分支上的任何合入、其它型号或目标的发布、飞书写入（Git-only 不写 `HTML_link`）、打印用 App 截图、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「确认这张（推荐）」「同前（推荐）」「2026-05-25.1（推荐）」2026-09-24（答复 JE-3600A 候选图、合入发布与版本号三问）；「按这样发（推荐）」2026-09-24（答复英语路由格式跟进问题） | (a)(b) 均合入且 RTD 核对完成即失效；(b) 因差异超范围停下时仅 (a) 生效；或操作者撤销 | 已失效（#1250 squash `9efaad3c`、Hello-Docs #124 squash `79d27fd7` 均已合入，RTD 英/西/法三个规范网址与面板图核对完成） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-147 | PR #1249（`chore/ci-node-ui-tests`，**含本登记行**）：`Manual Validation` 新增 `node-ui` 任务（Node 22、无 npm 依赖，沿用现有 Node 任务的 checkout/setup-node 写法），运行全部 `tests/*.test.mjs`；`code-as-doc/build_doc_guide.md` 记下该任务与本地命令。本 PR 首次 CI 中 `node-ui` 实跑 5/5 通过；同一次改动把 MA-146 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：把 `node-ui` 设为必需检查、钉钉适配器 Node 测试接入 CI（操作者 2026-09-24 选择不管）、`tests/AGENTS.md` / `tests/CLAUDE.md` 改动、其它 workflow、依赖、公开 CLI 或 Base schema 变更。 | 「合入 #1249」「等它跑完再合入 #1249」2026-09-24（此前「把 Node UI 测试接进 CI」） | #1249 合入即失效，或操作者撤销 | 已失效（#1249 squash `cca6d904` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-146 | PR #1247（`fix/portal-voc-preview-copy`，**含本登记行**）：产品建议弹窗的预览模式回复由 `Preview only — nothing was sent to Feishu.` 改为 `Preview only — nothing was sent.`（保留参考编号，含义不变），`tests/product_voc_ui.test.mjs` 断言新文案且回复不含平台名（本地 5/5 通过；换回旧 JS 时该断言失败）；同一次改动把 MA-144 标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge，合入后经既有镜像与 RTD 链路核验线上 `product-voc.js`。**不含**：服务端 `integrations/product_voc/**` 的异常文本与注释、把 Node UI 测试接入 CI（需改 `.github/workflows/**`）、依赖、公开 CLI 或 Base schema 变更。 | 「合入 #1247」2026-09-24（此前「预览模式那句 Feishu 也删掉」） | #1247 合入且线上 `product-voc.js` 核验完成即失效，或操作者撤销 | 已失效（#1247 squash `7d444194` 已合入；线上 `product-voc.js` 已为新文案、无 Feishu，手册页引用 `?v=17a9d866`） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-145 | 按顺序合入两项 PR：(a) PR（`feat/web-je2000e-eu-app-screens`，**含本登记行**）：JE-2000E/EU 法/西/德/意/乌五语网页两张 App 截图改用欧规印刷版（`source/manual_je2000e_eu_uk_20260803`）英语语块按本型号英语面板的 12x 裁出的共用面板：p23 添加设备两屏（`66264675…`，含 2.1/2.2 编号，止于其下各语块按本语言标注的灰底控制面板框之上，按钮名留作网页正文）、p24 连接结果三屏（`efa9084d…`，含 2.3/2.4/2.5 编号，不含「截图仅供参考」句）；配方两条均按 App/QR 配方关卡保持隔离；新建五个两条目插图清单并在 `config.eu-{fr,es,de,it,uk}.yaml` 绑定，冻结源 `source_manifest.json` 绑定 `app_asset_recipe`，登记表新增两行 `web/je2000e/eu/shared/app_add_device`、`…/app_connect_result`；英语不变。(b) 其后那一项 Hello-Docs `publish` → `main` 发布 PR：JE-2000E/EU 法–乌五条单语路由按冻结源 `manual_sources/JE-2000E/EU/en/2.0/phase2` 由 (a) 合入后的 main 以 Git-only 方式重建为 **2.1**；前提是五语 Markdown 相对线上只变这两张 App 图（可附带与 Hello-Docs #120/#121 相同的文件层格式跟进），且改动全部在 `docs/publish/**`、只涉及 JE-2000E/EU、资产池与发布清单；出现其它差异即停下报告、不开该 PR。两项均须最终 head 包含最新 main（(b) 为 Hello-Docs `main`）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge（(b) 不带 `--delete-branch`）；(b) 合入后核对 RTD 上五个规范网址与图片。**不含**：英语路由重建、逐语言控制面板线稿（操作者定为另开任务）、Hello-Docs 评审分支上的任何合入、其它型号或目标的发布、飞书写入（Git-only 不写 `HTML_link`）、打印用 App 截图、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「两张都确认（推荐）」「这次只换截图（推荐）」「同 JE-3000C（推荐）」2026-09-24（答复 JE-2000E 候选图、控制面板与合入发布三问） | (a)(b) 均合入且 RTD 核对完成即失效；(b) 因差异超范围停下时仅 (a) 生效；或操作者撤销 | 已失效（#1248 squash `fe6d61ec`、Hello-Docs #123 squash `9ab3611e` 均已合入，RTD 法/西/德/意/乌五个规范网址与两张面板图核对完成） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-144 | PR #1246（`docs/registry-expire-spent-grants`，**含本登记行**）：登记表清账——把 42 行失效条件已满足的授权状态改为已失效，每行写明所列 PR 的 squash SHA，带上线核验条件的另附证据（Hello-Docs 每日部署校验 2026-09-24 run 35974913402 或 RTD 实页检查）；MA-088、MA-109 按操作者 2026-09-24 裁定改为已失效。只改状态格，不增删行；MA-002、MA-141、MA-066、MA-016、MA-023、MA-104 保持生效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：其它行的内容改动、重复编号（MA-067/077/081）的改号、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「合入 #1246」2026-09-24（此前「清一次登记表」「MA-088 和 MA-109 算失效」） | #1246 合入即失效，或操作者撤销 | 已失效（#1246 squash `1f733d33` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-143 | 按顺序合入两项 PR：(a) PR（`feat/web-je3000c-eu-app-connect`，**含本登记行**）：JE-3000C/EU 法/西/德/意/乌五语网页 App 连接结果改用欧规印刷版（`source/manual_je3000c_eu_uk_20260731`）p21 英语语块按本型号英语 App 面板的 12x 裁出的共用面板（`0dfd46ee…`，三屏加 2.3/2.4/2.5 编号，不含「截图仅供参考」句），配方条目按 App/QR 配方关卡保持隔离；新建五个单条插图清单并在 `config.eu-{fr,es,de,it,uk}.yaml` 绑定，冻结源 `source_manifest.json` 绑定 `app_asset_recipe`，登记表新行 `web/je3000c/eu/shared/app_connect_result`；英语不变。(b) 其后那一项 Hello-Docs `publish` → `main` 发布 PR：JE-3000C/EU 法–乌五条单语路由按冻结源 `manual_sources/JE-3000C/EU/en/2.0/phase2` 由 (a) 合入后的 main 以 Git-only 方式重建为 **2.1**；前提是五语 Markdown 相对线上只变这张 App 图（可附带与 Hello-Docs #120/#121 相同的文件层格式跟进），且改动全部在 `docs/publish/**`、只涉及 JE-3000C/EU、资产池与发布清单；出现其它差异即停下报告、不开该 PR。两项均须最终 head 包含最新 main（(b) 为 Hello-Docs `main`）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge（(b) 不带 `--delete-branch`）；(b) 合入后核对 RTD 上五个规范网址与图片。**不含**：英语路由重建、Hello-Docs 评审分支上的任何合入、其它型号或目标的发布、飞书写入（Git-only 不写 `HTML_link`）、打印用 App 截图、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「确认这张（推荐）」「同 JE-2000F（推荐）」2026-09-24（答复 JE-3000C 候选图与合入发布两问） | (a)(b) 均合入且 RTD 核对完成即失效；(b) 因差异超范围停下时仅 (a) 生效；或操作者撤销 | 已失效（#1245 squash `9dc0aa46`、Hello-Docs #122 squash `885f4d66` 均已合入，RTD 法/西/德/意/乌五个规范网址与面板图核对完成） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-142 | PR #1243（`feat/portal-system-workspace`，**含本登记行**）：新增 RTD 知识库「系统建设」页 `/workspace/system/`。状态契约 `tools/rtd_portal_assets/system_workspace.yaml`（能力卡、生产流程连接、阶段门标签，每条带证据）、`tools/rtd_system_workspace.py`（规则校验、`check` / `check --online`、只读的 `corpus-export`）、页面模板与样式；`collect_workspace_pages` 在 `workspace/index` 之后挂页，知识库侧栏只在页面生成时显示入口；语言资产块读取汇总快照 `system_workspace_corpus.json`（只存计数，2026-09-24 首份：句对 1372、术语 232）。Hello-Docs `3dededf7` 整站 A/B：新增 2 个文件、3 个文件按预期变化，231 个 HTML 中 230 个逐字节一致。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：飞书写入（内部治理视图已按操作者另行指示建好）、定时联网校验或定时语料导出的 workflow、Hello-Docs 评审分支上的任何合入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「合入 #1243」2026-09-24（此前操作者本地看过整站渲染后答「没问题，补测试和文档，开 PR」，语料库可视化答「1」「3」） | #1243 合入即失效，或操作者撤销 | 已失效（#1243 squash `14c04619` 已合入；2026-09-24 RTD 实页核验通过） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-141 | 按顺序合入两项 PR：(a) PR #1244（`feat/web-je2000f-eu-app-connect`，**含本登记行**）：JE-2000F/EU 六语（en/fr/es/de/it/uk）网页 App 连接结果改用欧规印刷版（`source/manual_je2000f_eu_uk_20260803`）p21 英语语块裁出的共用面板（`6797b35e…`，三屏加 2.3/2.4/2.5 编号，不含「截图仅供参考」句），配方条目按 App/QR 配方关卡保持隔离；六个语言插图清单各加一条替换，冻结源 `source_manifest.json` 重绑英语清单并绑定 `app_asset_recipe`，登记表新行 `web/je2000f/eu/shared/app_connect_result`。(b) 其后那一项 Hello-Docs `publish` → `main` 发布 PR：JE-2000F/EU 六条单语路由按冻结源 `manual_sources/JE-2000F/EU/en/2.0/phase2` 由 (a) 合入后的 main 以 Git-only 方式重建为 **2.1**；前提是六语 Markdown 相对线上只变这张 App 图（英语可附带与 Hello-Docs #120 相同的文件层格式跟进），且改动全部在 `docs/publish/**`、只涉及 JE-2000F/EU、资产池与发布清单；出现其它差异即停下报告、不开该 PR。两项均须最终 head 包含最新 main（(b) 为 Hello-Docs `main`）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge（(b) 不带 `--delete-branch`）；(b) 合入后核对 RTD 上六个规范网址与图片。**不含**：Hello-Docs 评审分支上的任何合入、其它型号或目标的发布、飞书写入（Git-only 不写 `HTML_link`）、打印用 App 截图、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「全绿后合入」「候选只动这张图就发（推荐）」「统一 2.1」2026-09-24（答复 #1244 合入、发布与版本号三问） | (a)(b) 均合入且 RTD 核对完成即失效；(b) 因差异超范围停下时仅 (a) 生效；或操作者撤销 | 已失效（#1244 squash `8cb694eb`、Hello-Docs #121 squash `2207a86e` 均已合入，RTD 六个规范网址与面板图核对完成） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-140 | 按顺序合入两项 PR：(a) Hello-Docs PR #120（`publish` → `main`）——JE-1000F/EU 五语（en/fr/es/de/it）Web **2.1** 的 Git-only 发布：由 auto-manual `95a6907c`（#1241 冻结源）构建，App 连接结果图改用欧规印刷版的英文截图面板，英语另换 LED 隐藏层底图（可见成品图不变）；115 个文件全部在 `docs/publish/**`，且只涉及 JE-1000F/EU、资产池与发布清单；54 个目标集合不变，英语旧路由 `JE-1000F/EU/md` 与别名保留。(b) 本登记 PR（`docs/authorize-hello-docs-120`，**含本登记行**）。先合 (a) 再合 (b)（先合 (b) 会让镜像同步推进 Hello-Docs main，使 (a) 的 head 落后）。两项均须最终 head 包含最新 main（(a) 为 Hello-Docs `main`）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；(a) 合入后核对 RTD 上五个规范网址、根别名、旧路由与图片。**不含**：Hello-Docs 评审分支上的任何合入、其它目标的发布、飞书写入（Git-only 不写 `HTML_link`）、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「批准，全绿后合入」2026-09-24（答复 Hello-Docs #120 上线问题） | (a)(b) 均合入且 RTD 核对完成即失效，或操作者撤销 | 已失效（Hello-Docs #120 squash `2d52fd92`、#1242 squash `fde3558b` 均已合入，RTD 核对完成） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-139 | PR #1241（`feat/web-je1000f-eu-frozen-app-panel`，**含本登记行**）：把 #1240 的欧规 App 共用面板并入 JE-1000F/EU 冻结源 `manual_sources/JE-1000F/EU/en-fr/2.0/phase2`（清单条目按原顺序追加，附件与夹具逐字节一致）；`source_manifest.json` 锁定完整 phase2（115 个文件：补登 #1138 的 33 个西德意附件、修正已过期的清单哈希），`build_configs` 补西德意，README 写明西德意用 `--source review`（默认的 `auto` 下 check 不做评审同步，三步证据对不上；2026-09-24 更正）；新增冻结源一致性测试。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：Hello-Docs 发布 PR 的合入（欧规五语 2.1 发布另经操作者批准）、`target.languages`/`publication_status`/`unverified_languages` 字段改动、飞书写入、Hello-Docs 评审分支上的任何合入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「全绿后合入」2026-09-24（答复 #1241 合入问题；同轮定欧规五语重新发布版本号为 2.1） | #1241 合入即失效，或操作者撤销 | 已失效（#1241 squash `95a6907c` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-138 | PR #1240（`feat/assets-je1000f-eu-app-screenshots`，**含本登记行**）：JE-1000F/EU 网页 App 连接结果改用欧规印刷版（`source/manual_je1000f_eu_uk_20260618`）p22 裁出的成品面板，五语共用（`locale=shared`），配方条目按 App/QR 配方关卡保持隔离；测试夹具面板与清单条目（钉值 `7a8700a6…`）、登记表新行 `web-composite/je1000f_eu/reference.app-connect-result`，并把两张共享 App 图 `app/add_device`、`app/connect_result` 的备注如实改为日规截图（解析不变）。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：飞书 `04_资产定义`/`04_资产导出物` 两行的写入（另经操作者批准）、欧规网页重新发布、打印用 App 截图覆盖版与转正合同（另开任务）、另外四个欧规型号、Hello-Docs 评审分支上的任何合入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「全绿后合入」2026-09-24（答复 #1240 合入问题） | #1240 合入即失效，或操作者撤销 | 已失效（#1240 squash `11a754d4` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-137 | 本 PR（`fix/web-receipt-url-field`，**含本登记行**）：把工作树 `auto-manual-worktrees/web-receipt-readback`（分支 `fix/web-receipt-url-readback`，2026-09-20 写成、从未提交或推送）里的回执读回修复并入主线。`tools/document_link_queue.py` 新增 `split_rendered_url` / `url_field_matches` / `describe_url_field`；`tools/write_web_publish_receipt_links.py` 的读前比对与写后读回改用 `url_field_matches`（链接文字与目标都等于规范网址才算已登记，取代 #1238 的 `registered_link`；人工加标题的链接会被改写成规范网址）；`tools/ops_catalog_sync.py` 的 `extract_link` 改用同一解析（链接目标优先）。同时更正 #1238 引入的措辞：`HTML_link` 是超链接（URL）字段，不是文本字段。同一次改动把 MA-107（Hello-Docs #95）、MA-118（Hello-Docs #106）标为已失效。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：删除或改动 `web-receipt-readback` 工作树本身、触发回执或发布工作流、线上表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「那份旧修复并入主线，顺便改掉措辞」「MA-107 和 MA-118 也标成失效」2026-09-24（答复清理时发现的未提交回执修复） | 本 PR 合入即失效，或操作者撤销 | 已失效（#1239 squash `846558e7` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-136 | 按顺序合入两项 PR：(a) Hello-Docs PR #119（`publish` → `main`，发布队列 run 35996540411 生成）——发布 JE-1000F/US **2.5** 的 en/fr/es 三个单语 Web 目标（队列行 `recvw8AhrrIddm` / `recvw8Ahrr9doj` / `recvw8AhrrglsY`）：操作图改用登记底图加实时文字，LED 改用美规自有底图；PR 只改 `docs/publish/**`，其它目标文件逐字节不变，图片池 582 个引用全部可解析、无孤儿文件。共享 `web_manual.css` 里 #1227 的参考图整栏宽规则会让欧规五语 `app-connect-result` 截图由 360px 变为整栏宽（该截图本身是日语界面的 Explorer 1500 Ultra 错图），操作者选择照常合入、错图另开任务。(b) 本 PR（`fix/web-receipt-link-readback`，**含本登记行**）：`tools/write_web_publish_receipt_links.py` 的读前比对与写后读回接受 `lark-cli base +record-get` 把超链接字段（`HTML_link` 是 URL 类型字段；原登记误写为「文本单元格」，2026-09-24 更正）读成 Markdown 链接（链接文字与目标都是该网址）的形态（09-20、09-21 两次回执失败的根因）。(b) 合入且镜像同步后，手动触发一次 `web-publish-receipt.yml`，补齐 2.5 三行 `HTML_link` 并逐行读回。两项均须最终 head 包含最新 main（(a) 为 Hello-Docs `main`）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：Hello-Docs 评审分支（`review/*`）上的任何合入、欧规 App 截图替换（另开任务）、再次触发发布队列、除回执工作流写 `HTML_link` 以外的线上表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「照常合入，错图另开任务」「授权你用我的账号重跑」「开 PR，全绿后合入并补跑回执」2026-09-24（答复 HD#119 合入前三问）；此前「批准，按此执行」「批准，修复后重跑」2026-09-24（美规 2.5 发布队列：加版本选项、写三行、触发构建、全绿后合入发布 PR） | (a)(b) 均合入且 2.5 三行 `HTML_link` 回执读回通过即失效，或操作者撤销 | 已失效（Hello-Docs #119 squash `fe80f661`、#1238 squash `e7652ff9` 均已合入；回执 run 35999578828 三行读回通过） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-135 | PR #1237（`fix/idml-front-product-override-width`，**含本登记行**）：IDML 按资产槽位识别 `overview/front_product`（整栏宽、段后距 1.58 pt），UPS 覆盖图按槽位取 UPS 段前距；17 个 IDML 目标 28/30 包不变，只变 JE-1000F/JP 模板源两个包，InDesign 25 页 0 溢出（并入含 #1236 的 main 后复验）；生产评审源输出不变。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：Hello-Docs 评审分支（`review/*`）上的任何合入（操作者 2026-09-24：评审分支不能合）、JBP-2000B `left_side_ports` 宽度、手动触发发布队列或网页发布、线上源表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「全绿后合入」2026-09-24（答复 #1237 合入问题） | #1237 合入即失效，或操作者撤销 | 已失效（#1237 squash `41988a70` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-134 | PR #1236（`feat/assets-je1000f-eu-jp-led-base-art`，**含本登记行**）：登记 JE-1000F/EU（按语言：en 英标插座、fr/es/de/it 欧标插座）与 JE-1000F/JP 自有 LED 底图——两份配方哈希锁定、6 个导出物、`data/asset_registry.csv` 3 行（清单 431→434）；飞书 `04_资产定义` 同三行已按操作者批准写入并回读（`recvw7710A8Edc` / `recvw7710AMOja` / `recvw7710ATzZr`，预览附件 token 非空；同步合并对 CSV 无改动）。17 个 IDML 目标 22/30 包不变，JE-1000F EU de/en/fr 与 JP 的 LED 卡片与 flow 图换成登记底图，InDesign 两本 0 溢出；欧规网页可见页面不变。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：Hello-Docs 评审分支（`review/*`）上的任何合入（操作者 2026-09-24：评审分支不能合，含 Hello-Docs #117）、JE-1000F/JP 正面产品图的 IDML 宽度修复（另开 PR）、手动触发发布队列或网页发布、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「全绿后合入」2026-09-24（答复 #1236 合入问题） | #1236 合入即失效，或操作者撤销 | 已失效（#1236 squash `5cc23ff3` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-133 | 按顺序合入两项 PR：(a) PR #1234（`refactor/idml-governed-art-asset-keys`，**含本登记行**）：IDML 按资产槽位识别受管底图（`manifest_asset_slot`、`tools/idml/asset_slots.py`、`RenderContext.asset_slot`），LED/节能卡片与 App 图宽不再在渲染代码里写目标文件名，删去 4 条型号字面量白名单；17 个 IDML 目标 30/30 包与 main 逐字节一致；(b) PR #1235（`fix/idml-jp-full-measure-figures`，原叠在 #1234 上，合入前改指向 main 并并入最新 main）：UPS 与充电槽位也按资产键给整栏宽（JE-1000F/JP 五张批准覆盖图 120→312 pt），实测计划下单栏故事估算计入整图挪页留下的页底空白（`story_estimates.StoryHeight`；`tools/idml/stories.py` 行数上限 256→259），JE-1000F/JP 模板源 InDesign 24 页 0 溢出，17 目标矩阵只变 JE-1000F/JP auto 两个包；生产评审源输出不变。两项均须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；(b) 在 (a) 合入之后进行。**不含**：Hello-Docs `review/JE-1000F-JP` 评审数据 PR 的合入（另行授权）、欧规/日规 LED 覆盖图的登记与飞书资产表写入（另行审批）、BP 电池包 NOTE/TIP 三角跨输出修改、手动触发发布队列或网页发布、线上源表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「全绿后顺序合入」2026-09-24（答复合入授权问题） | 两项 PR 均合入即失效，或操作者撤销 | 已失效（#1234 squash `2af885dc`、#1235 squash `91b87239` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-132 | PR #1233（`refactor/idml-alert-icon-common-default`，**含本登记行**）：`data/layout_params.csv` 新增公共默认 `idml_symbols_signal_alert_icon=all`（此前只是渲染器内的默认值），电池包覆盖层 `layout_params.idml-compact.csv` 的 `hazard` 成为对公共值的显式覆盖，并在 `tests/test_layout_token_override.py` 的 `PINNED_OVERRIDES` 钉住；已批准版式计划 `je1000f_us_v2_20260605.json` 做普通重绑，只动 `style.layout_params_sha256`（`2a7e0ea1…` → `9781ef9e…`；页面绑定 0、不做内容重批）；重新生成 `docs/renderers/latex/params.tex`；同步 build guide 身份表与 `layout_params_guide.md` §3.1。17 个 IDML 目标 30/30 包与 main 一致。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：PR #1234（按资产槽位识别受管底图，操作者保留评审）、JE-1000F/JP UPS 与充电图宽修复（另开任务）、电池包 NOTE/TIP 三角在 PDF/Word/网页的跨输出修改（暂缓）、其它区域 LED 底图的登记与飞书资产表写入、手动触发发布队列或网页发布、线上源表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「#1233 公共默认值」2026-09-23（答复合入授权问题；#1234 未勾选） | #1233 合入即失效，或操作者撤销 | 已失效（#1233 squash `585b05ec` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-131 | 按顺序合入三项 PR：(a) auto-manual PR #1231（`fix/idml-je1000f-us-pilot-queue`，**含本登记行**）：JE-1000F/US review 页 16 个文件刷新为发布队列 review 同步（`sync_review --sync-scope params`，en/fr/es）写出的状态，已批准版式计划按操作者批准做内容变更重绑（批准人夏冰，2026-09-23T14:40:20Z；6 个页面绑定，装配与排版图不变），并更正 `idml_component_targets.md`；(b) Hello-Docs PR #116（`backport/JE-1000F-US-queue-sync-refresh` → `review/JE-1000F-US`）：同样 16 个文件，逐字节一致；**协议例外（留痕）**：其 `unit` 检查红是该 review 分支仍停在 2026-08-14 播种代码的既有失败（与 #113 同 29 条：5 failures、24 errors），本 PR 上下文不可能变绿、本 PR 未新增失败；#1231 合入后不合它会破坏 auto-manual `docs/_review/JE-1000F/US` 与该 review 分支的字节一致；先例 MA-013 / Hello-Docs#45；操作者知悉该红后批准；其余检查须全部成功；(c) auto-manual PR #1232（`fix/idml-symbols-alert-icon-scope`）：IDML 信号徽章默认四行都画警示三角（主机线印刷版、PDF、网页一致），电池包覆盖层 `layout_params.idml-compact.csv` 设 `idml_symbols_signal_alert_icon=hazard`，金样经逐 token 核验后重做；JE-3000C/KR 按主机线默认画三角（操作者确认）。auto-manual 两项须最终 head 包含最新 main（#1232 须在 #1231 合入后并入最新 main 并重跑检查）、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：手动触发发布队列或网页发布、线上源表写入、另一窗口的 #1226 与 Hello-Docs #113–#115（循环寿命）的合入或随之需要的重绑、把 `idml_symbols_signal_alert_icon` 默认值迁入 `data/layout_params.csv`（须重钉已批准计划）、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「批准」「继续」2026-09-23（答复三项待定：合入 #1231 与 Hello-Docs #116、#1232 全绿后合入、JE-3000C/KR 按默认） | 三项 PR 均合入即失效，或操作者撤销 | 已失效（#1231 squash `67e72fb6`、Hello-Docs #116 `731f1954`、#1232 squash `956752b3` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-130 | PR #1230（`feat/web-je1000f-us-car-base-art`，**含本登记行**）：JE-1000F/US 网页车充图改用登记底图 `charging/je1000f_us/car_charge`（Word/PDF 与 IDML 已在用），「Vehicle」标签与车充线注释改为实时文字、位置照 IDML `_charging_car` 卡片；网页展示契约新增参考图 `base_art_layout`（色带、底色、每条源文字行一个矩形，fail-closed），整书 IR 冻结该布局；西语注释保持模板措辞 vehículo（操作者已定）。IDML、Word/PDF 与已批准版式计划不变。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：#1223 发布队列 inert 的 review 刷新与内容变更重绑（另开 PR）、西语模板与印刷版的术语统一、飞书合成图清单行的退役及其它线上源表写入、手动触发网页发布或发布队列、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「授权合入」2026-09-23 | #1230 合入即失效，或操作者撤销 | 已失效（#1230 squash `195a76a3` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-129 | PR #1229（`feat/idml-led-card-registered-art`，**含本登记行**）：JE-1000F/US 生产 IDML 的 LED 卡片改用登记底图 `operation/je1000f_us/led_light`（止于编号圆列），替代图 `led_light_complete.png` 只保留给共用 `operation/led_light` 导出；会改变交给设计师的 JE-1000F/US IDML LED 卡片（操作者已确认「=== 改」），其它目标不变。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：其它目标的 LED 底图、LED 卡片以外的 IDML 版式改动、手动触发发布队列或网页发布、线上源表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「授权」2026-09-23 | #1229 合入即失效，或操作者撤销 | 已失效（#1229 squash `30f66015` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-128 | 按顺序合入两项 PR：先 PR #1227（`feat/web-je1000f-dcusb-led-base-art`，**含本登记行**：JE-1000F/US DC/USB 操作图在网页端以 `base-art-live-copy` 渲染、画出的前提药丸色值入契约（`prerequisite_fill`）、两条网页路径共用面板文案规则、LED footer-panel 卡片组件（本 PR 中 LED 仍用已批准合成图）、等待成品面板的参考图满宽），再 PR #1228（`fix/assets-je1000f-us-led-base-art`：JE-1000F/US 专用 LED 底图——V2.0-2026-07-28 美加规印刷 PDF 第 12 页、配方哈希锁定——登记为 `operation/je1000f_us/led_light` 覆盖行，网页、Word/PDF 与 IDML flow 共用；网页 LED 卡片改用该底图；IDML 受管底图识别名单与评审过的型号字面量白名单；操作者批准的版式计划内容变更重绑 `013e0d03`；对应飞书 04_资产定义行 `recvw2kIKmlyDu` 已按批准写入并回读）。#1228 须在 #1227 合入前改以 `main` 为基底（基底分支删除会自动关闭叠加 PR），并在最终 head 纳入包含 #1227 的最新 main。每项须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：IDML LED 卡片改用登记底图（仍用 `led_light_complete.png`，待操作者决定）、V2.0 源文件在 04_资产源文件的归档及其它线上源表写入、关闭 #1220、#1223 发布队列 inert 的 review 刷新与重绑、手动触发网页发布或发布队列、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「授权你 顺序合入」2026-09-23 | #1227 与 #1228 均合入即失效，或操作者撤销 | 已失效（#1227 squash `3e9f38cf`、#1228 squash `0908dca8` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-127 | 按顺序合入两项 PR：先 PR #1221（`feat/web-je1000f-base-art`，**含本登记行**：JE-1000F/US 主电源、AC 输出、节能三张操作图在网页端以 `base-art-live-copy` 渲染），再 PR #1223（`feat/idml-je1000f-registered-components`：JE-1000F/US/en 单语言 IDML 以 pilot 组件目标组合已登记页面；合入后发布队列的该目标 IDML 随之切换版式，操作者已知悉）。#1223 须在 #1221 合入后改以 `main` 为基底，并在最终 head 纳入包含 #1221 的最新 main。每项须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：关闭 #1220、DC/USB 与 LED 两张图的后续 PR（另需授权）、手动触发网页发布或发布队列、线上源表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「授权你 先按顺序合入，再在最新的 main 上开新分支做剩下的图。」2026-09-23 | #1221 与 #1223 均合入即失效，或操作者撤销 | 已失效（#1221 squash `c1c01477`、#1223 squash `153951b5` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-126 | PR #1222（`feat/terminology-jp-rules`，**含本登记行**）：在 `data/terminology_rules.csv` 登记 12 条日语术语规则（飞书 Style Guide 第八章已拍板条目），`tools/check_docs_runtime.py` 为未声明 per-target lang 的单语言家族解析页面语言（术语门与 FCC 渲染契约共用），以及配套测试与 `code-as-doc/build_doc_guide.md` §5.1。须最终 head 包含最新 main、全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge。**不含**：日文模板与飞书规格表的存量修复、`_infer_fragment_lang` 绝对路径推断修复、线上源表写入、`.github/workflows/**`、依赖、公开 CLI 或 Base schema 变更。 | 「授权你合入」2026-09-22 | #1222 合入即失效，或操作者撤销 | 已失效（#1222 squash `ddb2f26a` 已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-125 | 本登记分支 `codex/share-analytics-authorization`（含本行）及 Hello-Docs PR #112（`codex/share-reading-analytics`）。内容仅限 `docs/knowledge/ai-share/**`：接入用户提供的 Umami Cloud 网站 ID，采集浏览、来源、正文滚动档位、引用稿和下载点击；包含统一注入、隐私限制、测试和维护说明。两项 PR 最终 head 包含最新 main、所有检查成功（含非必需项）、无 changes-requested 或未解决评审线程后 squash merge；随后核验 RTD 发布。不含录屏、身份采集、表单、钉钉写入、工程功能或 workflow 变更。 | 「授权你合入」2026-09-21 | 两项 PR 合入及发布核验完成，或操作者撤销 | 已失效（Hello-Docs #112 squash `4aee8de2`、登记 #1219 squash `edd1b668` 均已合入；线上分享页已加载含 Umami 的 `reading-analytics.js`） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-124 | 本登记分支 `codex/share-first-success-authorization`（含本行）与 Hello-Docs `codex/share-first-success` 内容 PR。仅包含已审核的 `docs/knowledge/ai-share/**` 主稿、引用稿、首次练习、工作台说明及 ZIP README 一致性、HTML 渲染校验、引用链接样式和原狗头去白边修正。各最终 head 须包含最新 main、所有检查成功（含非必需项）、无 changes-requested 或未解决评审线程后 squash merge；经既有 RTD 链路核验。排除其他工程代码、workflow、依赖、线上数据和权限变更。 | 「提交吧 推上去 合入」2026-09-21 | 两项 PR 合入及发布核验完成，或操作者撤销 | 已失效（Hello-Docs #111 squash `f652f810`、登记 #1218 squash `c89506f9` 均已合入；授权已用完；分享稿后被新版取代，本次未重新核验线上原文） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-123 | 本授权登记分支 `docs/authorize-hello-docs-110`（含本登记行）与 Hello-Docs PR #110（`docs/ai-share-pelican-cost-note`）。范围仅为 `docs/knowledge/**`：在 AI 分享主稿的鹈鹕彩蛋标题中加入操作者提供的狗头原图和“这只鸟太贵了，慎用”提示，同步生成 HTML 与校验契约。两项 PR 均须最终 head 包含最新 main、全部已配置检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；随后通过既有 RTD 链路核验标题图片。不含说明书、workflow、依赖、线上数据或权限变更。 | 「提交 推送 合入」「推！」「然后合入！」2026-09-21 | 两项 PR 合入且 RTD 实页标题图片核验完成，或操作者撤销 | 已失效（Hello-Docs #110 squash `9fe77791`、登记 #1217 squash `7a05dbfe` 均已合入；线上主稿含「这只鸟太贵了，慎用」提示） |
| MA-122 | 本授权登记分支 `docs/authorize-hello-docs-109`（含本登记行）与 Hello-Docs PR #109（`docs/ai-share-practical-title`）。范围仅为 `docs/knowledge/**`：让实用版分享稿接管既有公开主链接 `ai-share/index.html`，保留原稿 Markdown 源文件但停止发布；`practical.html` 与 `00_打开分享.html` 兼容跳转至主链接；主页沿用原卡片、不新增重复入口；同时移除标题后缀并更新 GitHub 参考页锚点。两项 PR 均须最终 head 包含最新 main、全部已配置检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；随后通过既有 RTD 链路核验主链接与两个兼容入口。不含说明书、workflow、依赖、线上数据或权限变更。 | 「合入」2026-09-21 | 两项 PR 合入且 RTD 三个入口核验完成，或操作者撤销 | 已失效（Hello-Docs #109 squash `eaaa1396`、登记 #1216 squash `c368a7d7` 均已合入；三个入口线上均返回 200） |
| MA-121 | 本授权登记分支 `docs/authorize-hello-docs-108`（含本登记行）与 Hello-Docs PR #108（`docs/ai-share-integrated-flow`）。范围仅为 `docs/knowledge/**`：精简并整合实用版 AI 分享主稿，保留既有首页稿与完整真实生成过程不变，增加经解压启动及改价回归验证的“小野海报工作台” ZIP，并提供 macOS 与 Windows 启动入口。两项 PR 均须最终 head 包含最新 main、全部已配置检查成功（含非必需项，pending 不算绿）、无 changes-requested 或未解决评审线程后 squash merge；随后通过既有 RTD 链路核验新页面与 ZIP 下载。不含说明书、workflow、依赖、线上数据或权限变更。 | 「合入」2026-09-21 | 两项 PR 合入且 RTD 实页与 ZIP 下载核验完成，或操作者撤销 | 已失效（2026-09-21 auto-manual #1215 squash `8e211ee2`、Hello-Docs #108 squash `4fba97e6` 已合入，RTD build `34671350`、实页与 ZIP 下载均验收通过） |
| MA-117 | 本授权登记分支 `codex/share-review-merge-authorization` 与 Hello-Docs `codex/share-review-fixes` 内容 PR。范围为本轮已确认的分享稿审核修正、可运行产品介绍练习及来源说明、HTML/CSS 新手海报练习、完整提示词和配图入口、GPT-6 Astra（High）与 Agent 章节文案、Hello-Docs 链接、Fork/Clone 和上传状态一致性。内容仅限 `docs/knowledge/**`；各最终 head 全部检查成功（含非必需项）、无 changes-requested 或未解决评审线程后 squash merge，经既有镜像及 RTD 链路核验发布。不含说明书、其他工程代码、workflow、依赖、线上数据或权限变更。 | 「提交推上去 再合入」2026-09-20 | 两项 PR 合入且上线核验完成，或操作者撤销 | 已失效（Hello-Docs #103 squash `488057e4`、登记 #1213 squash `4ee5ddee` 均已合入；授权已用完；分享稿后被新版取代，本次未重新核验线上原文） |
| MA-116 | 本授权登记分支 `codex/share-wording-merge-authorization` 与 Hello-Docs `codex/share-rule-wording` 内容 PR；仅修正 `docs/knowledge/ai-share/04_参考资料/02_代码怎样工作.md` 及对应 HTML 中混淆中文概念名和代码语法的说明，改为教学筛选条件的自然语言解释。各最终 head 全部检查成功（含非必需项）、无 changes-requested 或未解决评审线程后 squash merge，并通过既有 RTD 链路发布。不含其他内容、工程代码、workflow、依赖、线上数据或权限变更。 | 「可 提交 然后推送 合并」2026-09-20 | 两项 PR 合入即失效，或操作者撤销 | 已失效（Hello-Docs #102 squash `b2960408`、登记 #1212 squash `82141004` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-115 | 本授权登记分支 `codex/astra-sharing-merge-authorization` 与 Hello-Docs `codex/github-example-chinese` 内容 PR；内容范围仅 `docs/knowledge/**`，包含中文 GitHub 学习示例、GPT-6 Astra 与鹈鹕 HTML 动画、分享主线及延伸场景、分级目录、两个上手附录和引用稿同步。各最终 head 全部检查成功（含非必需项）、无 changes-requested 或未解决评审线程后 squash merge；经既有镜像及 RTD 链路发布。保留产品说明书及其他内容，不含 workflow、依赖、线上数据或权限变更。 | 「好 提交 然后推上去」「然后你合并」2026-09-20 | 两项 PR 合入即失效，或操作者撤销 | 已失效（auto-manual #1211 squash `f2966259`、Hello-Docs #101 squash `933a514d` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-114 | 本授权登记分支 `codex/camping-merge-authorization` 与 Hello-Docs `codex/camping-poster-demo` 内容 PR；分享内容仅限 `docs/knowledge/**`，包含主稿、引用稿、HTML、配图及露营海报教学 demo。各最终 head 全部检查成功（含非必需项）、无 changes-requested 或未解决评审线程后 squash merge。保留其他内容，不含 workflow、依赖、线上数据或权限变更。 | 「合入」2026-09-20（承接已推送的分享稿提交 a466e294） | 两项 PR 合入即失效 | 已失效（auto-manual #1210 squash `7a68a6e6`、Hello-Docs #100 squash `923bafda` 均已合入） |

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-065 | 本次 `codex/rtd-manual-portal` 首页 PR：接入已确认的产品卡片/搜索/US-EU-UK 地区下拉，暂以 EU 为默认；EU/UK 共用冻结目录和手册链接。仅在最终 head 包含最新 main、所有检查成功、无 changes-requested 和未解决评审讨论后 squash merge；随后经既有镜像同步完成 RTD 上线验证。不含其他 PR、线上 Base 写入、正文/翻译/图内容修改、依赖、公开 CLI 或 `.github/workflows/**` 改动。 | 「授权本次首页 PR 全绿后合入并上线」2026-09-12 | 本次首页 PR 合入且 RTD 实页验证完成，或操作者撤销 | 已失效（#1102 squash `a55d666d`；RTD build `34531930` 与实页验收完成） |
| MA-067 | 本轮多语网页三个 PR，**严格按 #1131 → #1132 → #1134 顺序**（#1134 的 base 是 `feat/web-per-language-illustration-manifests`，须待 #1132 合入后自动重定基再合）：#1131（`data/model-languages-dingtalk-coverage`，**含本登记行**——删除 11 行只记录 Web 首发范围的 `model_languages.csv` 行，经全部 40 个已声明 (目标,config) 组合前后对比证明 0 差异）、#1132（`feat/web-per-language-illustration-manifests`——`load_web_document` 把成品插图按 `(语言, 文件名)` 绑定，使合并多语文档首次能拿到成品图；配置新增 `Document_Key -> {语言: 路径}` 形状）、#1134（`feat/je2000f-eu-four-language-illustrations`——JE-2000F/EU 进 `config.eu.yaml` 并绑 en/fr/es/de/it 五语清单，各 11 张成品图）。每个 PR 均须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：乌克兰语清单（章节 id 重复，需先修 slug 生成，单独立项）；插图 bbox 口径重定（会改动已批准的英文资产）；`docs/_review` 或 `docs/_build` 派生面提交；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「授权」2026-09-13（承接同轮对三个 PR 的逐项汇报与合入顺序建议） | 三个 PR 全部合入即失效 | 已失效（2026-09-13 三个 PR 全部合入：#1131 squash `8fa406c4`、#1132 squash `2724c80e`；#1134 因 base 分支随 #1132 删除被 GitHub 自动关闭，同一棵树 `rebase --onto` 去掉已 squash 的提交后重开为 #1135，squash `83c7df79`。各自在最终 head 上检查全 pass、CLEAN、无未解决评审线程） |
| MA-068 | PR #1136（`fix/je2000f-eu-ukrainian-illustrations`，**含本登记行**）——绑定 JE-2000F/EU 乌克兰语成品插图清单，并订正 #1135 里「乌语因 section id 重复无法绑定」这一错误判断（该判断按整篇文档量 id，而 `_consume_covered_annotations` 实为按页求值）。合入后**六语全部 11 张成品图、`missing` 归零**。须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**同时授权**把该六语书经既有 Web Publish 链路发布到 Hello-Docs `docs/publish/**` 并完成 RTD 上线验证：只允许经审核的既有输入、既有家族 config 与既有发布流程；发布后须核对实际 RTD 构建成功与线上页面。**不含**：`section-N` slug 生成修复（会改动已发布手册锚点，单独立项）；插图 bbox 口径重定；新增品类/翻译事实或未批准的源数据变更；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「1136合入 6语 上线 readthedoc」2026-09-13 | #1136 合入且六语书完成 RTD 上线验证，或操作者撤销 | 已失效（#1136 squash `36f0ae0d` 已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-069 | PR #1137（`feat/je2000f-eu-single-language-routes`，**含本登记行**）——把 JE-2000F/EU 声明进 `config.eu-fr/es/de/it/uk.yaml` 并各绑一份成品插图清单，使其按**欧规英语单语相同的 Git-only 形状**发布为五个单语路由（各 11 张成品图）。须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**同时授权**随后的 Git-only 发布：从冻结源 `manual_sources/JE-2000F/EU/en/2.0/phase2` 构建这五个语言包，经既有 `publish_branch_assembly` 形状提交进 Hello-Docs `docs/publish/**`（diff 只允许含该子树），开 PR 合入并验证 RTD 构建成功与线上页面可访问。**不含**：合并六语书的发布（2026-09-14 订正：原记「队列强制从 `review/JE-2000F-EU` 渲染」为**事实错误**——Git-only 路线不经过构建队列，已上线各队列行 remarks 写明 `no build dispatch` 且 `review_trigger_enabled: false`，是事后回执。真实障碍是发布契约无合订本槽位：`release_lang_for_config` 只返回 `languages[0]`，实测装配会删除线上 `manual_je1000f_eu_en.md` 并把 `EU/en` 路由换成标签仍为 `lang: en` 的六语文档，故未发）；`section-N` slug 生成修复；插图 bbox 口径重定；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合入 1137 继续发布」2026-09-14 | #1137 合入且五个单语路由完成 RTD 上线验证，或操作者撤销 | 已失效（2026-09-14 #1137 合入 `c18fe3f4`，五个单语路由经 Hello-Docs #75 上线 RTD 并验证 200） |
| MA-066 | Manual 运营闭环：[验收 checklist](manual_operations_acceptance_checklist.md)，总计划分支 `codex/manual-operations-plan` 及实现分支 `codex/manual-ops-*`。包括既有 JBP-2000B 修订切片 PR #1118（`codex/jbp2000b-pdf-20260911`）这一明确分支别名。每个切片独立 PR，最终 head 对齐最新 main、全部检查成功、无 changes-requested 或未解决讨论后可 squash merge。总计划 PR 必须保持 Draft，直到全部运营出口有实证才可合入。允许复用 #1079 的必要代码，但不直接覆盖或自合该独立 PR。业务发布 PR 仅限经审核的既有输入对应的 Hello-Docs `docs/publish/**`；不授权新增品类/翻译事实或未批准的源数据变更。不越过线上 Base 写入、workflow、公开 CLI、依赖、schema、参考资产重审批、外部收集服务等独立门禁。 | 「为实现manual运营 做一个计划 以checklist记录，单独一个pr，这个pr的合并条件就是‘实现manual运营的打通’。完成一个 开一个pr 并勾选相应的checklist记录，授权你自合并 你开多个窗口并行去做 注意控制成本 按实现难度去选模型。」2026-09-12 | checklist 全部运营出口验收且总计划 PR 合入，或操作者撤销 | 已失效（操作者 2026-09-25 裁定「MA-066、MA-016、MA-104 算失效」：总计划 #1103 squash `c1ee9992` 已合入，验收清单 35 项全部勾选，其中部分出口为操作者选择暂缓而非验收） |

| ID | 范围 (branch pattern / PR list) | 授予出处 | 失效条件 | 状态 |
| --- | --- | --- | --- | --- |
| MA-001 | 闭环报告工程 ①–⑦ 的 PR（2026-07-02 当日各线） | 「授予你全绿就合入PR的权利 直到闭环报告工程①–⑦完成为止」2026-07-02 | 工程①–⑦完成（当日达成） | 已失效 |
| MA-002 | IDML 参考版式对齐（parity）线：`fix/idml-*`、`feat/idml-*` 分支，及提交题头 `fix(idml)`/`feat(idml)` 的对齐类 PR | 「授权你绿了自合」2026-07-10；2026-07-30 操作者确认仍生效 | 操作者撤销 | 生效 |
| MA-003 | PR #738、#739、#740、#741、#742、#743（2026-07 月度回顾 P0 批次 + 本协议 PR） | 「全绿即合的授权给你 六个PR都合了」2026-07-30 | 六个 PR 全部合入即失效 | 已失效（2026-07-30 六 PR 全部合入） |
| MA-004 | PR #744、#745、#746、#747（2026-07 回顾 P1 第一波，含本登记行所在的 #744） | 「合入吧」2026-07-30（指 P1 第一波四个全绿 PR） | 四个 PR 全部合入即失效 | 已失效（2026-07-30 四 PR 全部合入） |
| MA-005 | PR #748（Workstream W 规模化执行计划登记，含本登记行） | 「合入吧 授权给你」2026-07-30 | #748 合入即失效 | 已失效（2026-07-30 #748 已合入） |
| MA-007 | same-source 门 CI 稳定性修复 PR（fix/same-source-gate-ci-stability，含本登记行；1.7 publish 解阻） | 「合入 重新派发」2026-07-31 | 该 PR 合入即失效 | 已失效（2026-07-31 #813 已合入） |
| MA-009 | same-source 门失配诊断 PR（fix/repin-je1000f-us-after-812，含本登记行） | 「合」2026-07-31 | 该 PR 合入即失效 | 已失效（2026-07-31 #815 已合入） |
| MA-010 | 1.7 publish 解阻链的诊断/修复类 PR（含本登记行所在 PR），按批全绿即合；不越过 F6/契约审批语义 | 「授」2026-07-31 | JE-1000F_US 1.7 publish 成功交付即失效 | 已失效（2026-08-01 run 30683387378 交付 1.7，wiki XhhVw4nDNij5DckM00ocxnlCnlf） |
| MA-011 | 队列/护栏加固批次 PR #856、#857、#858、#859（含本登记行所在 PR）——终态写回所有权、钉钉会话凭据探测、web 表面护栏登记、review 分支同步哨兵；验收发现由本窗口修复后按批全绿即合 | 「你直接优化 这4个pr 然后合入」2026-08-03 | 四个 PR 全部合入即失效 | 已失效（2026-08-03 #856/#857/#858/#859 全部合入） |
| MA-012 | 钉钉交付对接切片 stacked PR #864、#866、#867（含本登记行所在 PR）——交付坐标映射表、outbox 写入器与 `/output/` ignore、队列接线与文档；按依赖序 #864 → #866 → #867 合入。不含语言门（EU 整本按型号裁语言）改动，那是下一批 | 「合入吧 语言门下一批做」2026-08-03 | 三个 PR 全部合入即失效 | 已失效（2026-08-03 #864/#866/#867 全部合入） |
| MA-013 | 能力门批次 PR #865、#872、#873(auto-manual)与 #45(Hello-Docs)——韩文探针补全、删除三条产线不存在功能的小节+拆 AU 模板、按能力裁剪小节机制+收紧记忆恢复规则、法/西「提示」单数收口;按依赖序 #865 → #872 → #873 合入,#871+HD#45 配对合入。不含其余五条 section 规则的改动(待逐条产品事实确认) | 「全绿即合的授权给你 四个PR都合了」2026-08-04 | 四个 PR 全部合入即失效 | 已失效(2026-08-04 #865/#872/#873/#871 与 Hello-Docs#45 全部合入;HD#45 的 check-en 为 PR 环境性红,按 #35/#39 先例并为保 review 面字节一致而合,已在 PR 内留证) |
| MA-014 | 语言门批次 PR #870（EU 整本按型号裁语言，含本登记行所在 PR）——`data/model_languages.csv` 按 (型号,区域) 定语言集合、装配期裁页与数据页、`lang_blocks` 前言块裁剪（含 review overlay 副本）、`LANG_SCOPE_*` 检查门；顺带清 `lang_parity_known_exceptions.csv` 的 JE-1000F/US preface 悬挂例外。**不含**两项待拍板项：`configs/config.eu-uk.yaml` 默认 target 重指向（会牵动 `.github/ci_check_targets_skip_baseline.json`）与语言表做成飞书镜像（改 `文档构建表` schema，按 §3 需审批） | 「你给你授权」2026-08-04（承 MA-012「语言门下一批做」） | #870 合入即失效 | 已失效（2026-08-04 #870 已合入，squash 1a5f408d；18/18 检查全绿、无评审意见） |
| MA-015 | Workstream X 四渲染器 Style Component Contract v2 全部串行 PR：#874（PR 0）及 [`style_component_contract_v2_plan.md`](style_component_contract_v2_plan.md) §4.3 所列 PR 1–9 分支。只允许分支 → PR → 全检查绿 → squash merge；严格按 PR 0→9 顺序，不含直接向 `main` 提交，不越过 workflow、公开 CLI、依赖、Base schema、reference content/assembly reapproval 等独立 gate | 「全部给你啊 授权 你就在分支上做」；「不要再问我了 权限都给你授了 直到这个目标完成」2026-08-08 | §4.3 全部 PR 合入、完成 main/RTD 验收，或操作者撤销 | 已失效（2026-08-08 #874/#889–#897 全部合入；main `c0581038`、Hello-Docs `6cf73fff`、RTD build `33975687` 验收完成） |
| MA-016 | `JE-1000F_US_1.8` Publish 解阻所需修复 PR（含 `fix/publish-review-overlay-repro` 及本登记行），以及 Hello-Docs #49（`backport/JE-1000F-US-publish-1.8` → `review/JE-1000F-US`）的一次性 `check-en` 例外。#49 仅允许已现场证明与本次 9 个 review 文件无关的既有 review 基线环境红（23 个既有资产解析缺失 + 1 个既有 `DUPLICATE_RENDER_TEXT_MISMATCH`）；其余检查必须全部通过，且仍须满足无 changes-requested、无未解决 review thread。仅限 review overlay 可复现性修复与完成该记录消费所必需的后续修复；不越过 F6、workflow、公开 CLI、Base schema 等独立 gate | 「授权你合入 能达到此次目标的所有pr」；「允许 #49 针对此既有 check-en 环境红例外合入」2026-08-10 | `JE-1000F_US_1.8` Publish 成功交付并完成线上表读回验收，或操作者撤销 | 已失效（操作者 2026-09-25 裁定「MA-066、MA-016、MA-104 算失效」：未见 `JE-1000F_US_1.8` Publish 交付与线上表读回的记录，按裁定结束） |
| MA-017 | PR #937（`feat/skeleton-s2-contract-tiering`，含本登记行）——修复合同 category 未识别时静默降级为 `default` 的 fail-open，补回归测试后按全绿协议合入；不扩大到 #938 或后续骨架切片 | 「修复后合入 #937」2026-08-21 | #937 合入即失效 | 已失效(2026-08-21 #937 squash `eeb8eaa8` 进 main。2026-09-01 补记失效——此前一直误留「生效」) |
| MA-018 | PR #943（`fix/review-sync-line-merge`，含本登记行）、#940（骨架切片 S4）、#942（JBP-2000B 四幅插图，base 为 #940 分支，需待 #940 合入自动重定基后再合）——**严格按 #943 → #940 → #942 顺序**。#943 必须先合：`main` 上仍是旧的 merge_params 逻辑，在它合入前对 JE-1000F/US 跑 `sync-review` 或以 review 为源的构建会静默销毁审校文案。**不含** #938、#939、#941、#935 与任何其它骨架切片；不含 `docs/_review` 追平提交（那是 #943 合入后的独立数据提交，需逐文件复核）；不越过 F6、workflow、公开 CLI、Base schema 等独立 gate | 「按你建议的顺序走 1、2、3、4」2026-08-22（承接同轮建议：1=合 #943，2=合 #940 → #942） | 三个 PR 全部合入即失效 | 已失效（2026-08-22 三个 PR 全部合入：#943 squash `80dce0d1`、#940 squash `5e1d0c26`、#942 squash `c8b83f65`；#942 在 #940 合入后按 §8.2 做 `rebase --onto` 去掉重复的 S4 提交，5 个提交无冲突重放后 17/17 绿再合） |
| MA-019 | Milestone M 骨架库**主线**的 PR：#944（加电包进 CI + FAIL 棘轮，含本登记行）、#935（骨架库设计稿）、#941（Milestone M checklist 刷新，base 为 #935 分支）、#938（切片 S3）、#939（切片 S5），以及此后同一主线上的切片 / 四栈验收 / CI 覆盖类 PR。按依赖序合（#935 → #941；其余各自独立）。**不含**：非本主线的任何 PR；切片红线（不碰 `data/layout_params.csv` 与任何已批准的 reference-layout 契约、不扩 `figure_targets` 白名单）；线上源表写入（F6 审批门）；`.github/workflows/**`；公开 CLI 行为；依赖版本；Base schema；以及已挂起的数据面事项（JE-1000F 本地化值对齐、`docs/_review` 追平提交）——这些仍逐项等操作者 | 「授权你在这条主线绿了自合」2026-08-22 | Milestone M 切片 S1–S6 验收完成，或操作者撤销 | 已失效（Milestone M 切片 S1–S6 全部完成，见 `next_optimization_checklist.md` §6k；S6 于 2026-08-30 按操作者裁定收口） |
| MA-020 | JE-3000C_KR 回写轮收尾的三个 PR,**严格按 Hello-Docs#65 → Hello-Docs#64 → auto-manual#956 顺序**:#65(`backport/JE-3000C-KR-r2` → `review/JE-3000C-KR`,云文档回写 + 商品名改 `리튬이차전지시스템` + 助词收音修正)、#64(`review/JE-3000C-KR` → main,Start Review 主 PR)、#956(`fix/korean-josa-wa-pair`,含本登记行)。#65 必须先合,否则 #64 带不上本轮回写。**不含**:清单/状态类文档 PR(按既往反馈这类始终由操作者合);本轮尚未执行的收尾步骤(rediff 归零、基线重快照、构建表行指针、sync-data 重建)——那些仍逐项等操作者;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「你帮我依次合入 (确保全绿的情况下)」2026-08-26 | 三个 PR 全部合入即失效 | 已失效(2026-08-28 三个 PR 按序全部合入:Hello-Docs#65 squash `0e2adfda`、Hello-Docs#64 squash `13e120bf`、auto-manual#956 squash `5db6f6f9`;各自在 head SHA 上 17/17 检查全 success、CLEAN、无未解决评审线程) |
| MA-021 | IDML 三目标最终装配收口批次：授权登记分支 `chore/authorize-idml-pr-batch`，以及 PR #969、#970、#968、#971、#967。严格按 #969 → #970 → #968 → #971 → #967 顺序；前三项合入后 #971 必须更新到最新 `main`，重跑三目标构建、完整 CI 和 Mac InDesign native finalize，全部通过后方可合入；#967 必须在 #971 后更新 URL inventory，并确认 QR 物料号 `16-0102-000341` 与 14 行 `待无字化=FALSE` 口径后方可合入。不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「帮我执行」2026-08-30（承接同轮对 #967–#971 的逐项审查与合入顺序） | 五个 PR 按条件全部处理完成，或操作者撤销 | 已失效(2026-08-30/31 五个 PR 按序全部合入:#969 squash `ab05c838`、#970 squash `b8572482`、#968 squash `f017256f`、#971 squash `c1d75d62`、#967 squash `83833f20`,均进 main;#971 与 #967 的附加条件由操作者在合入时确认。2026-09-01 补记失效——此前一直误留「生效」) |
| MA-022 | PR #975（`docs/milestone-m-r0-coverage-rebaseline`，含本登记行）——仅限 R0 覆盖率重算、`JBP-2000B_EU` 身份与母版裁决证据；必须满足 17/17 检查全绿、无 changes-requested、无未解决 review thread 后 squash merge。**不含** #974；不授权提前执行 R1a/R1b 或写入规格、占位、文案、资产等后续线上数据 | 「授权你绿了合入」2026-08-30 | #975 合入即失效 | 已失效(2026-08-30 #975 squash `0ed1871d` 进 main。2026-09-01 补记失效——此前一直误留「生效」) |
| MA-023 | PR #974 的 post-S6 R0–R6 checklist 主线：PR #976，以及此后明确交付 `code-as-doc/next_optimization_checklist.md` 中 R1b → R2 → R3a → R3b → R3c → R4a → R4b → R5a–R5f → R6 的目标中立前置、实施、证据和跟踪 PR；严格按清单依赖顺序，每个 PR 均须全部检查成功、无 changes-requested、无未解决 review thread 后 squash merge。#974 仅在 R0–R6 与 Milestone M 退出条件全部勾完后才进入 Ready 并合入。授权不允许跳过 target/source/legal/asset gate，不替代线上源表或资产表的逐批 operator gate/readback，不越过 `.github/workflows/**`、公开 CLI、依赖、Base schema、approved reference-layout 晋升等独立审批边界 | 「授权 #976 全绿合入」；「这条checklist上的pr 都授权绿了合入」2026-08-30 | #974 与其 R0–R6 checklist 全部完成并合入，或操作者撤销 | 生效 |
| MA-024 | PR #979（`fix/idml-troubleshooting-semantic-recognition`,含本登记行）——故障排除表改由声明语义路由、删除 EN/FR/ES 表头文案集合,并修掉随之暴露的标定表下标 `KeyError`、四处表头反推语言、以及谚文回退与语义 `FontStyle` 的重复属性。**本授权明确覆盖它带来的渲染变化**:ja / zh / de / it / uk / pt-BR / ko 七种语言的故障排除表都从旧版方表回到共享圆角组件——这正是让实现追上 `manual_style.yaml` 中 `HB-TABLE-TROUBLESHOOTING` 「全语言、无方表变体」的既有声明。**附带条件(非阻塞)**:除 ko 外这六种语言尚无实测行高,借英文 minima + `AutoGrow` 兜底,**JP/CN/EU/pt-BR 各线下次出版前需要一轮视觉复核**并按需补各自的 `lang_<code>_idml_trouble_row_minima`。须全部检查成功、无 changes-requested、无未解决 review thread 后 squash merge。**不含**后续「行基线改数据驱动」那个改动(独立 PR,单独等操作者);不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「合入让实现追上契约 然后把行基线也改成数据驱动」2026-08-30(承接同轮就 JP/CN 渲染变化范围的二选一提问) | #979 合入即失效 | 已失效(2026-08-30 #979 squash `7bed2a4a` 进 main;18/18 检查全绿、CLEAN。2026-09-01 补记失效——此前一直误留「生效」) |
| MA-025 | BP JP 产线 CI 解阻批次,**严格按 #988 → #985 顺序**:#988(`fix/gitleaks-record-id-allowlist`,含本登记行,进 main——把飞书 `record_id` 业务键加入 secret-scan 白名单,附负对照)、#985(`fix/bp-jp-layout-token-prefix` → **base 是 `feat/bp-jp-target-onboarding`,不是 main**——七行读不到的 `lang_jp_*` 改成规范前缀 `lang_ja_*`、修掉两处改动 JE-1000F US 字节的共享 TOC 改动、修正分支自带的失败测试)。#988 必须先合:#985 的 secret-scan 红就是它修的那一条,且 `.gitleaks.toml` 要经 main 进入该分支才不会与后续 main 合并冲突。**不含**:`feat/bp-jp-target-onboarding` 本身进 main(仍未开 PR、仍有未裁决发现——JP 字体只接通 4 个 story 汇聚点中的 2 个、新 JP 页未声明故障排除语义);**不含 #987**(版式令牌闸门)——它进 main 后会拒绝该分支现存的 `lang_jp_*` 行,必须等 #985 合入后再合,否则 BP JP 构建直接失败;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「你先把 #985 合进 BP JP 分支让它能过 CI(配合 #988 解掉 secret-scan) 然后 继续基于 PR #974 去做」2026-09-01 | 两个 PR 全部合入即失效 | 已失效(2026-09-01 两个 PR 按序全部合入:#988 squash `9aeade22` 进 main、#985 squash `bbde6ab4` 进 `feat/bp-jp-target-onboarding`;各自在 head SHA 上 CLEAN、非成功检查 0、无未解决评审线程。#985 合前先把 main 合入其 head,让 `.gitleaks.toml` 经 main 进入该分支而不是重复改同一行) |
| MA-026 | **严格按 #990 → #989 顺序**:#990(`docs/close-ma-025`,含本登记行,进 main——关闭 MA-025 并记上两个 squash SHA)、#989(`fix/bp-jp-data-story-font-sinks` → **base 是 `feat/bp-jp-target-onboarding`,不是 main**——把 `add_trouble_story` / `add_spec_story` 接回共享 story 汇聚点 `add_story_parts`,让 JP 规格页与故障排除页不再落在 `Arial Unicode MS` 宿主系统字体上;`composed_ja` golden 主动重基线 14 行,逐行确认全为 CJK 字体替换,非 ja 三个变体逐字节不变)。#990 先合,让授权与 MA-025 的关闭同时落在 main 上。**不含**:`feat/bp-jp-target-onboarding` 本身进 main(仍未开 PR、R3c 审批门未完成——资产入库审批与法务/保修确认仍缺、原生 InDesign 视觉轮未跑);**不含 #987**(版式令牌闸门,仍须等 BP JP 整条进 main 之后);不含新 JP 页是否声明故障排除语义那项待裁决改动;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合入 #989 和 #990」2026-09-01 | 两个 PR 全部合入即失效 | 已失效(2026-09-01 两个 PR 按序全部合入:#990 squash `3da016da` 进 main、#989 squash `ce550021` 进 `feat/bp-jp-target-onboarding`;各自在 head SHA 上 CLEAN、非成功检查 0) |
| MA-006 | Workstream W 切片 PR（交付 scaling_execution_plan.md §4 条目、PR body 标注 Workstream W/Milestone L 的 PR），**按批全绿即合**；但计划 §5 的 gated 条目（workflow 变更、公开 CLI 行为、队列并发语义、strict 翻转、F6 seed 等）必须先有对应拍板记录才可合，本授权不越过任何 gate；stacked PR 按依赖序合 | 「W切片的PR授权也一并给你 按批全绿即合」2026-07-30 | Workstream W 收官或操作者撤销 | 已失效（最终逐项状态与证据收官变更合入时） |
| MA-027 | 版式令牌语言前缀收口的两个 PR：#987（`feat/layout-token-language-gate`，**含本登记行**，进 main——版式令牌语言行闸门，本轮已把它建立在错误前提上的判据改正）、#992（`fix/bp-jp-revert-prefix-rename` → **base 是 `feat/bp-jp-target-onboarding`，不是 main**——把 #985 误改的七行 `lang_ja_*` 还原为 `lang_jp_*`，并补 `tests/test_layout_language_row_reachability.py` 可达性护栏含负对照）。两者 base 不同、互不依赖，任一顺序均可；建议先 #987，让本登记行与闸门同时落 main。**本授权显式取代 MA-025 与 MA-026 中「不含 #987」那条排除**：该排除的理由是「#987 进 main 后会拒绝 BP JP 分支现存的 `lang_jp_*` 行」，而这个前提本身就是同一个错误认知——`tools/idml/loaders.py::normalize_lang` **故意**把规范码 `ja` 映射成 `jp`（注释：`jp` 是历史 phase2 列后缀），IDML 页面路径的前缀正是从它的输出拼的，所以 `lang_jp_` 是**可达的正确拼写**，#985 那次改名反而让七行读不到、inbox 卡片从实测 145.0pt 静默回落到基线 108.0pt。改正后的闸门只拒「没有任何路径会拼出的拼写」（数据列别名 `ukr`/`br`、拼错的 `xx`），已逐项实测接受 main、`je3000c-kr` overlay 与 BP JP 分支三处 CSV（含全部七行 `lang_jp_`）。**不含**：`feat/bp-jp-target-onboarding` 本身进 main（R3c 审批门未完成——资产入库审批与法务/保修确认仍缺、原生 InDesign 视觉轮未跑）；#993 与 #991 等清单/状态/登记类文档 PR（按既往反馈这类始终由操作者合）；新 JP 页是否声明故障排除语义那项待裁决改动；本机 `FEISHU_PHASE2_MODEL_CAPABILITIES_TABLE_ID` 指向队列表那个环境缺陷的修复（不在 repo 内）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「合入 #992 和 #987」2026-09-01 | 两个 PR 全部合入即失效 | 已失效(2026-09-01 两个 PR 全部合入:#987 squash `46888c25` 进 main、#992 squash `03fc8f51` 进 `feat/bp-jp-target-onboarding`;各自在 head SHA 上检查全 success(#987 17/17、#992 16/16——差的那个 `package-review-preview` 经核对 `review-preview.yml` 的 paths 过滤,#992 的两个文件不匹配任何触发路径,属正确未触发而非漏跑)、无 changes-requested、review thread 数为 0、CLEAN。合后已实测模拟 BP JP 分支与 main 的合并态:仅 `merge_authorizations.md` 冲突(登记表本身),版式 CSV 与闸门代码无冲突,合并态下闸门接受全部 7 行 `lang_jp_`,套件 3343 测试通过——即 MA-025 当初担心的那个未来事件已实证不成立) |
| MA-028 | 本轮两个待合文档 PR:#991(`docs/close-consumed-authorizations`,**含本登记行**,进 main——登记表维护,关闭六条已消费授权 MA-021/022/024/026/017/027 并逐条记上核实过的 squash SHA)、#993(`docs/bp-jp-phase5-evidence` → **base 是 `feat/bp-jp-target-onboarding`,不是 main**——BP JP Phase 5 收官证据:真实数据四渲染器对账、跨产线回归、以及两个本机环境缺陷的记录)。两者 base 不同、互不依赖,任一顺序均可;建议先 #991,让本登记行与六条关闭同时落 main。**本条是对既有惯例的一次具名例外,不是惯例本身的撤销**:按 MA-020 的备注与既往反馈(#941 自合后被要求撤回),清单/状态/登记类文档 PR **默认始终由操作者合**;我在上一轮也据此明说了不合这两个。操作者随后按编号明确指示「合入 #991 和 #993」,故按登记表规则先记录再执行。默认惯例继续有效,后续同类 PR 仍需操作者逐个指名,不得援引本条类推。**遗留**:本行自身的失效需在下一次触碰本文件时补记(与 MA-027 同样的收尾节奏)。**不含**:`feat/bp-jp-target-onboarding` 本身进 main(R3c 审批门未完成——资产入库审批与法务/保修确认仍缺、原生 InDesign 视觉轮与 12 页视觉台账未跑);MA-019 是否按自身条款失效那项裁决;新 JP 页是否声明故障排除语义那项待裁决改动;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「合入 #991 和 #993」2026-09-01 | 两个 PR 全部合入即失效 | 已失效(2026-09-01 两个 PR 全部合入:#991 squash `2928ed10` 进 main、#993 squash `96ce7ac4` 进 `feat/bp-jp-target-onboarding`;各自在 head SHA 上 17/17 检查全 success、无 changes-requested、review thread 数为 0、CLEAN。**合前按核验结论各修一处**:#991 标题原写「close five」而实际关闭六行并新增 MA-028——仓库是 squash 合并,该标题会成为 main 的永久提交信息,已改为「close six consumed authorizations and record MA-028」后再合;#993 原先挂在 #992 之前的 base 上,树与其自身证据不一致,已 rebase 到含 #992 的 base 并在文档内补记测量出处(实测构建产物中卡片 145.0pt、内容框 119.0pt,无 108.0/82.0 回退框,证明证据取自修复后的树)) |
| MA-029 | PR #994(`docs/close-ma-028`,**含本登记行**,进 main——把已消费的 MA-028 翻成已失效并记上 #991 `2928ed10` / #993 `96ce7ac4` 两个 squash SHA)。须全部检查成功、无 changes-requested、无未解决 review thread 后 squash merge。**关于本行自身的收尾——这里定一条规矩,终止我造出来的递归**:授权行的关闭属于账本卫生,按本文件「下一次触碰时翻状态」的既有规则处理即可,**不得为关闭某一行而单开 PR**。我在 MA-027 → #991、MA-028 → #994 连开了两个只为关闭上一行的 PR,每个又需要新授权,于是每合一次就多欠一次——这是我制造的跑步机,不是规则要求的。故本行的失效由**下一次因实质原因触碰本文件**的改动顺手补记;在那之前它是一条只指向 #994(且 #994 已合)的已消费行,无法被复用于任何其它 PR,风险与 MA-020 那种宽口径悬挂授权不同。**不含**:任何其它 PR;`feat/bp-jp-target-onboarding` 进 main;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「合入 #994」2026-09-01 | #994 合入即失效(状态由下一次实质性触碰本文件时补记,不单开 PR) | 已失效(2026-09-01 #994 squash `cacc915a` 进 main;17/17 检查全 success、CLEAN、review thread 0。本行的关闭正是按它自己定的规矩执行的:由下一次因实质原因触碰本文件的改动(本次新增 MA-031)顺手补记,未单开 PR) |
| MA-030 | 日文 IDML 字重收口三个 PR:#997(`fix/idml-cjk-bold-preserved` → **base 是 `feat/bp-jp-target-onboarding`**——`_style_range` 的 fallback 分支硬编码 Regular 且以 `elif` 挡在加粗前面,导致每个 CJK run 的加粗被丢弃;新增 `family_declares_style` 门控,落地时零行为变化、IDML 内容哈希 `6d8b6f71` 前后一致)、#998(`feat/idml-jp-font-weights`,base 为 #997 分支——从清单记录的同一可变源实例化 DemiLight/Medium/Bold 三个字面,配方经 `wght=400` 自校验与已提交 Regular 的 name 表零差异;并修正门控原本查通用 CJK 令牌而非日文家族的问题,改为在知道语言的汇聚点裁决;结果 21 处日文 run 恢复加粗)、以及随后的**段落样式字重映射 PR**(按出货书实测的组件级映射给相关段落样式声明字重)。**字重取值口径由操作者定为「按主导的来」**:证据混合的样式取占比最高的那个字重,不再逐条等裁决——映射与各自占比记录在 PR 与 `code-as-doc/reviews/bp_jp_reference_vs_built_2026-09.md`。合前须全部检查成功、无 changes-requested、无未解决 review thread。**影响面须逐项证明**:四个 IDML golden 中仅 `composed_ja` 变化,`composed`/`composed_fr`/`composed_ko` 逐字节不变;字体家族稳定性测试中 `styles`/`fonts`/`fonts_ko`/`flow` 哈希不变。**不含**:`feat/bp-jp-target-onboarding` 本身进 main(R3c 审批门未完成);字号(PointSize)的任何改动——本轮只动字重;清单/状态类文档 PR;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate。**遗留**:本行失效按 MA-029 的规矩,由下一次实质性触碰本文件时补记,不单开 PR | 「授权你合入，字体样式按主导的来」2026-09-01 | 三个 PR 全部合入即失效(状态由下一次实质性触碰本文件时补记) | 已失效(2026-09-01 三个 PR 全部合入:#997 squash `00a91e19`、#999 squash `35caeeff`、#1000 squash `dbfa84ef`,均进 `feat/bp-jp-target-onboarding`;各自在 head SHA 上 17/17 检查全 success、CLEAN、review thread 0。**过程与本行原文的一处差异**:原文写的第二个 PR 是 #998,但合 #997 时删除其 head 分支导致 GitHub 自动关闭了 #998,同内容以 #999 重开(先用 `rebase --onto` 去掉被 squash 带走的重复提交),范围未变) |
| MA-031 | BP@JP R3c 的两个文档 PR 加本登记 PR:#995(`docs/bp-jp-capabilities-env-resolved`——把 Phase 5 里记成「待修、在 repo 外」的能力表 env 缺陷改为已解决,并写入正确表 id `tbltnkDIdwiDOP7d` 与 10/10 对 0/10 的判别方法)、#996(`docs/bp-jp-phase6-pipeline-evidence`——退役 Phase 6 三个陈旧的 zero、记录管线侧机械验收表与 Gilroy 字体线索,并新增 `code-as-doc/reviews/bp_jp_reference_vs_built_2026-09.md` 逐页台账)、以及**本登记行所在的 PR**(进 main:记录本授权,并补记 MA-029、MA-030 的失效)。两个文档 PR 的 base 均为 `feat/bp-jp-target-onboarding`,改同一文件的不同小节,任意顺序合入。**本条与 MA-028 同属具名例外**:清单/状态/登记类文档 PR 默认始终由操作者合(见 MA-020 备注与 #941 前例),操作者按编号明确指示后才由我执行;默认惯例继续有效,后续同类 PR 仍需逐个指名。**不含**:`feat/bp-jp-target-onboarding` 本身进 main(R3c 审批门未完成——原生 InDesign 视觉验收未过、资产与法务审批仍缺);不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate。**遗留**:本行失效按 MA-029 的规矩由下一次实质性触碰本文件时补记,不单开 PR | 「你执行：#995、#996 两个文档 PR，加上刚才那个 MA-030 登记分支」2026-09-01 | 三个 PR 全部合入即失效(状态由下一次实质性触碰本文件时补记) | 已失效(2026-09-04 三个 PR 全部合入并逐个用 `gh pr view` 核实:#995 squash `724514ec`、#996 squash `581401d7` 进 `feat/bp-jp-target-onboarding`,#1001「record MA-031 and close MA-029 and MA-030」squash `2178a58a` 进 main。本行的关闭正是按 MA-029 的规矩执行的:由下一次因实质原因触碰本文件的改动(本次新增 MA-032)顺手补记,未单开 PR) |

| MA-032 | 本轮三个 PR,**严格按 #1024 → #1022 → #1023 顺序**:#1024(`chore/ignore-runtime-artifacts`,**含本登记行**,进 main——给四类运行期产物补 ignore 规则:`sync-data` 的快照写锁 `.*.snapshot.lock`(留在盘上且落在导出根**旁边**,`data/phase2/*` 管不到)、`build.py check` 暂存的 `docs/_review/<target>/renderers/`、以及根目录手工解包的 `*_handoff.zip` 与同名目录)、#1022(`feat/category-tag-axis` → **base 是 `feat/bp-jp-target-onboarding`,不是 main**——把 `category` 轴接到**两条渲染面**:Sphinx 的 `-t category_<值>` 与 manual IR 的 `category_*` base tag,取值来自 `build.skeleton_family`,由 `tools/page_contracts.py::resolve_category` 单点解析(`check_docs_contracts` 也改为调它,不再各自推导默认值)。**两条链必须同一个 PR**:`.. only::` 对未命中的标签是**静默省略**,只接一条会让同一载体在 PDF 里印出、在 IDML 里消失且无任何报错)、#1023(`fix/assembly-plan-page-mismatch` → **base 同为 `feat/bp-jp-target-onboarding`**——冻结装配计划与实际 bundle 页数不符时改为报出**具体页名**(双向:计划有而 bundle 无 / bundle 有而计划无),此前真诊断在上一屏已算出却被丢掉,最终以 `_validate_flow_splits` 的 `strict=True` zip 抛出 `zip() argument 2 is shorter than argument 1`;并新增每个已配置计划的「计划页是否都被 manifest 声明」静态棘轮)。**#1024 必须先合**,让本授权与 ignore 规则同时落在 main 上;**#1022 合入后 #1023 必须先更新到新 base 再合**——两者都往 `code-as-doc/build_doc_guide.md` 的不同小节加行,squash 后会脱钩,须重跑全绿方可合。**落地影响须逐项证明**:#1024 与 #1022 均为对现有产物**零变化**——JE-1000F/US、JE-3000C/KR、JBP-2000B/JP 三个包按 zip 成员内容逐一比对(735/181/107 个成员,changed 均为 0);#1024 另需 `git ls-files | git check-ignore --stdin` 全索引扫出 **0 个已跟踪文件被遮住**,且 `overrides/renderers/`(AGENTS §3 留给 review 覆盖的路径)经实测未被忽略。**边界声明**:#1022 给 `tools/idml/export_cli.py` 与 `tools/manual_ir_cli.py` 各加了一个可选 `--category`,并给 `build_manual_ir` 等函数加了带默认值的 keyword-only 参数——`build.py` 自身**未新增或改动任何公开 flag**,现有调用方零改动,故不触发 §8.7 的公开 CLI 门;若操作者认为这仍属公开面变更,请驳回本行。**不含**:`feat/bp-jp-target-onboarding` 本身进 main(R3c 审批门未完成——资产入库审批与法务/保修确认仍缺、原生 InDesign 视觉轮未跑);#1021(欧规书 en/fr/es 保修载体,operator 已明示搁置,仍为 draft);JE-3000C_KR 那两页封面/封底的根因修复(`manual_kr.yaml` 被三个 KR 目标共用且 manifest 页无按型号门,需给另两个型号备封面资产或按型号拆 manifest——产品决策且涉线上资产表写入);法语保修 §4/§5 正文错位(待操作者/法务裁决);JBP-2000B/EU 缺的 `Spec_Master.csv` 行(F6 审批门);清单/状态类文档 PR(按 MA-020 备注与 #941 前例,这类默认始终由操作者合);不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate。**遗留**:本行失效按 MA-029 的规矩,由下一次实质性触碰本文件时补记,不单开 PR | 「你     合」2026-09-04(承接同轮我对三个 PR 全绿状态与「无生效授权覆盖」的报告) | 三个 PR 全部合入即失效(状态由下一次实质性触碰本文件时补记) | 已失效(2026-09-04 三个 PR 按序全部合入并逐个用 `gh pr view` 核实:#1024 squash `be5244ac` 进 main、#1022 squash `13e5eb47`、#1023 squash `cd5a020a` 进 `feat/bp-jp-target-onboarding`;各自在 head SHA 上检查全 success、CLEAN、review thread 0。#1023 在 #1022 合入后按条款 rebase 到新 base 并重跑 17/17 才合。按 MA-029 的规矩由下一次实质性触碰本文件的改动(本次 main 对齐合并)顺手补记,未单开 PR) |
| MA-033 | JP 验收收尾的两个 PR，严格按 #1027 → #1028 顺序合入 `feat/bp-jp-target-onboarding`，含本登记行。先将 #1028 的 base 移至 JP 功能分支以保留依赖 PR；#1027 合入后，#1028 必须对齐更新后的 base，确认只含冻结验收准备的自身改动，并在最终 head 上全检查成功、无 changes-requested、无未解决评审讨论后合入。随后协助设计机冻结包原生验收和十二页记录；JP 分支只处理验收所需修复。验收完成后准备功能分支进入 main 的总 PR，再从统一基线开启公共 IR、Web、数据读取规则三项工作。此行的直接合并授权仅覆盖 #1027/#1028；总 PR 在此阶段为准备及评审，不以流水线通过代替视觉验收。 | 「先处理 #1027、#1028 的依赖并依次合入，然后协助我在设计机完成冻结包的原生验收和十二页记录。JP 分支只处理验收所需修复；验收完成后准备进入 main 的总 PR，再从统一基线开启公共 IR、Web、数据读取规则三项工作。」2026-09-04 | #1027/#1028 依序合入即失效；按 MA-029 由下次实质性触碰补记 | 已失效（2026-09-04 按序合入：#1027 `fe25a8f8`、#1028 `2c6315ff`，均进 JP 功能分支；最终 head 各 17/17 成功。随本次实质性原生验收修复补记，未单开关闭 PR） |

| MA-034 | PR #1029（JP 候选产线及原生验收总 PR，含本登记行）进入 main。将 Draft 转为 Ready，最终 head 全部检查成功、无 changes-requested、无未解决讨论且对齐 main 后 squash merge。保留验收台账 D1–D4，不以代码合入声明产品事实或发布审批已完成。合入后以该 main 提交为统一基线，开启公共 IR、Web、数据读取规则三项独立工作；后续 PR 的合并不由本条扩张授权。 | 「合入总pr 然后 继续 三项后续工作」2026-09-04 | #1029 合入即失效；按 MA-029 在下次实质性触碰时补记，不另开关闭 PR | 已失效（2026-09-04 #1029 squash `24a10e9f` 进 main；最终 head 18/18 成功、CLEAN、无评审讨论。随本次三项合入授权补记） |

| MA-035 | 公共 IR #1031、数据读取 #1032、Web #1030 三个首批迁移 PR 进入 main，含本登记行。按 #1031 → #1032 → #1030 依次 squash merge；每次合入后，剩余 PR 对齐最新 main，验证组合兼容性，在最终 head 全部检查成功、无 changes-requested、无未解决评审讨论后方可合入。保留 JP 原生验收 D1–D4 和各 PR 已列出的范围外债务。不扩大到后续细化 PR、生产发布、线上数据写入或其他独立审批事项。 | 「各自都完成了 先合入吧」及公共 IR、Web、数据读取三项清单，2026-09-04 | 三个 PR 全部合入即失效；按 MA-029 在下次实质性触碰时补记，不另开关闭 PR | 已失效（2026-09-04 #1031 `3ca4dfb3`、#1032 `627ac716`、#1030 `d41ecaf7` 已按序合入 main；随本次第二批合入授权补记，未单开关闭 PR） |

| MA-036 | 第二批公共 IR #1033、数据读取 #1034、Web #1035 进入 main，含本登记行。按 #1033 → #1034 → #1035 依次 squash merge；每次合入后剩余 PR 对齐最新 main，最终 head 全部检查成功、无 changes-requested、无未解决评审讨论后合入。随后从统一 main 基线另开修复分支，处理 JE-1000F/JP Web 包转 IR 的警告表识别失败与 document-profile Markdown 的装箱清单提示表失败。本条直接合入授权仅覆盖三个第二批 PR；后续修复 PR 交付评审，不扩张合入授权。保留 JP 原生验收 D1–D4，不涉及生产发布、线上数据写入或其他独立审批事项。 | 「先完成第二批合入，再优先修复两条可复现的 JP 构建失败路径」2026-09-04 | #1033/#1034/#1035 全部合入即失效；按 MA-029 在下次实质性触碰时补记，不另开关闭 PR | 已失效（2026-09-04 #1033 `6af9c566`、#1034 `04024916`、#1035 `71989197` 已依序合入；最终 head 各 17/17 成功。随 #1037 合入授权补记） |

| MA-037 | PR #1037（JP 定义表与纯文字装箱清单构建修复，含本登记行）进入 main。在最终 head 全检查成功、无 changes-requested、无未解决讨论且对齐 main 后 squash merge。随后从合入后的 main 构建 JP IDML 本地查看包，保留构建来源与解析/原生验收债务记录；不以预览包声明生产发布或原生验收完成。 | 「合入 构建个JP idml文件包给我看看」2026-09-04 | #1037 合入即失效；按 MA-029 在下次实质性触碰时补记，不另开关闭 PR | 已失效(2026-09-05 #1037 squash `f36711bf` 进 main;17/17 检查全 success、CLEAN。授权里「构建个 JP idml 文件包」那半句也已执行:从合入后的 main 构建 JBP-2000B/JP 得 107 成员 / 12 跨页 / 88 story,并借此证明 #1037 对该书零影响——在它前后各构建一次逐成员比对,changed 0。按 MA-029 的规矩由下一次实质性触碰本文件的改动(本次新增 MA-039)顺手补记,未单开 PR) |
| MA-038 | Hello-Docs #67（`backport/je2000e-kr-align-20260905` → `review/JE-2000E-KR`）——JE-2000E KR 评审云文档回写的评审页文本级落地（06 UPS `본 제품은`、11 `제외 사항`、12 `App`+按钮标签 POWER/AC1/AC2）。data-plane 单 PR、操作者逐条直接授权；**不含**源表 Class D F6 线上写入（48 条无 record_id、压倒性为陈旧基线格式噪音，待 CI 快照解析出 record_id 后逐条审批）、图片/07 电池复合改动/비상충전 标题层级等延后项、`.github/workflows/**`、公开 CLI、Base schema 等独立 gate。 | 「先合 #67」2026-09-04 | #67 合入即失效 | 已失效（2026-09-04 #67 squash `0811decd` 合入 `review/JE-2000E-KR`；17/17 检查全 success、mergeable=CLEAN、无 changes-requested、无未解决 review thread。**事后补记**：合入时依操作者直接指示 + gate-on-green 执行，登记留痕滞后一步，按 MA-029 收尾节奏于本次触碰本文件时补正） |
| MA-039 | 本轮两个进 main 的文档 PR:#1036(`chore/register-ma017-je2000e-kr-backport-auth`,**含本登记行**——把 Hello-Docs #67 的 JE-2000E-KR 回写授权从撞车的 MA-037 改为 MA-038,并合并 main 解掉登记表冲突)、#1038(`docs/portable-freeze-digest`——JP 冻结指纹改为可移植并说明 R1 已被 R3 取代)。两者改的文件不重叠,任意顺序;建议先 #1036,让本登记行与编号修正同时落 main。**#1036 的两处过程约束已执行并留证**:该分支正被 `auto-manual-worktrees/ma017-je2000e-kr` 这个 worktree 占用,故用 merge 而非 rebase、推送为快进(原 tip `2bd581d8` 是新提交的祖先),不在他人脚下改写历史;PR 标题已从 MA-037 同步为 MA-038(仓库 squash 合并,旧标题会永久写进 main)。**#1038 的两个数字均为把文档内嵌代码原样运行所得**,可移植性为实测(改写包内 checkout 前缀后可移植摘要不变、原始摘要变),非断言。**不含**:#1036 所登记的 Hello-Docs #67 本身(已于 2026-09-04 合入 `review/JE-2000E-KR`,本行只是补记);JE-3000C_KR 封面/封底根因(需给另两个 KR 型号备封面资产或按型号拆 manifest,属产品决策且涉线上资产表写入);任何其它 PR;不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate。**遗留**:本行失效按 MA-029 的规矩由下一次实质性触碰本文件时补记,不单开 PR | 「合入：#1036、#1038」2026-09-05 | 两个 PR 全部合入即失效(状态由下一次实质性触碰本文件时补记) | 已失效（2026-09-05 #1036 `38d83aba`、#1038 `8826495c` 已合入；本次新增 MA-040 时补记） |
| MA-040 | 第三批公共 IR #1039、Web #1040、数据读取 #1041 进入 main，含本登记行。依次 squash merge；每次合入后剩余 PR 对齐最新 main，最终 head 全部检查成功、无 changes-requested 或未解决讨论后合入。随后从统一 main 开独立修复分支，处理 JE-1000F/JP 原生操作说明与规格表溢出并重新构建验收。本条合入授权仅覆盖上述三个 PR；后续版式修复提交评审，保留其他独立审批边界。 | 「合入三项，再集中修复 JP 版式溢出。」2026-09-05 | 三个 PR 全部合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1039 `951e2210`、#1040 `4d91d96b`、#1041 `f49b394a` 已依序合入；本次新增 MA-054 时补记） |
| MA-041 | auto-manual #1042（`fix/kr-shared-copy-app-consumer-productname`，含本登记行，进 main）——KR `page_shared/ko` 共享模板家族级措辞回写(06_ups_mode UPS 전환 문장 → 본 제품은;11_warranty 표준 보증 기간 셀 → 본 제품의 + 该单元격仅去 소비자),源自 JE-2000E_KR 评审云文档回写(基准钉钉 JE-2000E_KR_ko-KR_0.2)。合入触发 sync-hello-docs 下发镜像后重播种 JE-2000E_KR。**不含**全文 본 제품 通扫、不越过 F6 线上源表写入/`.github/workflows/**`/公开 CLI/依赖/Base schema/approved reference-layout 晋升等独立 gate。 | 「授权你 gate-on-green 合 #1042」2026-09-05 | #1042 合入即失效;下次实质性触碰本文件时补记状态 | 已失效（2026-09-05 #1042 squash `f62a39b8` 进 main） |
| MA-054 | 本轮三个进 main 的 PR，按 #1044 → #1043 → #1057 依次 squash merge：#1044（KR `본 제품` 通扫补齐剩余 4 处正文占位符，含本登记行）、#1043（JE-1000F/JP IDML 内容、插图与页码修复）、#1057（从公共 document IR 重播完整 Web 手册）。每次合入后剩余 PR 对齐最新 main；最终 head 的每项检查必须成功，且无 changes-requested 或未解决评审讨论。允许把 #1043 从 Draft 转为 Ready 并只解决合入主线所需冲突；不把其截图验收债务误报为已完成。三项合入后，从统一 main 实际盘点并验证当前可构建的网页文档数量。**不含**线上发布、F6 线上源表写入、`.github/workflows/**`、依赖版本、Base schema 或 approved reference-layout 晋升等独立 gate。 | 「把这3pr 合入了 我要看看 现在 具备构建 几个网页文档 的能力」2026-09-05 | 三个 PR 全部合入并完成网页构建能力盘点即失效；下次实质性触碰补记状态 | 已失效（2026-09-06 #1044/#1043/#1057 已合入并完成网页构建能力盘点；本次新增 MA-055 时补记） |
| MA-055 | PR #1058（`fix/web-component-asset-coverage`，含本登记行）进入 main：共享 Warranty 数字组件、整本 IR 插图覆盖报告、JE-1000F/EU 通过目标实例继承复用 US Overview 几何，并让 Overview/Operation/Charging 使用本地化带字整图，LCD 保持硬件图加 HTML 表格。只在最终 head 的每项检查全部成功、无 changes-requested、无未解决评审讨论且 mergeable=CLEAN 后 squash merge。**不含**隔离预览目录 `tmp/`、正式资产注册、飞书写入、线上发布、F6、`.github/workflows/**`、依赖版本、Base schema 或 approved reference-layout 晋升。 | 「先合入 PR #1058」2026-09-05 | #1058 合入即失效；下次实质性触碰补记状态 | 已失效（2026-09-06 #1058 squash `bc31fec0` 进 main；本次新增 MA-056 时补记） |
| MA-056 | 跨渲染器 IR 完整七刀目标的全部串行 PR，含本登记行所在 #1059（第 1 刀 `feat/manual-ir-v2-neutral-flow`）及随后第 2–7 刀：现有五类 ComponentSpec 嵌入整本 IR；Operation/Warranty/LCD Mode；LCD Icons/Troubleshooting/Symbols；App/Reference Figures 与 carrier/locale/hash；presentation 的 shared base → skeleton profile → target overlay；反型号专属 Python/CSS 硬闸门、代表语料四端验收和旧 DOM 路径退役。每刀必须从前一刀合入后的最新 main 建独立分支/PR，最终 head 的每项检查全部成功、无 changes-requested、无未解决评审讨论且 mergeable=CLEAN 后方可 squash merge；不得以堆叠 PR、兼容空壳或缩小语料/渲染器范围替代完整目标。授权明确覆盖第 7 刀按验收证明退役已被替代的旧 DOM 识别代码；**不含**线上发布、F6/线上 Base 写入、`.github/workflows/**`、依赖版本、Base schema、approved reference-layout/冻结插图内容重新审批或与七刀无关的改动。 | 「授权你整个目标完成 都可以 合入」2026-09-05 | 七刀全部合入 main，并完成代表语料四端、Web 桌面/移动视觉与冷重放最终验收，或操作者撤销 | 已失效（2026-09-06 #1059/#1060/#1061/#1062/#1067/#1069/#1070 全部合入；四代表包 401 个 ComponentSpec、1,604 个四端绑定冷重放通过，EU IT 11/11 含字整图通过桌面与移动验收） |
| MA-057 | JE-1000F/EU Web 含文字完整面板图资产收口 5B（在第 5 刀后、6/7 刀前的独立 PR，含本登记行所在 #1067）：从操作者提供的 `Jackery Explorer 1000 User Manual (JE-1000F) V2.0 EU-UK-2026-06-18.pdf` 忠实抽取 DE/IT（以及同一 PDF 中该目标实际要求的语言）Overview、Operation、Charging 完整面板，登记 source page/crop、locale、content SHA-256、source-fragment SHA-256 和 manifest/recipe，并把 JE-1000F/EU 必需 slot 的 `editable-fallback` / `missing` 清零。该授权只覆盖源 PDF 的像素忠实抽取、Web figure 资产登记/绑定及其随附 hash/pin 更新，不授权重绘或改写图中文字，不把规格、Warranty、LCD、Troubleshooting、Symbols 表格截图化，也不扩展到线上发布、F6/线上 Base 写入、`.github/workflows/**`、依赖版本、Base schema 或 IDML approved reference-layout 几何/内容重审批。PR 必须从 #1067 合入后的最新 main 建立，最终 head 所有检查成功、无 changes-requested、无未解决讨论、mergeable=CLEAN，且 DE/IT 桌面/移动截图和 coverage inventory 证明所列 slot 仅为 `finished-panel` / `approved-composite` 后方可 squash merge。 | 「EU 意大利语用‘无字底图 + 本地化 HTML 文字/引线’重绘……这个记债，也要统一用含文字整图」并承接「授权你整个目标完成 都可以 合入」2026-09-05 | 5B 资产 PR 合入 main 且 JE-1000F/EU 必需图 slot 债清零，或操作者撤销 | 已失效（2026-09-06 #1068 squash `4da3121a` 进 main；EU 五语 55/55 `approved-composite`，其中 IT 11/11） |
| MA-058 | 跨渲染器 IR 七刀最终验收所需的 JE-1000F/EU `review-asis` 语言范围兼容 PR，含本登记行：历史六语审稿索引只按页面显式 `\HBApplyLang{...}` 投影到当前 EN/FR/ES/DE/IT 范围，并同步裁剪共享 Preface 的语言目录；不修改 `docs/_review`、成品图资产、源表或公开 CLI。最终 head 必须全部检查成功、无 changes-requested、无未解决讨论且 mergeable=CLEAN 后方可 squash merge；合入后从新 main 重建 EU Web并确认 76 页、UK=0、EU 55/55 `approved-composite`、IT 11/11 含字整图，桌面与 390 px 无横向溢出。**不含**线上发布、F6/线上 Base 写入、`.github/workflows/**`、依赖版本、Base schema 或 approved reference-layout 变更。 | 「授权你整个目标完成 都可以 合入」及「docs/_review/JE-1000F/EU 审稿包在别的分支上，可以建 worktree 去构建」2026-09-05；「无字底图 + 本地化 HTML 文字/引线只记债，也要统一用含文字整图」2026-09-06 | 本 PR 合入并完成新 main EU Web 重建验收，或操作者撤销 | 已失效（2026-09-06 #1071 squash `db0e067b` 进 main；76 页、UK=0、EU 55/55、IT 11/11、桌面与 390 px 无横向溢出） |
| MA-059 | `docs/manual-ir-cut4-closeout` 最终记录收口 PR（含本登记行）：只修正第 4 刀实施记录的完成状态和真实验证证据、把第 5 刀及 Web 资产覆盖计划中 5B 的历史开放债务标记为已由 #1068 清零，并关闭 MA-056/MA-058。不得修改运行时代码、测试、资产、配置、审稿包或生成物。最终 head 的每项检查必须成功、无 changes-requested、无未解决讨论且 mergeable=CLEAN 后方可 squash merge。 | 「授权你整个目标完成 都可以 合入」2026-09-05；「无字底图 + 本地化 HTML 文字/引线只记债，也要统一用含文字整图」2026-09-06 | 本收口 PR 合入 main，或操作者撤销 | 已失效（#1072 squash `588f97f7` 已合入） |

| MA-042 | PR #1045（公共 IR 读取与解析完整性修复，含本登记行）进入 main。最终 head 全部检查通过、无 changes-requested 或未解决评审讨论后 squash merge。下一批从统一 main 基线接入真实 Web 的公共 IR 消费路径并退出相应重复读取；后续 PR 另行评审，不扩大本次合入授权。 | 「好 合入 然后 下一批」2026-09-05 | #1045 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1045 squash `54779cf4` 已合入；最终 head 17/17 成功、无待解决评审，随本次授权补记） |

| MA-043 | PR #1046（Web 规格表消费公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。之后从统一 main 开下一批公共 IR 调用方迁移；本条不扩大到后续 PR 或 JP 原生版式修复。 | 「合入 继续」2026-09-05 | #1046 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1046 squash `251c774c` 已合入；最终 head 17/17 成功、无待解决评审，随本次授权补记） |

| MA-044 | PR #1047（Web LCD／故障排除公共 IR 消费，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后从统一 main 继续公共 IR 调用方迁移；不扩大到后续 PR 或 JP 原生版式工作。 | 「合入继续」2026-09-05 | #1047 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1047 squash `482b011a` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-045 | PR #1048（独立 Markdown 规格表接入公共 IR，含本登记行）进入 main。最终 head 全部检查通过、无 changes-requested 或未解决评审讨论且对齐 main 后 squash merge。随后从统一 main 继续公共 IR 调用方迁移；不扩大到后续 PR 或 JP 原生版式工作。 | 「合入继续」2026-09-05 | #1048 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1048 squash `39d71cfa` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-046 | PR #1049（Web 提示框通过公共 IR 交接，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决评审讨论且对齐 main 后 squash merge。随后从统一 main 迁移独立 MyST 提示框；后续 PR 另行评审，不扩大合入授权或涉及 JP 原生版式。 | 「合入继续」2026-09-05 | #1049 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1049 squash `fa502dd0` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-047 | PR #1050（独立 MyST 提示框接入共享 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决评审讨论且对齐 main 后 squash merge。随后从统一 main 继续处理复合插图内部提示框的公共 IR 路径；后续 PR 另行评审，不扩大合入授权或涉及 JP 原生版式。 | 「合入继续」2026-09-05 | #1050 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1050 squash `48aed2a7` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-048 | PR #1051（Web Inbox 与内部 TIP 接入公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后从统一 main 继续公共 IR 的真实组件调用方迁移；后续 PR 另行评审，不扩大到 JP 原生版式。 | 「合入继续」2026-09-05 | #1051 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1051 squash `4ec51446` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-049 | PR #1052（Web FCC 双栏组件接入公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后从统一 main 继续迁移公共 IR 真实组件调用方；后续 PR 另行评审，不扩大到 JP 原生版式。 | 「合入继续」2026-09-05 | #1052 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1052 squash `3a437a32` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-050 | PR #1053（Web 警示等级说明表接入公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后从统一 main 继续图标对照表的公共 IR 调用方迁移；后续 PR 另行评审，不扩大到 JP 原生版式。 | 「合入继续」2026-09-05 | #1053 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1053 squash `d14ae45e` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-051 | PR #1054（Web 图标对照表接入公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后从统一 main 继续公共 IR 真实组件调用方迁移；后续 PR 另行评审，不扩大到 JP 原生版式。 | 「合入继续」2026-09-05 | #1054 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1054 squash `b379331d` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-052 | PR #1055（Web App 下载双栏接入公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后从统一 main 继续公共 IR 真实调用方迁移；后续 PR 另行评审，不扩大到 JP 原生版式。 | 「合入继续」2026-09-05 | #1055 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1055 squash `2f295daf` 已合入；最终 head 17/17 成功，随本次授权补记） |

| MA-053 | PR #1056（Web App 添加设备按钮接入公共 IR，含本登记行）进入 main。最终 head 全检查通过、无 changes-requested 或未解决讨论且对齐 main 后 squash merge。随后构建并展示本地网页效果；不授权其他 PR 合入、线上发布或 JP 原生版式调整。 | 「合入 给我看网效果」2026-09-05 | #1056 合入即失效；下次实质性触碰补记 | 已失效（2026-09-05 #1056 squash `5788b59f` 已合入；本次新增 MA-054 时补记） |

| MA-060 | PR #1064、#1065、#1066、#1073 按最新 main 解冲突、验证并依次合入，含本登记行；#1065 在 #1064 合入后改 base 为 main。每次合入前最终 head 全部检查成功、无 changes-requested 或未解决评审讨论，且包含最新 main。保留已收口的跨渲染器 IR，不涉及线上 Base 写入或正式 Web 发布。 | 「把#1064、#1065 、#1066 、#1073按照最新main，无冲突合入，然后再针对文档web化，做一轮阶段分析」2026-09-06 | 四个 PR 全部合入即失效 | 已失效（2026-09-06 #1064/#1065/#1066/#1073 全部合入） |

| MA-061 | 本批三个 EU 英文 Web 目标的必要合入与 Git-only 正式发布步骤：auto-manual PR #1074（JBP-3600A）、#1075（JS-100I）、#1076（JA-AD01A，含本登记行）的 Git 结构源、资产、验证记录及本批 Git-only 发布说明；以及 Hello-Docs 中由这三目标新增 `sources/web/**`、对应聚合 `web/**` 与 manifest 组成的 `docs/publish/**`-only 发布 PR。保留 Hello-Docs 既有发布源，禁止写入任何线上多维表、触发 Print Publish、夹带额外 IDML 或 IR 重构。每个 PR 的最终 head 必须包含当时 latest main、全部检查成功（包括非 required，pending/queued 不算成功）、无 changes-requested、无未解决评审讨论且可合并，方可 squash merge；Hello-Docs 发布 PR 还必须通过范围门禁并只包含上述三目标的发布增量。 | 「需要先通过不写线上多维表的形式实现上线（能在read the doc访问），你把这个执行贯彻下去」2026-09-06 | #1074/#1075/#1076 及限定的 Hello-Docs 发布 PR 全部按门禁合入，且三目标 Read the Docs 真实页面完成访问验收；或操作者撤销 | 已失效（#1074 `5e176b5e`、#1075 `d2b4629b`、#1076 `d1b12bf8`、Hello-Docs #69 `6701a9bd` 均已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |

| MA-062 | 「先发再清」序列：auto-manual PR #1078（JBP-3600A LCD 错挂修复，含本登记行）先合，随后按 MA-061 已授范围依次合 #1074、#1075、#1076，最后合 Hello-Docs 的 `docs/publish/**`-only 发布 PR #69 并完成三目标 RTD 真实页面验收。每次合入前该 PR 最终 head 必须包含当时 latest main、全部检查成功（含非 required；pending/queued 不算成功）、无 changes-requested、无未解决评审讨论。**不含** 16 张带印刷版式残骸插图的裁切重切（明确留到上线之后的独立 PR），也不含任何线上多维表写入、Print Publish 触发或 IR 重构。 | 「A」2026-09-07（在「先发再清 / 先清再发」两案中选定 A：先合 #1078 → #1074/#1075/#1076 → #69 上线，重切后置） | #1078 合入、MA-061 批次全部合入且三目标 RTD 页面验收完成即失效 | 已失效（#1078 squash `f2f8abfc` 及 MA-061 批次、Hello-Docs #69 均已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |

| MA-063 | PR #1082、#1081、#1085、#1086、#1087、#1088、#1089、#1090：本批八本欧规英文 Web 工程交付，含本登记行。先合 #1082，再把依赖分支转到最新 main，按最终 head 全部检查成功、无 changes-requested 和未解决讨论、包含最新 main 后依次 squash merge。允许为集成修复冲突和回归；不包括其他 PR、线上多维表写入、OSS 上传或正式发布。 | 「先合入 你再开4个」2026-09-09 | 八个 PR 全部合入即失效；随后新四本仅授权开发与开 PR，不自动授权合入 | 已失效（2026-09-09 八个 PR 全部合入） |
| MA-064 | PR #1091–#1099 九本独立欧规英文手册及 #1100 JE-3000C 插图修正，含本登记行；允许为集成修复冲突与回归。逐个对齐最新 main，最终 head 全检查成功、无 changes-requested 和未解决评审讨论后 squash merge。随后仅通过既有 Git-only 发布路径更新本批独立手册 Read the Docs 预览；发布 PR 限 Hello-Docs 的 `docs/publish/**`，同样全绿并核验范围后合入。排除组合项目、线上多维表写入、OSS 上传、Print Publish 和无关 PR。 | 「下一步」2026-09-12（承接集中合入剩余九本和插图修正、集成验证、更新 RTD 预览的下一步） | 本批合入及 RTD 实页验证完成，或操作者撤销 | 已失效（#1091–#1100 共 10 个 PR 均已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |

Note: MA-003 superseded the earlier per-PR-review decision for #738–#742 —
the operator re-decided after reviewing the batch's CI state.

## Gate decision addenda

This table records releases of implementation gates. It does not create a new
self-merge authorization; PR merge authority still comes from the registry
above.

| ID | 范围 | 操作者原话与日期 | 保留边界 | 状态 |
| --- | --- | --- | --- | --- |
| GD-001 | Workstream W 剩余 Stage 4 与全部 Stage 5 gate，包括 F6 production seed | 「814批准：Stage4全部放心；Stage5全量放行」2026-07-31 | K14 首次计时回滚演练、下一条真实产线 ≤2 操作者日只在真实现场事件后记录；不伪造结果 | 已执行；证据见 [`../reviews/workstream_w_closeout_2026-07-31.md`](../reviews/workstream_w_closeout_2026-07-31.md) |


### MA-066 接棒澄清（2026-09-13）

用户要求完成 #1103 全部 checklist；上线不抽取正文结构化数据到线上，
目检由用户后续执行。总计划仍须完整技术/运营证据后才能合入。用户选择
GitHub Issues 反馈、夏冰负责。上表对 #1118 的分支别名登记只明确既有
清单内修订切片，不扩大到新产品、翻译、线上数据写入或 workflow。


### MA-066 operational checkpoint — 2026-09-13

The [checkpoint](manual_operations_closeout_20260913.md) records all-green
implementation/business merges, approved metadata readback and Issue closure.
#1103 is still Draft and unmerged: final online verification is blocked by
a current-client Cloudflare challenge. MA-066 does not allow bypassing that
challenge or treating partial resources as full acceptance. Its final
all-checks/reviews/ancestry protocol remains unchanged; it expires only on
actual umbrella completion/merge or operator revocation.


### MA-066 explicit final acceptance adjustment — 2026-09-14 UTC

Operator answer: **“后置剩余复验，收口合入 #1103”**.
The preceding question disclosed 23 canonical pages/608 resources verified and
101 remaining reads, of which98 files are unchanged and the changed items are
the homepage plus two JE-500A images. The operator accepts those remaining
online reads as **user-deferred**, to be carried by later visual acceptance;
they must never be represented as passed. See the
[exact decision](manual_operations_evidence/20260913/final-acceptance-deferral.json)
and [accepted closeout](manual_operations_closeout_20260913.md).
This supersedes the preceding checkpoint's requirement to wait for automatic
client access recovery before closing the umbrella. Every final-head CI check,
latest-main ancestry, no changes-requested review and no unresolved thread
still applies. No challenge bypass or further Cloudflare request is authorized.
MA-066 expires automatically after the actual #1103 merge.

### MA-067 — Manual Center directory and keyword search

| ID | 范围 | 操作者原话与日期 | 失效条件 | 状态 |
| --- | --- | --- | --- | --- |
| MA-067 | `codex/manual-center-search-preview` 的首页紧凑列表、静态跨手册正文检索及配套测试文档，含本登记。完成本地校验、最终 head 全部检查成功、无 changes-requested/未解决讨论、包含最新 main 后 squash merge；允许既有工程镜像同步和 RTD 构建上线及限定首页/搜索资源验收。不含源说明书改写、线上表写入、OSS、workflow 或依赖变更。 | 「上线」2026-09-13（审阅本地 18824 预览后） | 本 PR 合入及部署验收完成，或撤销 | 已失效（#1133 squash `f88fd0b9` 已合入；线上检索索引 `portal-search-index.js` 返回 200） |
| MA-070 | 本 PR（`feat/je1000f-eu-localized-panels-to-frozen-source`，**含本登记行**）——把 #1068 已抽取、已登记 `asset_registry.csv` 且状态为 ✅成品 的 JE-1000F/EU es/de/it 本地化整图面板（各 11 张，共 33 张）从 `tests/fixtures/phase2/_attachments/web_composites/` 落进冻结源 `manual_sources/JE-1000F/EU/en-fr/2.0/phase2/`，并在该源的 `web_composite_manifest.json` 补 33 条绑定；其中 3 条 `product-overview.front` 的 `source_fragment_sha256` 按冻结源实际构建重算（夹具值是对夹具文案算的，不可搬运），其余 30 条与夹具一致。**同时授权**随后按 MA-069 同一 Git-only 形状把 es/de/it 三个单语路由发布到 Hello-Docs `docs/publish/**`（diff 只允许含该子树）并验证 RTD 线上页面。**不含**：uk（`model_languages.csv` 记 JE-1000F 不出乌克兰语）；六语合订本；源稿语言缺陷的修字（如 it 车充页 `e venduto` 缺重音、de p60 标签——按 `asset_sources.csv` 既定口径像素忠实保留）；JBP-2000B / JE-2000E / JE-3000C / JE-3600A 的多语（实测缺按型号×按语言的页面接线与配方，属独立工作线）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「不是可以都走git only吗？？」2026-09-14（指出 `manual_sources/` 是 Git 目录、该改动走 PR 即可，不需按线上表写入设门） | 本 PR 合入且 es/de/it 三路由完成 RTD 上线验证，或操作者撤销 | 已失效（#1138 squash `58de12fd` 已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-071 | 本 PR（`feat/eu-single-language-je2000e`，**含本登记行**）——把 **JE-2000E/EU** 接进五条欧规单语线（fr/es/de/it/uk）：五个 `config.eu-*.yaml` 声明该 target；五份 `manual_eu-single-*.yaml` 补 `07_extra_battery` 能力载体页并为 `03_product_overview`/`05_operation_guide`/`12_app_setup` 加 JE-2000E 覆盖；新增 15 份按语言模板与 5 份 `12_app_setup_je2000e_<lang>.yaml` 配方；冻结源 `Spec_Master.csv` 补 `dc_expansion_input` 两行的五语 `Row_label_*`（原为空、英文泄漏进规格表）并同步 `source_manifest.json`。**全部译法取自已发布 EU PDF**（HTE152 欧英规 V2.0-2026-08-03，概览页 en8/fr27/es46/de65/it84/uk103、操作页 en13/fr32/es51/de70/it89/uk108、规格页 en21/fr40/es59/de78/it97/uk116）；两处例外已在 PR 正文标明：合并标题「LEFT AND RIGHT SIDE VIEWS」PDF 无对应（由 PDF 自身左右视图词汇合成），德语右视图 PDF 印成西语 `VISTA LATERAL DERECHA`（源稿缺陷，改用仓库既有德语译法）。配套更新五处棘轮/golden（`page_roles.py` 给 05/12 加与 03 同形 pattern、`test_pilot_configs.py` 期望、`template_structure_parity.json`、`source_manifest.json`、四份 eu-single family diff），逐项理由见 PR 正文。**同时授权**随后按 MA-069/MA-070 同一 Git-only 形状把这五个路由发布到 Hello-Docs `docs/publish/**`（diff 只允许含该子树）并验证 RTD。**不含**：JBP-2000B / JE-3000C / JE-3600A 的多语（实测各自缺不同的按型号接线，另立）；源稿语言缺陷的修字；其余 31 行未本地化规格标签的批量填充（无验证译法，且不泄漏进输出）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「继续做多语言内容 我给你发过文件目录的 都走git only 线上表先不写」2026-09-14 | 本 PR 合入且五路由完成 RTD 上线验证，或操作者撤销 | 已失效（#1139 squash `b828eb74` 已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-072 | 本 PR（`feat/bp-eu-single-language-routes`，**含本登记行**）——把 **JBP-2000B/EU** 接进四条欧规单语线（fr/es/de/it）：四个 `config.bp-eu-<lang>.yaml`、四份 `region_profiles/eu-<lang>.yaml` 并经 **`tools/skeleton_resolve.py emit`** 生成四份 `manual_bp-eu-<lang>.yaml`（清单为生成物，不得手写）；新增 `page_bp/<lang>-web/` 四组共 16 份载体（前言 + 连接 + 充电 + 操作）；`asset_registry.csv` 的 `page/jbp2000b_eu/cover` 由「按语言/en」改为「中立」；一并修掉 #1139 误给 `manual_sources/JE-2000E/.../Spec_Master.csv` 加的 BOM 并重算 `source_manifest.json`。**主机通称的每种变格形式都逐句取自操作者提供的已发布 EU 手册 V2.0-2026-09-11**（不能用单个替换变量：模板把冠词写在占位符外，而通称为阴性，德语形容词变尾、乌语变格）。封面改中立的依据：EU 合订本只有一张封面，六语区共用，已按该 PDF 第 1 页核实。配套更新五处棘轮（`test_asset_registry.py` 的 page_bp 目录名去 `-web` 后缀再当语言、`test_capability_pages` 36→40、`test_manifest_family` 37/31→41/35、`template_structure_parity.json`、`family/index.yaml` 补四条折叠 + 四份 diff），逐项理由见 PR 正文。**同时授权**随后按 MA-069/070/071 同一 Git-only 形状把这四个路由发布到 Hello-Docs `docs/publish/**`（diff 只允许含该子树）并验证 RTD。**不含**：**uk**（`LANG_PARITY_FOREIGN_SHELL` 对合规声明页与 TOC 报红，而这两页按已发布 PDF 就是全书共用的单份英文，需先单独裁决是否给该门加窄豁免——那会影响所有非拉丁语言线，操作者已选「先发四语」）；JE-3000C / JE-3600A 的多语；规格数据里既有的弯撇不一致；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「继续做 JBP-2000B」+ 提供 EU 手册 V2.0-2026-09-11 PDF + 「B」（在 A 加门豁免 / B 先发四语 / C 翻译合规页 三案中选定 B）2026-09-14 | 本 PR 合入且四路由完成 RTD 上线验证，或操作者撤销 | 已失效（#1140 squash `2c04d5d8` 已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-073 | 本 PR（`feat/eu-single-language-je3600a`，**含本登记行**）——JE-3600A/EU 多语的**第一步（模板与产品事实）**：`data/model_languages.csv` 补 `JE-3600A_EU = en;fr;es;de;it`（此前缺行致 fail-open 当它出乌克兰语；依据=已发布欧英规手册 91 页、全书西里尔字符数 0、目录语言标记仅 EN/FR/ES/DE/IT）；新增 12 份按语言模板（`targets/je3600a/{03_product_overview,05_operation_guide,07_extra_battery}_{fr,es,de,it}.rst`）；四份 `manual_eu-single-*.yaml` 加对应 `model_overrides`；重建四份 eu-single family diff。素材来源：章节标题用仓库既有审定译文、LCD 模式表用各语言通用模板现成译文、正文逐句取自已发布 EU PDF；alt 无障碍描述 PDF 无对应，按各语言既有体例撰写（PR 正文标明）。**刻意不做**：不在 `config.eu-*.yaml` 声明 JE-3600A——`07_extra_battery` 的能力为 TRUE 而其连接示意图 `connections/je3600a/{expansion,accessories}` 是**英文烧字图**（实测 accessories 图内含 Expansion Cable / User Manual / Sold separately），无 fr/es/de/it 版本，声明 target 会让 check-all 红。**同时授权**后续的插图抽取单（从已发布 EU PDF 抽四语版连接示意图、登记 registry、再声明 target 并按 Git-only 形状发布到 Hello-Docs `docs/publish/**` 并验证 RTD）。**不含**：uk（产品不出该语言）；JE-3000C 的多语；`LANG_PARITY_FOREIGN_SHELL` 豁免；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「继续做 JE-3600A」+「C 然后 A」2026-09-14（在 A 抽图 / C 先固化模板 两案中选定先 C 后 A） | 本 PR 合入即失效；插图抽取单另记 | 已失效（#1142 squash `7e3457f8` 已合入） |
| MA-074 | 本 PR（`feat/je3600a-localized-connection-diagrams`，**含本登记行**）——JE-3600A/EU 多语第二步（插图与上线）：自已发布欧英规手册 p32/p49/p66/p83 按 bbox_pt (28,108,346,304)/(28,382,346,461) @300dpi 抽出 fr/es/de/it 的 `connections/je3600a/{expansion,accessories}` 共 8 张，`asset_registry` 两行更新为五语（各 5 个哈希）；英文两张图改名带 `-en` 后缀——**必需**，解析器对无 `-xx` 后缀的文件名跳过语言过滤，不改名则四语都会解析到英文图（改名后英语路由实测仍 check OK）；仅在 `config.eu-fr/es.yaml` 声明 JE-3600A。**源稿缺陷按既定口径像素忠实保留并记入 registry 备注**：德语间距标注印作「≥Mindestens ≈ 200 mm」、意语印作「≥Almeno 200 mm」（≥ 符号与「至少」一词重复叠加，德语另有游离 ≈），法语两处标注空格不一致。**同时授权**把 fr/es 两个路由按 Git-only 形状发布到 Hello-Docs `docs/publish/**` 并验证 RTD。**不含**：de/it 上线（2026-09-15 订正：原记「被 `resolve_app_control_label_role` 挡住」为**错误诊断**——实测该分类器接受德/意的实际标签与 PDF 按钮名；当时那次报错由我自己临时写进 `Spec_Master` 的填充引起，撤回后未重跑即下了结论。de/it 已由 MA-076 补发）；JE-3000C 多语；`LANG_PARITY_FOREIGN_SHELL` 豁免；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「照口径原样入库，备注记下 de/it 那两处」+「先发 fr/es 两语」2026-09-14 | 本 PR 合入且 fr/es 完成 RTD 上线验证，或操作者撤销 | 已失效（2026-09-15 fr/es 已上线；de/it 由 MA-076 补发） |
| MA-075 | 本 PR（`feat/eu-single-language-je3000c`，**含本登记行**）——把 **JE-3000C/EU** 接进五条欧规单语线（fr/es/de/it/uk）：`data/model_languages.csv` 补 `JE-3000C_EU = en;fr;es;de;it;uk`（等于家族并集，显式登记以免依赖 fail-open；依据=已发布欧英规手册 102 页六语、西里尔 22064、乌语概览 p88）；新增 10 份按语言模板（`targets/je3000c/{03_product_overview,05_operation_guide}_{fr,es,de,it,uk}.rst`）；五份 `manual_eu-single-*.yaml` 加 `model_overrides`；五个 config 声明该 target；重建四份 eu-single family diff。**本型号零新增翻译**：与英文目标模板的差异全部是结构性的（去掉 LED 灯章节/行、去掉低功耗设备段、概览页去掉 LED 与 USB-C 低功率行），按**占位符**（语言无关）定位对各语言既有审定模板做变换，正文一句未改写。03 五语的占位符序列与英文目标模板逐项一致。**不含**：JE-3000C 的 05 保留各语言既有那句更完整的节能表述（英文目标模板省掉了 LCD 图标从句），差异已在 PR 正文标明——逐语言改句属改写，不在本 PR 做；`app.py` 的 dc-usb 判定放宽；`LANG_PARITY_FOREIGN_SHELL` 豁免；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「继续做 JE-3000C」2026-09-15 | 本 PR 合入且五语完成 RTD 上线验证，或操作者撤销 | 已失效（#1145 squash `7152778b` 已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |

### MA-077 — 恢复说明书首页并保存产品中心分支

| ID | 范围 | 操作者原话与日期 | 失效条件 | 状态 |
| --- | --- | --- | --- | --- |
| MA-077 | `codex/restore-manual-center`：回退 #1144，恢复说明书首页和原有全文检索。产品中心完整版本已保存到 `codex/product-hub-preserved-20260915`（9306967b）。包括回退带来的文件移除与配套记录，保留其他产品文档更新。最终 head 全部检查成功、无 changes-requested/未解决讨论、包含最新 main 后允许 squash merge，并通过既有镜像/RTD链路更新线上首页及实页验证。不含线上表写入、OSS、workflow 或依赖修改。 | 「把这个保存为一个分支，然后 main 回退到原来只有说明书那个样子」2026-09-15 | 本回退合入并完成部署验证，或撤销 | 已失效（#1146 squash `25ad79aa` 已合入；说明书首页与检索线上正常） |
| MA-076 | 本 PR（`fix/je3600a-de-it`，**含本登记行**）——把 **JE-3600A/EU 的 de/it** 补进单语线：两个 config 声明该 target 并补身份白名单；模板与四语插图已由 #1142/#1143 入库，本 PR 不新增内容。**同时订正 MA-074 的错误排除理由**。**同时授权**把 de/it 按 Git-only 形状发布到 Hello-Docs `docs/publish/**` 并验证 RTD。**不含**：`LANG_PARITY_FOREIGN_SHELL` 豁免；JE-3600A 的 uk（产品不出该语言）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「app.py 那条规则 是啥」2026-09-15（追问导致复核，查出原诊断有误） | 本 PR 合入且 de/it 完成 RTD 上线验证，或操作者撤销 | 已失效（2026-09-15 撤回：**该行的前提也是错的**——我复核时只跑了 `check`（绿）就断定 de/it 可用，实际 `md` 步在 `parse_app_add_device_html`（web IR 整文档组件发现，读渲染后的 HTML）仍报同一错误。CI 的 check-all 只跑 `check`，所以这个缺陷不会被 CI 捕获。de/it 的 target 声明已由本次撤回） |
| MA-078 | PR #1149（`docs/fable-delegation-rule`，**含本登记行**）——`AGENTS.md` 新增 `## 9. Delegation (Fable)`：Fable 不亲自读文档 / 写代码 / 跑测试，任务拆解后经 Sub-agent 与 Dynamic Workflow 派发，自身回合只做路由、交底与验收；派活时受派方最高只到 Opus，唯一例外是高风险架构评审可派给 Fable。规则写成「When the active model is Fable」条件句，经操作者确认**只约束 Fable**，对 Codex 与其他 Claude 模型不生效。**不含**：放宽到全体 agent；改 `CLAUDE.md`（第二个提交已把该节从 `CLAUDE.md` 移出，最终 diff 仅 `AGENTS.md` + 本登记行）；§1–§8 既有编号重排；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「你。 合入」2026-09-15（承接同轮「合入了吗」的状态汇报与不自合说明） | 本 PR 合入即失效 | 已失效（#1149 squash `b7727c01` 已合入） |
| MA-079 | 手册运营规划第 0 步批次，仅限四个 PR：#1150（`docs/manual-operations-growth-plan`，规划文档+D1–D4 决策回填，**含本登记行**）、#1151（`feat/portal-cwa-beacon`，默认关的 CWA 统计机制）、#1152（`feat/portal-email-feedback`，售后邮箱渠道+3 工作日 SLA，堆叠于 #1151）、#1153（`docs/rtd-custom-domain-runbook`，域名绑定 runbook）。按 #1150 → #1151 → #1152（retarget main）→ #1153 顺序 squash merge；每个 PR 须全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested/未解决讨论、head 包含最新 main（合并链导致的 rebase/冲突解决属于授权内动作，README 导航行相邻插入的冲突已预告）。**不含**：CWA token 激活（另行 settings 切片）、实际域名绑定（操作者在 RTD/DNS 执行）、线上表 `HTML_link` 改写、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合了 按顺序把这四个都合了」2026-09-15 | 四个 PR 全部合入即失效 | 已失效（2026-09-15 #1150/#1151/#1152/#1153 已按序全部合入） |
| MA-080 | 运营第 0 步收尾对，仅限两个 PR：#1154（`docs/rtd-domain-manuals-jackery`，**含本登记行**——域名 `manuals.jackery.com` 记录+D3 暂缓裁决+MA-079 翻已失效，纯文档）、#1155（`feat/portal-cwa-activate`，CWA token 激活：settings 令牌值+secret-scan 窄白名单「精确键名+32hex」+parity 测试契约收紧为「非首页唯一允许差异=beacon 行」+文档同步）。按 #1154 → #1155 顺序 squash merge；每个 PR 须全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested/未解决讨论、head 包含最新 main（#1155 在 #1154 合入后 rebase 属授权内动作）。**不含**：实际域名绑定（暂缓中）、线上表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合了 两个都合」2026-09-15 | 两个 PR 全部合入即失效 | 已失效（2026-09-15 #1154/#1155 已按序合入，CWA beacon 线上实证） |
| MA-081 | PR #1158（`feat/portal-page-metadata`，**含本登记行**）——L3 页面元数据派生：已验证单语页构建期获得派生 title/description/OG/canonical/自含 hreflang（全部来自冻结发布身份与 catalog `language_options`，零手填）；新增 portal 设置 `site_base_url`（裸 HTTPS origin 校验，置空 fail-closed 省略绝对 URL，为 D3 换域名唯一开关）；影子 `page.html` 仅覆写 htmltitle 块，无派生标题的页面 `super()` 逐字回退（既有 parity 测试实证字节等同）；首页 canonical/OG 经模板槽注入。全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested/未解决讨论、head 包含最新 main 后 squash merge。**不含**：QR 来源参数、per-page 手填 SEO 字段、线上表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「走」2026-09-15（承接同轮切片交付汇报） | #1158 合入即失效 | 已失效（2026-09-16 #1158 已合入，线上 title/hreflang/canonical 实证） |
| MA-081 | PR #1156（`codex/portal-accessory-thumbnails`，含本登记）：补齐 JA-CA05B/EU 与 JA-CA3SA/EU 首页缩略图，保留原生产品图优先级。最终 head 包含最新 main、全部检查成功（含非必需项）、无 changes-requested 或未解决讨论后 squash merge；沿用既有镜像/RTD 发布链路。不含正文、线上表、workflow 或依赖修改。 | 「合入 然后 告诉我 cwa面板在哪里看」2026-09-15 | #1156 合入即失效，或操作者撤销 | 已失效（#1156 squash `1f96a5a3` 已合入） |
| MA-082 | PR #1159（`codex/jbp3600a-inbox-clean-art`，含本登记及插图清单测试更新）：修复 JBP-3600A/EU/en 装箱图重复边框与编号。最终 head 包含最新 main、全部检查成功（含非必需项）、无 changes-requested 或未解决讨论后 squash merge。 | 「合入」2026-09-15（承接 #1159 修复交付） | #1159 合入即失效，或操作者撤销 | 已失效（#1159 squash `6d7449dd` 已合入） |
| MA-083 | 本发布授权登记 PR（`codex/jbp3600a-inbox-release`）及 Hello-Docs 的 `codex/jbp3600a-inbox-release` 发布 PR：从已合入 #1159 的工程提交重建 JBP-3600A/EU/en 2.0，仅更新该目标冻结 Web 产物和必要聚合清单；保持其他目标不变，沿既有 RTD 链路发布并核验三张装箱图。两个 PR 均在最终 head 全部检查成功、无 changes-requested/未解决讨论、包含最新 main 后 squash merge。无线上表、队列、workflow、依赖或正文改写。 | 「重建」2026-09-15（承接 #1159 合入及需重建发布的说明） | 本目标 RTD 发布验收完成，或操作者撤销 | 已失效（#1161 squash `cfa0e18d`、Hello-Docs #81 squash `68fb5609` 均已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-084 | PR #1160（`feat/portal-alias-entry`，**含本登记行**）——根别名改造为可计数印刷/QR 入口层：构建期为别名页加 noindex+指向嵌套规范页的 canonical；beacon 配置时瞬时转发换为发送窗口（load 后 ~200ms 跳转/2.5s 上限/no-JS 4s meta refresh 兜底），统计关或形状不识别保持瞬时转发；对外链接三分规范入册；parity 测试以精确逆变换钉死允许差异；顺带 MA-081 翻已失效。全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested/未解决讨论、head 包含最新 main 后 squash merge。**不含**：装配器/冻结源改动、线上表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合入吧 授权你」2026-09-16（承接同轮「合入了吗」状态汇报） | #1160 合入即失效 | 已失效（#1160 squash `9a876d44` 已合入） |
| MA-077 | 本 PR（`feat/je1000h-eu-localized-source`，**含本登记行**）——把已发布 EU 手册（HTE159 欧英规 V2.0-2026-08-03，108 页六语）的译文抽进 `manual_sources/JE-1000H/EU/en/2.0/phase2/Spec_Master.csv`：规格表 22 行 × 5 语言的 `Row_label_*`/`Value_*`（190 格）＋概览页等的规格槽 94 格，合计 **284 格**；同步重算 `source_manifest.json` 的 size/sha256 与 `files_inventory_sha256`。**方法与自校验**：值用「数值指纹定向搜索」——取 `Value_source` 的数字序列在目标语言规格页单元格内按序紧凑定位，**写入后逐行校验各语言值的数字序列与英文完全一致（22×5 全通过）**；标签优先用**跨型号术语表**（从其他型号已审定的 `Row_label_*` 汇出，19/22 覆盖），PDF 抽取仅用于术语表未覆盖的 3 条。术语表优先救回了一处真错位（`2 × DC8020 Ports` 的 PDF 标签被抽成 `1 × Entrée CA`，因两者值同格）。**发现并规避的源稿污染**：德语与意大利语规格表的 USB-C 输出行都印成法语 `2 × Sortie USB-C`，意语商标脚注夹德语 `und`——未写入，改用仓库审定译法（de `2 × USB-C-Ausgang` / it `2 × Uscita USB-C`）。**不含**：概览页的 `.label` 槽（端口名等约 18 串 × 5 语言）与 `Localized_Copy.csv` 的 25 行散文——概览页数值段按指纹对位在 es 仅 2/6、uk 0/6，未达可自校验的把握，留待下轮；JE-1000H **不声明为单语 target**，故本 PR 不产生半中半英的渲染；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「新开一轮做 JE-1000H」2026-09-15 | 本 PR 合入即失效；剩余槽位另记 | 已失效（#1162 squash `579a8487` 已合入） |

### MA-085 — 手册页标题与重复语言标记修复

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-085 | PR #1164（含本登记）：手册页共享标题改为 Manual Center，去掉单语正文开头重复的语言标记。最终 head 包含最新 main，全部检查成功、无 changes-requested 或未解决讨论后 squash merge，沿既有镜像与 RTD 链路上线并验证。不含线上表、OSS、workflow 或依赖修改。 | 「推上去」2026-09-16 | #1164 合入并完成上线验证，或撤销 | 已失效（#1164 squash `1fb2c699` 已合入；线上手册页标题为 Manual Center） |
| MA-086 | PR #1163（`feat/tools-cwa-report`，**含本登记行**）——Web Analytics 流量表格化工具 `tools/cwa_report.py`：只读 GraphQL 查询按 requestPath 分组，输出按入口分类（扫码/印刷入口=根别名、站内手册页、门户首页、站内功能页）汇总表 + Top 页面表；凭据仅环境变量（token 存本机 `~/.config/auto-manual/cwa.env`，不进仓库）；纯 stdlib 零新依赖。已用真 token 实跑且操作者真机扫码后「扫码/印刷入口」行出现（QR 归因端到端实证）。全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested/未解决讨论、head 包含最新 main 后 squash merge。**不含**：定时任务/工作流、线上表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合了 顺便把月度报表做成定时任务」2026-09-16 | #1163 合入即失效 | 已失效（#1163 squash `7b1b98c8` 已合入） |

| MA-087 | PR #1165（含本登记）及随后 `codex/jbp3600a-layout-release` 的 Hello-Docs 发布 PR：合入 JBP-3600A/EU/en 插图裁切、原生说明与保修组件修复，从合入提交重建该目标 2.0 冻结 Web 产物，发布 diff 限 `docs/publish/**` 及必要聚合清单。最终 head 含最新 main、全部检查成功、无 changes-requested/未解决讨论后 squash merge；沿既有 RTD 链路上线并核验。不含线上表、队列、OSS、workflow、依赖或其他目标正文修改。 | 「推上去」2026-09-16（审阅 18830 本地修复后） | 本目标上线验收完成或撤销 | 已失效（#1165 squash `271fa846`、Hello-Docs #82 squash `a38abd77` 均已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-088 | 本 PR（`feat/je1000h-eu-localized-panels`，**含本登记行**）及其上堆叠的 #1169（`feat/je1000h-eu-source-translations`）、#1170（`feat/je1000h-eu-localized-page-templates`）——把 **JE-1000H/EU** 的 fr/es/de/it/uk 五语补全并上线（线上现仅英语）。**#1167**：从同一源 PDF 的五个非英语语言块各裁 21 张成品整图（105 张），配方 126 条、登记表 430 行；插图按插座制式分版（英语块 BS 1363、其余 Schuko）不是同图换字，故不得复用英语图，已加哈希互斥守卫测试。**#1169**：7 张源表五语填满（spec_titles/Spec_Footnotes/Spec_Notes/symbols_blocks/lcd_icons_blocks/troubleshooting_blocks/Localized_Copy），五个 config 声明 target 并接插图清单，`data/capability_page_rules.csv` 补乌语同义词「Режим самозабезпечення」（放宽非收紧），`data/model_languages.csv` 补 `JE-1000H_EU = en;fr;es;de;it;uk`（依据=已发布手册 108 页六语、西里尔 24028、语言块 p6–107；此前缺行致 fail-open），并把 652 格译文镜像进 `tests/fixtures/phase2`（CI 的 check-all 读夹具而非冻结源，不补则五语全红）。**#1170**：`Spec_Master` 余下 15 行补齐至 53/53（那三页的英文残留真因是取数 token 回落，非模板硬编码），新增 5 份 `targets/je1000h/07_extra_battery_<lang>.rst` 并重新绑定 `battery_pack`。**同时授权**按 MA-069/070/071/072/075 同一 Git-only 形状把这五个路由发布到 Hello-Docs `docs/publish/**`（diff 只允许含该子树）并验证 RTD。**源稿缺陷按既定口径处理并记入 Milestone J**：像素忠实保留=fr `1800 W nomina` 截断、de/it 间距标注、es 装箱图单处 `HomePower`、it/uk 商标注记夹德语 `und`、de/it 未翻译的 `ENVIRONMENTAL OPERATING TEMPERATURE`、uk 的 WARNING 标签印成意语 `AVVERTENZA`；**不沿用印刷错误**=德语安全符号「禁拆解/禁明火」两格文字印反（按行键语义对正）、德语加电包页警示词印成法语 `ATTENTION`（用审定词 `VORSICHT`）、de/it 的 LCD「充电计划」整段印成法语（留空走 `icon_desc_en` 回落）。**不含**：修改印刷母版或设计源文件；F6 线上源表写入与飞书回执行（操作者定「线上表先不写」）；`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate；新模板 `:alt:` 的语言审校（印刷版无 alt，沿用 JE-3600A 式样改写，已记债待审） | 「概览页 操作图 等 按规则替换成带文字整图」+「接着做」×2 +「德语加电包扩容页的警示词印成了法语 ATTENTION 改成正确的德语」+「我让你走git-only 去publish页面 然后托管到readthedoc 把语言补全（现在只有英语）」2026-09-15/16 | 三个 PR 全部合入且五语完成 RTD 上线验证，或操作者撤销 | 已失效（操作者 2026-09-24 裁定：#1167 squash `2e71a07c`、#1170 squash `33055b60` 已合入，#1169 于 2026-09-16 关闭未合入；JE-1000H/EU 六语已上线，Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-089 | 本 PR（`feat/je1000h-web-asset-scale`，**含本登记行**）——把 JE-1000H/EU 六语 126 张成品整图由 scale 12 降到 **scale 3**（最宽 3822px → 956px，原始合计 50.4MB → 8.6MB，省 83%），同步重算配方 `expected_sha256`、`asset_registry.csv` 126 行内容哈希（含模板面向的别名键 `charging/battery_pack`，该行按 asset_key 匹配不到，需按导出路径补修）、六个插图清单、`source_manifest.json`。**起因**：Hello-Docs #83 合入后 RTD 构建失败于仓库自有守卫 `rtd_deployment_receipt.MAX_TOTAL_BYTES`——该守卫清点的是 `docs/publish/` **源树**（含 `sources/` 与 `_static/manual-assets/` 的重复副本，约 3×），合入后 630.4MB / 上限 512MB；已回滚（Hello-Docs #84）恢复可构建。实测扣除 JE-1000H 六语后其余 20 个型号已占 477.3MB，即回滚后余量仅 8.0MB；scale 4 即使只降五语也放不下（英语必须一起降）。选 scale 3 后总量约 503MB。**同时授权**用降分辨率后的产物重跑 Git-only 发布（六语暂存 → 组装 → Hello-Docs `docs/publish/**`-only PR → RTD 验收）。**不含**：改 `MAX_TOTAL_BYTES` 上限；发布树的资产去重改造（三副本结构，另立工作线）；调色板 PNG 编码（实测再省约 55%、可保 scale 4，但需改抽取管线，另立）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「1 先降分辨率发出去」2026-09-16（在 降分辨率 / 提上限 / 资产去重 三案中选定 1） | 本 PR 合入且六语完成 RTD 上线验证，或操作者撤销 | 已失效（#1172 squash `c99b7169`、Hello-Docs #83 `710eac7c`、#84 `5cc18a04` 均已合入；Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |
| MA-090 | 网页发布传送带四 PR 栈（登记行经**本独立 PR** 入库——追加在共享登记表尾部会与并行窗口天然冲突，冲突 PR 不产生 Actions 检查，故不随栈携带）：#1176（`feat/web-release-command-v2`，原 #1171 因此类冲突同树重开——`build.py web-release` 单书构建/留证/暂存/记债，默认 `--source auto`）→ #1173（`feat/web-assemble-command`——`web-assemble` 隔离 blobless 克隆装配 + three-dot 范围守卫只准 `docs/publish/**` + `--push` 才推）→ #1174（`feat/web-receipt-command`——`web-receipt` HTML_link 回执与「发布文档管理」目录首个写入器，默认 dry-run）→ #1175（`feat/web-sideload-mode`——`web-sideload` PDF 旁路进料，证据豁免双向 fail-closed + 强制「整本未结构化」记债，含 `.agents/skills/pdf-web-sideload` 与 AGENTS.md §7 注册行）。严格自底向上逐个合：每个在最终 head 上全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge；父分支删除前先手动把子 PR retarget（防 #1134 式误关）。同时授权 sideload 单书端到端试跑：选一本已在 family config 声明、无 phase2/评审源的书，按 pdf-web-sideload skill 走抽取 → MyST 包 → `web-sideload`（本地暂存+强制记债）→ `web-assemble` 本地校验后首次 `--push`（推 Hello-Docs publish 分支并开 publish→main PR，该 PR 仍由操作者合并）；试跑债务台账行经常规 PR 入库。不含：`web-receipt --write` 活表首写（目录表 `文档类型` 选项与区域/语言标签需先人工确认，试跑只出 dry-run 报告）、workflows、公开 CLI 既有行为变更、依赖版本、Base schema 等独立 gate | 「授权你合入 skill做好了吗」+「绿了就合 合完之后用sideload把一本书跑通试试」2026-09-16 | 四个 PR 全部合入且试跑的 publish→main PR 开出即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-091 | PR #1178（`fix/web-verify-rtd-containment`，**含本登记行**）——修复 MA-090 试跑实锤的阻断 bug：`web-sideload`/`web-release` 两处严格校验把 RTD 装配输出建成 build root 兄弟目录（装配器要求嵌套必炸），改为嵌套并补「mock sphinx 但真跑装配器」的测试层（修复前红、修复后绿）。最终 head 全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge。合入后 MA-090 试跑从第 3 步续跑。不含其他任何面 | 「授权」2026-09-16（针对 #1178 全绿放行的汇报） | #1178 合入即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-092 | PR #1179（`fix/web-assemble-repo-root`，**含本登记行**）——修复 MA-090 试跑第二锤：`web-assemble` 把一次性 Hello-Docs 克隆目录当 repo_root 传给装配器，暂存元数据的仓库相对路径解析错位被守卫拦下（守卫正确）；改为透传真仓库根，并补不 mock 装配器的红转绿回归测试（盲区同 #1178：编排测试 mock 掉了装配调用）。最终 head 全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge。合入后 MA-090 试跑从第 4 步续跑。不含其他任何面 | 「绿了合入 继续试跑」2026-09-16 | #1179 合入即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-093 | PR #1181（`fix/rtd-portal-ja-language`，**含本登记行**）——修复 MA-090 试跑第三锤（RTD 生产构建 34602012 实锤）：门户语言词表（tools/rtd_portal_assets/settings.json）无 ja 致首本日语 Web 书让整站构建失败，补 ja（日本語）两处镜像；同修校验环境漂移——web-assemble 聚合校验补上 RTD 同款 `-D extensions=myst_parser,tools.rtd_portal`（此前裸 sphinx 结构性拦不住此类错）；含 ko 负控与真 sphinx 复现测试。最终 head 全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge。合入经镜像同步触发 RTD 重建，JE-5000A 上线核验通过后按操作者指令执行 `web-receipt --write` | 「放行」+「核验过了直接跑 web-receipt --write」2026-09-17 | #1181 合入且 JE-5000A 上线核验完成即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-094 | PR #1182（`fix/catalog-receipt-live-vocabulary`，**含本登记行**）——按活表实测与操作者拍板对齐目录回执：`文档类型` 用专属新选项「Web手册」（避免 keyed-upsert 撞印刷手册行）、`版本` 写「未知」而真实版本串+别名 URL 进 `备注`、目录写入身份默认 user（bot 对业务 base 91403 实测）、Select 缺选项时先 field-update 追加再写、Git-only 书构建表零匹配=预期跳过（操作者定调「不用构建 git only」）。最终 head 全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge；随后以 user 身份执行 `web-receipt --write` 完成 JE-5000A/JP 目录登记（写后逐字段 GET 回读并报 record_id） | 「绿了合入 跑write收口」+「不用构建啊 git only」2026-09-17 | #1182 合入且 JE-5000A 目录登记回读通过即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-095 | PR #1183（`fix/rtd-portal-jp-region`，**含本登记行**）——门户 Region 下拉加 JP（settings.json regions 增 "JP":"JP" 独立绑定、页脚清单与市场提示语同步、真 Sphinx 测试钉死 JP 选项渲染且默认区域保持 EU 不变/MA-065 决策不动）；日规首本书 JE-5000A 已在线但门户导航不可达，此为补入口。最终 head 全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge；合入经镜像同步触发 RTD 重建后验证门户 JP 入口与日规书可达 | 「加个JP」+「绿了合入 验证JP入口」2026-09-17 | #1183 合入且 JP 入口线上验证完成即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-096 | PR #1185（`refactor/web-sideload-component-directives`，**含本登记行**）——修复线上样式空壳事故的根因改造：web-sideload 接入既有组件指令层（作者态用 {callout}/{spec-table}/{lcd-icons}/{troubleshooting}/{symbols} 等 8 个指令 → 哨兵+lineno 编译展开 → 入库态为展开产物，因 RTD 聚合构建只挂 myst+rtd_portal 且覆盖 conf），校验由单段裸 myst 改为两段（作者面指令可解析 + 禁手写组件类名/内联样式的 lint；展开后按 RTD 同构环境 sphinx -W），并重写 pdf-web-sideload skill 作者契约与两份过时行为文档。**共享面零改动**（web_manual.css/发射器/门户模板一字节未动，操作者红线）；样式对齐经真实 CSS 选择器匹配实证（callout 7/spec-table 12/troubleshooting 15/lcd-icons 7/symbols 8 条规则命中，零裸奔）。最终 head 全部检查成功（含非必需，pending ≠ 绿）、无 changes-requested、无未解决评审线程后 squash merge；合入后按新契约重排 JHP-3600A/US/en 并覆盖上线（日语书本轮不动） | 「放行 #1185， 然后 你试一下 重排那本英语的 日语的先不管」2026-09-17 | #1185 合入且英文书重排覆盖上线即失效 | 已失效（lane 已撤除，2026-09-17） |
| MA-097 | PR #1188（`docs/manual-revitalization-plan`，**含本登记行**）——《说明书产线盘活与演进方案》v3.1 + 34 项 REV 执行台账 + 十份卫星文档衔接（操作者本人起草），外加评审补丁：撤回命令名单修正（`web-publish`→`web-release`，`web-publish` 是活着的队列模式）与门户新区域/语言三处注册的契约补记。纯文档变更，不改代码/CI/线上表。须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：任何 REV 任务的实施或完成登记（台账初始全 planned/deferred）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「合了 #1188 关了 #1180」2026-09-18 | #1188 合入即失效（状态由下一次实质性触碰本文件时补记，不单开 PR） | 已失效（2026-09-19 #1188 squash `f3f82cdc` 进 main，检查全绿；本行随本次实质性触碰顺手补记） |
| MA-098 | 两个 PR：#1191（`docs/rev-0-30-evidence`，**含本登记行**——0–30 天 REV 调查证据入库 + 台账 9 行翻 verifying/active + 续接点重写 + MA-097 翻失效；纯文档）与 #1190（`feat/web-deploy-target-check`——REV-08 实现半：verify_deployment 可选 expected_project_slug 目标项目校验 + base_url 单一事实源，测试 4210 全绿；未动 `.github/workflows/**`）。合入顺序 #1191 → #1190。各自须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：任何 REV 任务翻 done（验收权在操作者）；缺口(b) 的 workflow 接线；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema、approved reference-layout 晋升等独立 gate | 「合了 #1190 #1191 然后给个拍板建议」2026-09-18 | 两个 PR 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（2026-09-19 #1191 squash `97423e33`、#1190 squash `ce2cf4cc` 依序合入，均检查全绿；随本次实质性触碰补记） |
| MA-099 | PR #1192（`feat/web-deploy-verify-cron`，**含本登记行**）——④/⑤ 短期方案二切片：tools/verify_web_deployment_targets.py（对全部发布目标做目标 RTD 项目/内容字节/可达性 fail-closed 校验，实网正负对照已验证）+ 每日 cron 哨兵 workflow `verify-web-deployment.yml` + 队列写入器 target_rtd_url 平铺根别名→嵌套 canonical 页（含契约文档同步）。**本行显式跨过 `.github/workflows/**` 独立 gate**：操作者已两次明示批准该新增 workflow（「其它按你建议批」批准建议原文=独立定时校验工作流+哨兵，2026-09-18；「都批」含 #1192 审合，2026-09-19）。须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：任何其它 workflow 改动；HTML_link 写回时机后移（方案一，归 REV-07 后续）；不越过 F6 线上源表写入、公开 CLI 行为、依赖版本、Base schema 等其余独立 gate | 「都批」2026-09-19 | #1192 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（2026-09-19 #1192 squash `2e4bda10` 合入，检查全绿；随本次实质性触碰补记） |
| MA-100 | 两个 PR：#1194（`docs/rev-0-30-closeout`，**含本登记行**——0–30 天批次收口：执行证据五份+验收单+台账验收翻账+MA-098/099 补记；纯文档）与 #1193（`fix/web-deploy-verify-throttle`——429 节流修复：FetchSession 节流缓存/指数退避尊重 Retry-After/退出码 75=限流未决/请求预算 ~2300→64，测试 4259 全绿；未新增 workflow，仅改既有 verify workflow 的参数与哨兵文案）。合入顺序 #1194 → #1193。各自须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：其余 REV 任务翻 done 之外的台账语义变更；不越过 F6 线上源表写入、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「合并 验收」2026-09-19 | 两个 PR 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（#1193 squash `b0f60e53`、#1194 squash `bd69e1b5` 均已合入） |
| MA-101 | REV-07 两切片，合入顺序 #1196 → #1195：#1196（`feat/ops-catalog-sync`，**含本登记行**——tools/ops_catalog_sync.py sync/reconcile 双模式 + 白名单 data/ops_catalog_reconcile_whitelist.json + 台账 REV-08 翻 done/REV-07 翻 verifying；运营表实跑刷新 row20 已回读）与 #1195（`feat/web-receipt-post-deploy`——queue_record_ids 穿透进 manifest + 新 workflow `web-publish-receipt.yml`（镜像门，合并后部署验证通过才写 HTML_link，幂等+回读）+ 队列构建时写回步降级为只记证据）。**本行显式跨过 `.github/workflows/**` gate**：操作者 2026-09-18 批方案一方向（「其它按你建议批」）+ 2026-09-19「继续 从REV-07开工」+「合」逐字授权本两 PR。各自须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge；#1195 如因 #1196 先合需要 rebase，只允许 `rebase --onto` 重放增量后在新 head 上重新全绿再合。**不含**：存量 49 target 补录队列回执（待 M2 决策）；reconcile 定时化接线；其余独立 gate | 「合」2026-09-19 | 两个 PR 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（#1196 squash `ac6651df`、#1195 squash `5a791001` 均已合入） |


### MA-102 — 发布资产去重与 JE-1000H 清晰度恢复

| ID | Scope | Grant | Expiry | Status |
| --- | --- | --- | --- | --- |
| MA-102 | PR #1189（feat/web-publish-asset-pool）及 #1197（feat/asset-palette-png，含本授权登记）。允许依次 squash merge，最终 head 所有检查成功、无 changes-requested 和未解决评审线程后执行。仅这两个工程 PR，不含额外型号转换、线上表写入或独立发布包重建。 | 「帮我先把这个合入了」2026-09-19，附图明确列出 #1189 与 #1197 | 两个 PR 合入即失效 | 已失效（#1189 squash `d0d239e4`、#1197 squash `15cfcacb` 均已合入） |
| MA-103 | PR #1198（`feat/us-single-language-web`，**含本登记行**）——JE-1000F US 单语化试点的命名切片：configs/config.us-en.yaml 一行 `md_output` 保 en 旧 stem（防线上 canonical 404，JBP-2000B EU 活体先例；fr/es 走默认 `_fr`/`_es` 后缀）；门户单默认（G3）经三面证实由既有机制天然满足、零代码；+9 测试含改名孤儿化负对照。授权链=操作者 2026-09-19「就按这个」（整包含本 config 切片）+ 2026-09-20「1」（批 publish 分支清理并放行试点主线）。须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：de/it 导出行（决策 2 未批）；能力表补行（决策 3 未批）；不越过 F6 线上源表写入、`.github/workflows/**`、依赖版本、Base schema 等独立 gate | 「1」2026-09-20 | #1198 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（#1198 squash `e7f551c4` 已合入） |

| MA-104 | PR #1199 (`feat/web-product-voc`) 及其本机集成提交：HT-Docs VOC 接收、OpenClaw main 单条分析、上线部署阶段试运行；允许临时 HTTPS 隧道作为本轮实测入口并明确运行期间有效。最终 head 全部检查成功、无 changes-requested/未解决讨论后 squash merge，经已有镜像与 RTD 链路发布验证。允许标记 TEST 的专用 VOC 表联调，不含源表修改或自动对外消息。 | 「可以先上线给我去实测了吗，整个网站都是部署阶段的，不用考虑太多」2026-09-19 | 本轮试运行上线验收完成或撤销 | 已失效（操作者 2026-09-25 裁定「MA-066、MA-016、MA-104 算失效」：#1199 squash `a11e5511` 已合入，试运行验收未单独留档） |

| MA-106 | `codex/voc-modal`：将内联 VOC 表单改为点击打开弹窗，采用已确认的 Suggest an improvement / How can we improve? / Send suggestion 文案。最终 head 全部检查成功、无 changes-requested 或未解决讨论并包含最新 main 后 squash merge，经既有镜像和 RTD 链路上线验证。 | 「推上去」2026-09-19（本地预览确认后） | 本次上线验收完成或撤销 | 已失效（#1201 squash `f7367e75` 已合入；线上反馈弹窗文案已是 Suggest an improvement） |
| MA-105 | PR #1200（`fix/us-single-preface-projection`，**含本登记行**）——仅修 JE-1000F US 单语 Web 的前言投影：`manual_us-single-{en,fr,es}.yaml` 继续保留三语前言以满足 IDML/Word/PDF；Web 构建按目标语言仅投影对应 EN/FR/ES 前言，并保持正文单语。最终 head 包含最新 main，全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：自动合并后续 Hello-Docs publish PR；不扩大线上表写入、workflow、依赖、Base schema 或其他目标内容范围。 | 「授权你合入」2026-09-19 | #1200 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（2026-09-20 #1200 squash `eda171b0` 进 main，18 项检查全绿；随本次实质性触碰补记） |
| MA-107 | Hello-Docs PR #95（`publish` → `main`）——发布 JE-1000F/US 2.4 的 en/fr/es 三个单语 Web 目标：en 保持旧 stem `manual_je1000f_us`，fr/es 分别为 `manual_je1000f_us_fr` / `manual_je1000f_us_es`；三者前言只保留对应语言且 `language_scope=single`。只允许 `docs/publish/**` 变化，其余 51 个 target 逐 blob 不变。最终 head 全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge；随后盯 RTD 部署及 receipt 回读至 settled。**不含**：其他目标内容、review 分支、线上源表、workflow、依赖或 Base schema 修改。 | 「合入」2026-09-20 | #95 合入并完成部署/回执验收，或撤销 | 已失效（Hello-Docs #95 squash `0cea5bd5` 已合入；2.4 已由 2.5（Hello-Docs #119 `fe80f661`）取代；操作者 2026-09-24「MA-107 和 MA-118 也标成失效」） |

| MA-108 | `codex/voc-customer-copy`：仅移除 VOC 弹窗中的 Feishu 账号及存储平台表述，保留反馈用途与个人信息、售后提示；含测试和本登记。最终 head 全部检查成功、无 changes-requested/未解决讨论并包含最新 main 后 squash merge，经既有镜像及 RTD 上线验证。 | 「ok 推上去」2026-09-20 | 本次上线验收完成或撤销 | 已失效（#1203 squash `b6683f4c` 已合入；线上弹窗文案无飞书表述，仅预览模式提示仍写「nothing was sent to Feishu」） |
| MA-109 | 两个文档 PR，**严格按 #1184 → #1187 顺序**（#1187 的 base 是 `docs/drop-jbp3600a-readme-pin`，须待 #1184 合入后重定基到 main 再合）：#1184（`docs/drop-jbp3600a-readme-pin`，**含本登记行**——从 `README.md` 与 `user-guide/hello_auto-doc.md` 撤掉 JBP-3600A 装箱图重复边框的一次性修复指针，该指针经 `sync-hello-docs.yml` 一直显示在 Hello-Docs 仓库首页）与 #1187（`docs/restructure-hello-auto-doc`——重排 `user-guide/hello_auto-doc.md` 累积成 85 行的开头，逐句保留、仅刷新 Updated 行，已用脚本核验 82 个实质句与 17 个链接全在）。各自须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge。**不含**：撤销指针所指的 `code-as-doc/reviews/` 记录本身；`hello_auto-doc.md` 契约节内容的过期判定（只重排不删减，另立）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate。**说明**：本次聊天授权原话覆盖四个 PR，其中 #1189、#1197 已由另一窗口于 2026-09-20 02:14/02:21 依 MA-102 合入（squash `d0d239e4`、`15cfcacb`），故本行只覆盖仍未合的两个文档 PR 。**2026-09-20 补记**：#1184 已 squash `0bc7218a` 合入；#1187 的分支基于 #1184 合入前的 main，而 `user-guide/hello_auto-doc.md` 期间被其他窗口实质改动（「对外链接规范」由三种链接改为四种、`HTML_link` 由根别名改为嵌套规范页，另新增产线盘活/执行台账/入口整合等段落），机械 rebase 会把这些新内容静默改回旧文本，故 #1187 关闭，改由 `docs/regroup-auto-doc-head-v2` 在当前 main 上重做同一次重排（本行所在 PR），覆盖范围不变 | 「不用清单 你直接按顺序合」2026-09-20 | 两个 PR 合入即失效 | 已失效（操作者 2026-09-24 裁定：#1184 squash `0bc7218a` 已合入，#1187 于 2026-09-20 关闭未合入，改由 #1205 squash `05bb3847` 合入） |

| MA-110 | auto-manual PR #1204（含本登记）与 Hello-Docs PR #96，按 #1204 → 镜像同步 → #96 顺序合入：知识库/工作资料页面、docs/knowledge 构建复制与镜像保留规则，以及 Hello-Docs 的 AI 分享内容。每个最终 head 全部检查成功（含非必需项）、无 changes-requested 和未解决评审线程后 squash merge；随后通过既有 RTD 链路核验页面。只涵盖本次两项 PR，保留 docs/publish 全部说明书，不含线上源表写入或其他工程范围。 | 「合入」2026-09-20 | 两个 PR 合入并完成上线核验，或操作者撤销 | 已失效（#1204 squash `7cafd383`、Hello-Docs #96 squash `2818de85` 均已合入；知识库与工作资料页面线上正常） |

| MA-111 | PR #1207（`codex/rtd-catalog-build-cache`，含本登记）：RTD 每轮构建只校验一次冻结发布目录，页面与搜索索引复用结果，构建结束清除缓存。最终 head 全部检查成功（含非必需项）、无 changes-requested 和未解决评审线程后 squash merge；经既有 Hello-Docs 镜像同步和 RTD 链路重建，核验耗时与线上页面。保留说明书、分享资料及原有校验，不含其他代码、workflow、依赖或线上源表改动。 | 「可以」2026-09-20（回应 #1207 全绿后合入并重新构建） | #1207 合入且部署核验完成，或操作者撤销 | 已失效（#1207 squash `a15b741a` 已合入；此后 RTD 多次部署成功，Hello-Docs 每日部署校验 2026-09-24 run 35974913402 通过） |

| MA-112 | 本次分享稿与首页文案两项 PR：auto-manual #1208（`codex/knowledge-copy-local`，含本登记）与 Hello-Docs #98（`codex/share-intro-local`）。范围为知识库首页中文文案、AI 分享稿六章结构与 CLI/MCP 说明、阅读样式及已确认的 Q 版配图。各最终 head 的全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 和未解决评审线程后 squash merge；通过既有镜像和 RTD 构建核验上线。保留 `docs/publish/**` 说明书，不含其他工程、workflow、依赖或线上数据变更。 | 「推完先合入」2026-09-20 | 两项 PR 合入且线上核验完成，或操作者撤销 | 已失效（#1208 squash `3252a7b7`、Hello-Docs #98 squash `8ea83a4b` 均已合入；知识库首页文案线上正常） |

| MA-113 | 本授权登记 PR（`docs/authorize-hello-docs-99`，含本登记）与 Hello-Docs #99（`docs/knowledge-direct-editing`），按授权登记 PR → Hello-Docs #99 顺序合入。范围仅为持久登记 `docs/knowledge/**` 是 Hello-Docs 业务内容直编区，并在该目录增加 Agent 操作边界和人工内容 PR 流程；Hello-Docs #99 的最终 diff 只允许 `docs/knowledge/AGENTS.md` 与 `docs/knowledge/README.md`。两项 PR 均须最终 head 全部检查成功（含非必需项，pending 不算绿）、无 changes-requested 和未解决评审线程后 squash merge。**不含**：本次改写分享正文、修改 `docs/publish/**`、镜像工程代码、workflow、依赖、线上 Base 或 RTD 配置。 | 「提交 合入」2026-09-20 | 两项 PR 合入即失效（状态由下一次实质性触碰本文件时补记） | 已失效（Hello-Docs #99 squash `49ca66d0`、登记 #1209 squash `cd580b3c` 均已合入） |
| MA-118 | Hello-Docs PR #106（`release/je1000h-eu-20260920` → `main`，**含本登记行所在 auto-manual PR**）——按操作者「那重跑 JE-1000H 六语发布」重跑 JE-1000H/EU 六语 Git-only 发布，让线上拿到 #1197 恢复到 scale 4 的索引色成品整图。路由与手册文件名不变（`JE-1000H/EU/<lang>/md/manual_je1000h_eu_<lang>.md`），仅版本元数据 `2.0` → `2.0-20260920`（`web_publish_meta.json` 按版本不可变，同版本改内容会报 `metadata is immutable`；沿用线上既有 `2.0-20260913` 先例，手册编辑版本仍是 V2.0-2026-08-03）。已用 RTD 的确切命令（`sphinx -b html -D extensions=myst_parser,tools.rtd_portal`）本地验证：构建成功、`manual-deployment.json` 部署回执生成、en/fr/uk 逐页 28 张图零断链且 21 张为索引色、发布树 3579 文件 / 278.8MB（占 512MiB 闸门 52%）。PR diff 只含 `docs/publish/**`，越界文件 0。须在最终 head 上全部检查成功（含非必需项，pending 不算绿）、无 changes-requested、无未解决评审线程后 squash merge，合入后核对 RTD 实际构建成功与六个语言页可访问。**不含**：其余型号的调色板转换（各需自己的设计母版重渲，另立）；手册编辑版本升版或任何正文/翻译改动；`sources/` 池化（破坏冻结包自包含语义，另立）；不越过 F6 线上源表写入、`.github/workflows/**`、公开 CLI 行为、依赖版本、Base schema 等独立 gate | 「那重跑 JE-1000H 六语发布」2026-09-20 | Hello-Docs #106 合入且六语完成 RTD 上线验证，或操作者撤销 | 已失效（Hello-Docs #106 squash `ba2b0744` 已合入；操作者 2026-09-24「MA-107 和 MA-118 也标成失效」） |
