# LaTeX 样式注册表已归并

LaTeX 的公共入口、模块归属、参数命名空间和加载顺序已经统一维护在
[`../contracts/STYLE_DEFINITION.md`](../contracts/STYLE_DEFINITION.md) 的 §1、§8.2
和附录 A。

本路径暂时保留，供 `manual_style.yaml` schema v1 元数据和旧外部链接兼容；它不是
第二份注册表。不要在这里新增样式行。机器可读绑定继续以
[`../contracts/manual_style.yaml`](../contracts/manual_style.yaml) 为准。
