# =====================================================
# Auto-Manual build dependencies — HUMAN-FACING RANGES
# Python 3.12 (what CI pins, ReadTheDocs uses, and the lock is
# generated under; see pyproject.toml)
#
# CI and ReadTheDocs install from requirements.lock (Milestone K1),
# NOT from this file. Edit ranges here, then refresh the lock (see
# the lock header for the when/how) in the same PR.
#
# Covers Python-side dependencies for:
# - RST / HTML build
# - Word export
# - PDF build orchestration
# =====================================================

# Core build / config
Sphinx>=7.0,<9.0
myst-parser>=3.0,<5.0
PyYAML>=6.0
docutils>=0.20,<0.22
furo>=2024.8.6

# Asset utilities
Pillow>=10.0
numpy>=1.26
PyMuPDF>=1.24  # QC findings -> annotated-PDF renderer (tools/pdf_annotate.py)
oss2>=2.19

# Notes:
# - DOCX export via pandoc still requires pandoc installed separately.
# - Windows Word COM export still requires Microsoft Word installed locally.
# - PDF export still requires xelatex installed separately (TeX Live / MiKTeX).
# - xelatex / pandoc are system dependencies, so they are not installed by pip.
# - `oss2` is required for the optional DingTalk AliDocs queue artifact sink.
